---
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const title = "SQL Formatter - تنسيق SQL أونلاين مجاناً | Th.Tech";

const description =
  "أداة SQL Formatter مجانية لتنسيق وترتيب استعلامات SQL وتحسين قراءتها مباشرة من المتصفح بدون رفع الكود.";

const faqs = [
  {
    q: "ما هي أداة SQL Formatter؟",
    a: "هي أداة مجانية لتنسيق وترتيب استعلامات SQL وجعلها أكثر وضوحاً وسهولة في القراءة والتعديل."
  },
  {
    q: "هل يتم إرسال استعلامات SQL إلى الخادم؟",
    a: "لا. تتم عملية التنسيق داخل المتصفح مباشرة، ولا تحتاج إلى رفع استعلام SQL إلى خادم خارجي."
  },
  {
    q: "ما قواعد SQL التي يمكن تنسيقها؟",
    a: "يمكن للأداة تنسيق معظم الاستعلامات الشائعة مثل SELECT وINSERT وUPDATE وDELETE وJOIN وGROUP BY وORDER BY وWHERE وغيرها."
  },
  {
    q: "هل يمكن تصغير SQL؟",
    a: "هذه الأداة مخصصة للتنسيق وتحسين القراءة. يمكن استخدام أداة SQL Minifier المخصصة لتصغير الاستعلامات."
  },
  {
    q: "هل الأداة مجانية؟",
    a: "نعم، SQL Formatter مجانية ويمكن استخدامها مباشرة من المتصفح."
  }
];
---

<!doctype html>
<html lang="ar" dir="rtl">
<head>

  <meta charset="UTF-8" />

  <meta
    name="viewport"
    content="width=device-width, initial-scale=1.0"
  />

  <title>{title}</title>

  <meta
    name="description"
    content={description}
  />

  <meta
    name="robots"
    content="index, follow"
  />

  <meta
    name="theme-color"
    content="#7c3aed"
  />

  <link
    rel="icon"
    href={`${base}/logo1.png`}
  />

  <link
    rel="preconnect"
    href="https://fonts.googleapis.com"
  />

  <link
    rel="preconnect"
    href="https://fonts.gstatic.com"
    crossorigin
  />

  <link
    href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap"
    rel="stylesheet"
  />

  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
  />

  <script is:inline>
    (() => {
      try {
        const saved = localStorage.getItem("theme");

        if (saved === "dark") {
          document.documentElement.classList.add("dark-mode");
        }
      } catch {}
    })();
  </script>

</head>

<body>

  <Header />

  <main>

    <!-- ======================================
         HERO
    ======================================= -->

    <section class="hero">

      <div class="hero-glow glow-one"></div>
      <div class="hero-glow glow-two"></div>

      <div class="container">

        <div class="breadcrumb">

          <a href={`${base}/`}>
            الرئيسية
          </a>

          <i class="fa-solid fa-chevron-left"></i>

          <a href={`${base}/tools/`}>
            الأدوات
          </a>

          <i class="fa-solid fa-chevron-left"></i>

          <span>
            SQL Formatter
          </span>

        </div>


        <div class="hero-badge">

          <span class="badge-dot"></span>

          Database Tools

          <i class="fa-solid fa-database"></i>

        </div>


        <div class="hero-icon">

          <i class="fa-solid fa-database"></i>

          <span class="sql-mark">
            SQL
          </span>

        </div>


        <h1>

          SQL

          <span>Formatter</span>

        </h1>


        <p>

          نسّق استعلامات SQL تلقائياً واجعلها أكثر وضوحاً
          وسهولة في القراءة والتعديل مع معالجة كاملة داخل المتصفح.

        </p>

      </div>

    </section>


    <!-- ======================================
         TOOL
    ======================================= -->

    <section class="tool-section">

      <div class="container">

        <div class="formatter-card">


          <!-- TOP BAR -->

          <div class="topbar">

            <div class="project-info">

              <div class="project-logo">

                <i class="fa-solid fa-database"></i>

              </div>

              <div>

                <strong>
                  SQL Formatter
                </strong>

                <span>
                  SQL Query Formatting Tool
                </span>

              </div>

            </div>


            <div class="topbar-status">

              <span class="online-dot"></span>

              Local Processing

            </div>

          </div>


          <!-- ACTION BAR -->

          <div class="actionbar">

            <div class="action-group">

              <button
                id="exampleBtn"
                class="action secondary"
                type="button"
              >

                <i class="fa-solid fa-wand-magic-sparkles"></i>

                مثال

              </button>


              <button
                id="clearBtn"
                class="action secondary"
                type="button"
              >

                <i class="fa-solid fa-trash-can"></i>

                مسح

              </button>

            </div>


            <div class="action-group">

              <button
                id="copyBtn"
                class="action secondary"
                type="button"
              >

                <i class="fa-regular fa-copy"></i>

                نسخ النتيجة

              </button>


              <button
                id="formatBtn"
                class="action primary"
                type="button"
              >

                <i class="fa-solid fa-wand-magic-sparkles"></i>

                تنسيق SQL

              </button>

            </div>

          </div>


          <!-- WORKSPACE -->

          <div class="workspace">


            <!-- INPUT -->

            <div class="editor-card">

              <div class="editor-head">

                <div class="editor-label">

                  <span class="file-icon input-icon">

                    <i class="fa-solid fa-code"></i>

                  </span>

                  <div>

                    <strong>
                      SQL Input
                    </strong>

                    <small>
                      الاستعلام الأصلي
                    </small>

                  </div>

                </div>


                <div
                  id="inputStats"
                  class="editor-stats"
                >
                  0 chars
                </div>

              </div>


              <div class="code-area">

                <div
                  id="inputLines"
                  class="line-numbers"
                >
                  1
                </div>


                <textarea
                  id="sqlInput"
                  spellcheck="false"
                  autocomplete="off"
                  autocorrect="off"
                  autocapitalize="off"
                  placeholder="SELECT * FROM users WHERE active = 1;"
                ></textarea>

              </div>


              <div class="editor-foot">

                <span
                  id="inputStatus"
                  class="status idle"
                >

                  <span class="status-dot"></span>

                  جاهز

                </span>


                <span id="inputSize">
                  0 B
                </span>

              </div>

            </div>


            <!-- OUTPUT -->

            <div class="editor-card output-card">

              <div class="editor-head">

                <div class="editor-label">

                  <span class="file-icon output-icon">

                    <i class="fa-solid fa-align-left"></i>

                  </span>

                  <div>

                    <strong>
                      Formatted SQL
                    </strong>

                    <small>
                      الاستعلام المنسق
                    </small>

                  </div>

                </div>


                <div
                  id="outputStats"
                  class="editor-stats"
                >
                  0 chars
                </div>

              </div>


              <div class="code-area">

                <div
                  id="outputLines"
                  class="line-numbers"
                >
                  1
                </div>


                <textarea
                  id="sqlOutput"
                  spellcheck="false"
                  readonly
                  placeholder="سيظهر SQL المنسق هنا..."
                ></textarea>

              </div>


              <div class="editor-foot">

                <span
                  id="outputStatus"
                  class="status idle"
                >

                  <span class="status-dot"></span>

                  في انتظار التنسيق

                </span>


                <span id="outputSize">
                  0 B
                </span>

              </div>

            </div>

          </div>


          <!-- RESULT SUMMARY -->

          <div class="result-section">

            <div class="result-head">

              <div class="result-title">

                <div class="result-icon">

                  <i class="fa-solid fa-database"></i>

                </div>

                <div>

                  <strong>
                    Formatting Result
                  </strong>

                  <span>
                    نتيجة تنسيق استعلام SQL
                  </span>

                </div>

              </div>


              <div
                id="resultBadge"
                class="result-badge"
              >

                <span class="result-dot"></span>

                Ready

              </div>

            </div>


            <div class="result-content">

              <div class="result-message">

                <div class="message-icon">

                  <i class="fa-solid fa-circle-info"></i>

                </div>

                <div>

                  <strong id="resultTitle">
                    SQL Formatter جاهز
                  </strong>

                  <p id="resultText">
                    أدخل استعلام SQL ثم اضغط على "تنسيق SQL".
                  </p>

                </div>

              </div>


              <div class="result-metrics">

                <div class="metric">

                  <span>
                    الأسطر الأصلية
                  </span>

                  <strong id="originalLines">
                    0
                  </strong>

                </div>


                <div class="metric">

                  <span>
                    الأسطر المنسقة
                  </span>

                  <strong id="formattedLines">
                    0
                  </strong>

                </div>


                <div class="metric">

                  <span>
                    الأحرف
                  </span>

                  <strong id="formattedChars">
                    0
                  </strong>

                </div>


                <div class="metric">

                  <span>
                    زمن التنفيذ
                  </span>

                  <strong id="executionTime">
                    0 ms
                  </strong>

                </div>

              </div>

            </div>

          </div>


          <!-- TOOL STATS -->

          <div class="tool-stats">

            <div class="tool-stat">

              <div class="tool-stat-icon">

                <i class="fa-solid fa-bolt"></i>

              </div>

              <div>

                <strong>
                  Fast
                </strong>

                <span>
                  تنسيق سريع
                </span>

              </div>

            </div>


            <div class="tool-stat">

              <div class="tool-stat-icon">

                <i class="fa-solid fa-database"></i>

              </div>

              <div>

                <strong>
                  SQL
                </strong>

                <span>
                  استعلامات SQL
                </span>

              </div>

            </div>


            <div class="tool-stat">

              <div class="tool-stat-icon">

                <i class="fa-solid fa-lock"></i>

              </div>

              <div>

                <strong>
                  Private
                </strong>

                <span>
                  بدون رفع البيانات
                </span>

              </div>

            </div>


            <div class="tool-stat">

              <div class="tool-stat-icon">

                <i class="fa-solid fa-mobile-screen-button"></i>

              </div>

              <div>

                <strong>
                  100%
                </strong>

                <span>
                  متوافق مع الهاتف
                </span>

              </div>

            </div>

          </div>


        </div>


        <!-- ======================================
             INFORMATION
        ======================================= -->

        <section class="information">

          <div class="section-title">

            <span>
              Database Tools
            </span>

            <h2>
              نسّق SQL بطريقة احترافية
            </h2>

            <p>
              حوّل الاستعلامات الطويلة وغير المنظمة إلى SQL
              واضح ومنسق يسهل فهمه وتعديله.
            </p>

          </div>


          <div class="info-grid">


            <article class="info-card">

              <div class="info-number">
                01
              </div>

              <div class="info-card-icon">

                <i class="fa-solid fa-wand-magic-sparkles"></i>

              </div>

              <h3>
                تنسيق تلقائي
              </h3>

              <p>
                قم بترتيب الكلمات المفتاحية وبنية الاستعلام
                تلقائياً للحصول على كود SQL واضح.
              </p>

            </article>


            <article class="info-card">

              <div class="info-number">
                02
              </div>

              <div class="info-card-icon">

                <i class="fa-solid fa-layer-group"></i>

              </div>

              <h3>
                تنظيم الاستعلامات
              </h3>

              <p>
                يتم وضع SELECT وFROM وWHERE وJOIN وغيرها
                في بنية أسهل للقراءة.
              </p>

            </article>


            <article class="info-card">

              <div class="info-number">
                03
              </div>

              <div class="info-card-icon">

                <i class="fa-solid fa-shield-halved"></i>

              </div>

              <h3>
                معالجة محلية
              </h3>

              <p>
                لا يتم إرسال استعلاماتك إلى الخادم،
                وتتم عملية التنسيق داخل المتصفح.
              </p>

            </article>

          </div>

        </section>


        <!-- ======================================
             HOW TO
        ======================================= -->

        <section class="how-section">

          <div class="section-title">

            <span>
              How It Works
            </span>

            <h2>
              طريقة استخدام SQL Formatter
            </h2>

          </div>


          <div class="steps">


            <div class="step">

              <div class="step-icon">

                <span>
                  01
                </span>

              </div>

              <div>

                <h3>
                  أدخل SQL
                </h3>

                <p>
                  الصق استعلام SQL الذي تريد ترتيبه
                  داخل محرر الإدخال.
                </p>

              </div>

            </div>


            <div class="step">

              <div class="step-icon">

                <span>
                  02
                </span>

              </div>

              <div>

                <h3>
                  اضغط تنسيق
                </h3>

                <p>
                  اضغط على زر "تنسيق SQL" لتحليل
                  وترتيب الاستعلام.
                </p>

              </div>

            </div>


            <div class="step">

              <div class="step-icon">

                <span>
                  03
                </span>

              </div>

              <div>

                <h3>
                  انسخ النتيجة
                </h3>

                <p>
                  استخدم SQL المنسق مباشرة في مشروعك
                  أو اضغط على زر النسخ.
                </p>

              </div>

            </div>

          </div>

        </section>


        <!-- ======================================
             FAQ
        ======================================= -->

        <section class="faq-section">

          <div class="section-title">

            <span>
              FAQ
            </span>

            <h2>
              الأسئلة الشائعة
            </h2>

          </div>


          <div class="faq-list">

            {
              faqs.map((faq) => (
                <details>

                  <summary>

                    <span>
                      {faq.q}
                    </span>

                    <i class="fa-solid fa-chevron-down"></i>

                  </summary>

                  <div class="faq-answer">
                    {faq.a}
                  </div>

                </details>
              ))
            }

          </div>

        </section>

      </div>

    </section>

  </main>


  <Footer />


  <!-- ======================================
       JAVASCRIPT
  ======================================= -->

  <script is:inline>
    (() => {

      const sqlInput =
        document.getElementById("sqlInput");

      const sqlOutput =
        document.getElementById("sqlOutput");

      const inputLines =
        document.getElementById("inputLines");

      const outputLines =
        document.getElementById("outputLines");

      const inputStats =
        document.getElementById("inputStats");

      const outputStats =
        document.getElementById("outputStats");

      const inputSize =
        document.getElementById("inputSize");

      const outputSize =
        document.getElementById("outputSize");

      const inputStatus =
        document.getElementById("inputStatus");

      const outputStatus =
        document.getElementById("outputStatus");

      const formatBtn =
        document.getElementById("formatBtn");

      const exampleBtn =
        document.getElementById("exampleBtn");

      const clearBtn =
        document.getElementById("clearBtn");

      const copyBtn =
        document.getElementById("copyBtn");

      const resultBadge =
        document.getElementById("resultBadge");

      const resultTitle =
        document.getElementById("resultTitle");

      const resultText =
        document.getElementById("resultText");

      const originalLines =
        document.getElementById("originalLines");

      const formattedLines =
        document.getElementById("formattedLines");

      const formattedChars =
        document.getElementById("formattedChars");

      const executionTime =
        document.getElementById("executionTime");


      /* ======================================
         EXAMPLE
      ======================================= */

      const exampleSQL =
`SELECT u.id,u.name,u.email,COUNT(o.id) AS order_count,SUM(o.total) AS total_spent
FROM users u
LEFT JOIN orders o ON o.user_id=u.id
WHERE u.active=1 AND u.created_at>='2025-01-01'
GROUP BY u.id,u.name,u.email
HAVING COUNT(o.id)>0
ORDER BY total_spent DESC
LIMIT 20;`;


      /* ======================================
         SIZE
      ======================================= */

      function formatBytes(bytes) {

        if (bytes < 1024) {
          return `${bytes} B`;
        }

        if (bytes < 1024 * 1024) {
          return `${(bytes / 1024).toFixed(2)} KB`;
        }

        return `${(bytes / (1024 * 1024)).toFixed(2)} MB`;

      }


      /* ======================================
         LINE NUMBERS
      ======================================= */

      function updateLineNumbers(textarea, numbers) {

        const value =
          textarea.value;

        const count =
          value
            ? value.replace(/\r\n/g, "\n").split("\n").length
            : 0;

        numbers.textContent =
          count > 0
            ? Array.from(
                { length: count },
                (_, i) => i + 1
              ).join("\n")
            : "1";

      }


      /* ======================================
         EDITOR STATS
      ======================================= */

      function updateInputStats() {

        const value =
          sqlInput.value;

        const chars =
          value.length;

        const bytes =
          new Blob([value]).size;

        inputStats.textContent =
          `${chars.toLocaleString()} chars`;

        inputSize.textContent =
          formatBytes(bytes);

        updateLineNumbers(
          sqlInput,
          inputLines
        );

      }


      function updateOutputStats() {

        const value =
          sqlOutput.value;

        const chars =
          value.length;

        const bytes =
          new Blob([value]).size;

        outputStats.textContent =
          `${chars.toLocaleString()} chars`;

        outputSize.textContent =
          formatBytes(bytes);

        updateLineNumbers(
          sqlOutput,
          outputLines
        );

      }


      /* ======================================
         STATUS
      ======================================= */

      function setStatus(element, type, text) {

        element.className =
          `status ${type}`;

        element.innerHTML = `
          <span class="status-dot"></span>
          ${text}
        `;

      }


      /* ======================================
         SQL KEYWORDS
      ======================================= */

      const keywords = [
        "SELECT",
        "FROM",
        "WHERE",
        "AND",
        "OR",
        "NOT",
        "NULL",
        "IS",
        "IN",
        "LIKE",
        "BETWEEN",
        "JOIN",
        "INNER",
        "LEFT",
        "RIGHT",
        "FULL",
        "OUTER",
        "CROSS",
        "ON",
        "AS",
        "GROUP",
        "BY",
        "ORDER",
        "ASC",
        "DESC",
        "HAVING",
        "LIMIT",
        "OFFSET",
        "UNION",
        "ALL",
        "DISTINCT",
        "INSERT",
        "INTO",
        "VALUES",
        "UPDATE",
        "SET",
        "DELETE",
        "CREATE",
        "ALTER",
        "DROP",
        "TABLE",
        "VIEW",
        "INDEX",
        "DATABASE",
        "PRIMARY",
        "KEY",
        "FOREIGN",
        "REFERENCES",
        "CASE",
        "WHEN",
        "THEN",
        "ELSE",
        "END",
        "EXISTS",
        "WITH",
        "RETURNING"
      ];


      const majorKeywords = new Set([
        "SELECT",
        "FROM",
        "WHERE",
        "GROUP",
        "ORDER",
        "HAVING",
        "LIMIT",
        "OFFSET",
        "UNION",
        "VALUES",
        "SET",
        "RETURNING"
      ]);


      const joinKeywords = new Set([
        "JOIN",
        "INNER",
        "LEFT",
        "RIGHT",
        "FULL",
        "CROSS"
      ]);


      /* ======================================
         TOKENIZE SQL
      ======================================= */

      function tokenizeSQL(sql) {

        const tokens = [];

        let current = "";

        let quote = null;

        let comment = null;


        for (let i = 0; i < sql.length; i++) {

          const char =
            sql[i];

          const next =
            sql[i + 1];


          if (comment === "line") {

            current += char;

            if (char === "\n") {
              tokens.push({
                type: "comment",
                value: current.trim()
              });

              current = "";
              comment = null;
            }

            continue;

          }


          if (comment === "block") {

            current += char;

            if (char === "*" && next === "/") {

              current += next;

              i++;

              tokens.push({
                type: "comment",
                value: current.trim()
              });

              current = "";
              comment = null;

            }

            continue;

          }


          if (!quote && char === "-" && next === "-") {

            if (current.trim()) {

              tokens.push({
                type: "text",
                value: current.trim()
              });

              current = "";

            }

            current = "--";
            comment = "line";
            i++;

            continue;

          }


          if (!quote && char === "/" && next === "*") {

            if (current.trim()) {

              tokens.push({
                type: "text",
                value: current.trim()
              });

              current = "";

            }

            current = "/*";
            comment = "block";
            i++;

            continue;

          }


          if (
            quote &&
            char === quote
          ) {

            current += char;

            if (next === quote) {

              current += next;

              i++;

              continue;

            }

            quote = null;

            continue;

          }


          if (
            !quote &&
            (
              char === "'" ||
              char === '"' ||
              char === "`"
            )
          ) {

            quote = char;

            current += char;

            continue;

          }


          if (!quote && /\s/.test(char)) {

            if (current.trim()) {

              tokens.push({
                type: "text",
                value: current.trim()
              });

              current = "";

            }

            continue;

          }


          if (
            !quote &&
            (
              char === "(" ||
              char === ")" ||
              char === "," ||
              char === ";"
            )
          ) {

            if (current.trim()) {

              tokens.push({
                type: "text",
                value: current.trim()
              });

              current = "";

            }

            tokens.push({
              type: "symbol",
              value: char
            });

            continue;

          }


          current += char;

        }


        if (current.trim()) {

          tokens.push({
            type:
              comment
                ? "comment"
                : "text",

            value:
              current.trim()

          });

        }


        return tokens;

      }


      /* ======================================
         FORMAT TOKEN
      ======================================= */

      function upperKeyword(value) {

        const upper =
          value.toUpperCase();

        if (
          keywords.includes(upper)
        ) {
          return upper;
        }

        return value;

      }


      /* ======================================
         FORMAT SQL
      ======================================= */

      function formatSQL(sql) {

        const tokens =
          tokenizeSQL(sql);

        if (!tokens.length) {
          return "";
        }


        const lines = [];

        let current = "";

        let indent = 0;

        let parenDepth = 0;

        let afterFrom = false;


        function indentString() {

          return "  ".repeat(
            Math.max(0, indent)
          );

        }


        function flush() {

          const clean =
            current.trim();

          if (clean) {
            lines.push(
              indentString() + clean
            );
          }

          current = "";

        }


        function add(value) {

          if (!current) {
            current = value;
          } else {
            current += " " + value;
          }

        }


        function addInline(value) {

          current += value;

        }


        tokens.forEach((token) => {

          const raw =
            token.value;

          const upper =
            raw.toUpperCase();


          if (token.type === "comment") {

            flush();

            lines.push(
              indentString() + raw
            );

            return;

          }


          if (token.type === "symbol") {

            if (raw === "(") {

              if (current.endsWith(" ")) {
                current = current.slice(0, -1);
              }

              addInline("(");

              parenDepth++;

              continue;

            }


            if (raw === ")") {

              if (current.endsWith(" ")) {
                current = current.slice(0, -1);
              }

              addInline(")");

              parenDepth =
                Math.max(
                  0,
                  parenDepth - 1
                );

              continue;

            }


            if (raw === ",") {

              if (current.endsWith(" ")) {
                current = current.slice(0, -1);
              }

              addInline(",");

              if (parenDepth === 0) {
                flush();
              } else {
                current += " ";
              }

              continue;

            }


            if (raw === ";") {

              if (current.endsWith(" ")) {
                current = current.slice(0, -1);
              }

              addInline(";");

              flush();

              continue;

            }

          }


          if (majorKeywords.has(upper)) {

            if (
              upper === "SELECT" &&
              current
            ) {
              flush();
            }


            if (
              upper !== "SELECT" &&
              current
            ) {
              flush();
            }


            if (
              upper === "SELECT"
            ) {

              current =
                "SELECT";

              continue;

            }


            if (
              upper === "FROM"
            ) {

              flush();

              current =
                "FROM";

              afterFrom = true;

              continue;

            }


            if (
              upper === "WHERE"
            ) {

              flush();

              current =
                "WHERE";

              afterFrom = false;

              continue;

            }


            if (
              upper === "GROUP"
            ) {

              flush();

              current =
                "GROUP BY";

              continue;

            }


            if (
              upper === "ORDER"
            ) {

              flush();

              current =
                "ORDER BY";

              continue;

            }


            if (
              upper === "HAVING"
            ) {

              flush();

              current =
                "HAVING";

              continue;

            }


            if (
              upper === "LIMIT"
            ) {

              flush();

              current =
                "LIMIT";

              continue;

            }


            if (
              upper === "OFFSET"
            ) {

              flush();

              current =
                "OFFSET";

              continue;

            }


            if (
              upper === "UNION"
            ) {

              flush();

              current =
                "UNION";

              continue;

            }


            if (
              upper === "VALUES"
            ) {

              flush();

              current =
                "VALUES";

              continue;

            }


            if (
              upper === "SET"
            ) {

              flush();

              current =
                "SET";

              continue;

            }


            if (
              upper === "RETURNING"
            ) {

              flush();

              current =
                "RETURNING";

              continue;

            }

          }


          if (
            joinKeywords.has(upper)
          ) {

            if (
              upper === "INNER" ||
              upper === "LEFT" ||
              upper === "RIGHT" ||
              upper === "FULL" ||
              upper === "CROSS"
            ) {

              flush();

              current =
                upper;

              continue;

            }


            if (
              upper === "JOIN"
            ) {

              if (
                current === "INNER" ||
                current === "LEFT" ||
                current === "RIGHT" ||
                current === "FULL" ||
                current === "CROSS"
              ) {

                current +=
                  " JOIN";

              } else {

                flush();

                current =
                  "JOIN";

              }

              continue;

            }

          }


          if (
            upper === "ON"
          ) {

            flush();

            current =
              "ON";

            indent++;

            continue;

          }


          if (
            upper === "AND" ||
            upper === "OR"
          ) {

            if (current) {
              flush();
            }

            current =
              upper;

            continue;

          }


          const formatted =
            upperKeyword(raw);

          add(formatted);

        });


        flush();


        let result =
          lines.join("\n");


        /*
         * Clean excessive spaces around
         * common SQL operators.
         */

        result =
          result
            .replace(/\s*=\s*/g, " = ")
            .replace(/\s*<>\s*/g, " <> ")
            .replace(/\s*<=\s*/g, " <= ")
            .replace(/\s*>=\s*/g, " >= ")
            .replace(/\s+AND\s+/g, " AND ")
            .replace(/\s+OR\s+/g, " OR ");


        /*
         * Restore indentation for ON conditions.
         */

        const resultLines =
          result.split("\n");

        let insideJoin = false;

        let joinIndent = 0;


        const finalLines =
          resultLines.map((line) => {

            const trimmed =
              line.trim();

            const upper =
              trimmed.toUpperCase();


            if (
              /^(INNER|LEFT|RIGHT|FULL|CROSS)?\s*JOIN\b/
                .test(upper)
            ) {

              insideJoin = true;
              joinIndent = 0;

              return line;

            }


            if (
              insideJoin &&
              upper.startsWith("ON ")
            ) {

              return "  " + trimmed;

            }


            if (
              insideJoin &&
              (
                upper.startsWith("WHERE") ||
                upper.startsWith("GROUP BY") ||
                upper.startsWith("ORDER BY") ||
                upper.startsWith("HAVING") ||
                upper.startsWith("LIMIT") ||
                upper.startsWith("UNION")
              )
            ) {

              insideJoin = false;

            }


            return line;

          });


        return finalLines.join("\n").trim();

      }


      /* ======================================
         FORMAT
      ======================================= */

      function format() {

        const input =
          sqlInput.value.trim();


        if (!input) {

          sqlOutput.value =
            "";

          updateOutputStats();

          setStatus(
            inputStatus,
            "warning",
            "أدخل استعلام SQL أولاً"
          );

          setStatus(
            outputStatus,
            "idle",
            "في انتظار التنسيق"
          );

          resultBadge.innerHTML = `
            <span class="result-dot"></span>
            Waiting
          `;

          resultTitle.textContent =
            "أدخل استعلام SQL";

          resultText.textContent =
            "الصق استعلام SQL في المحرر ثم اضغط على زر التنسيق.";

          return;

        }


        const started =
          performance.now();


        try {

          const formatted =
            formatSQL(input);


          const elapsed =
            performance.now() - started;


          sqlOutput.value =
            formatted;


          updateOutputStats();


          const inputLineCount =
            input
              .replace(/\r\n/g, "\n")
              .split("\n")
              .length;


          const outputLineCount =
            formatted
              ? formatted
                  .replace(/\r\n/g, "\n")
                  .split("\n")
                  .length
              : 0;


          originalLines.textContent =
            inputLineCount.toLocaleString();


          formattedLines.textContent =
            outputLineCount.toLocaleString();


          formattedChars.textContent =
            formatted.length.toLocaleString();


          executionTime.textContent =
            `${elapsed.toFixed(1)} ms`;


          setStatus(
            inputStatus,
            "success",
            "تم التحليل"
          );


          setStatus(
            outputStatus,
            "success",
            "تم التنسيق"
          );


          resultBadge.innerHTML = `
            <span class="result-dot"></span>
            Formatted
          `;


          resultTitle.textContent =
            "تم تنسيق SQL بنجاح";


          resultText.textContent =
            `تم ترتيب الاستعلام وتحسين بنيته خلال ${elapsed.toFixed(1)} ms.`;


        } catch {

          setStatus(
            outputStatus,
            "error",
            "حدث خطأ"
          );


          resultBadge.innerHTML = `
            <span class="result-dot"></span>
            Error
          `;


          resultTitle.textContent =
            "تعذر تنسيق الاستعلام";


          resultText.textContent =
            "حدث خطأ أثناء معالجة SQL. تحقق من النص وحاول مرة أخرى.";

        }

      }


      /* ======================================
         COPY
      ======================================= */

      async function copyResult() {

        const value =
          sqlOutput.value;


        if (!value.trim()) {

          setStatus(
            outputStatus,
            "warning",
            "لا توجد نتيجة للنسخ"
          );

          return;

        }


        try {

          await navigator.clipboard.writeText(
            value
          );


          setStatus(
            outputStatus,
            "success",
            "تم نسخ SQL"
          );


          const original =
            copyBtn.innerHTML;


          copyBtn.innerHTML = `
            <i class="fa-solid fa-check"></i>
            تم النسخ
          `;


          setTimeout(() => {

            copyBtn.innerHTML =
              original;

          }, 1500);


        } catch {

          sqlOutput.select();

          document.execCommand("copy");

          setStatus(
            outputStatus,
            "success",
            "تم نسخ SQL"
          );

        }

      }


      /* ======================================
         CLEAR
      ======================================= */

      function clearAll() {

        sqlInput.value =
          "";

        sqlOutput.value =
          "";


        updateInputStats();

        updateOutputStats();


        originalLines.textContent =
          "0";

        formattedLines.textContent =
          "0";

        formattedChars.textContent =
          "0";

        executionTime.textContent =
          "0 ms";


        setStatus(
          inputStatus,
          "idle",
          "جاهز"
        );


        setStatus(
          outputStatus,
          "idle",
          "في انتظار التنسيق"
        );


        resultBadge.innerHTML = `
          <span class="result-dot"></span>
          Ready
        `;


        resultTitle.textContent =
          "SQL Formatter جاهز";


        resultText.textContent =
          'أدخل استعلام SQL ثم اضغط على "تنسيق SQL".';


        sqlInput.focus();

      }


      /* ======================================
         EXAMPLE
      ======================================= */

      exampleBtn.addEventListener(
        "click",
        () => {

          sqlInput.value =
            exampleSQL;

          updateInputStats();

          format();

        }
      );


      /* ======================================
         INPUT
      ======================================= */

      sqlInput.addEventListener(
        "input",
        () => {

          updateInputStats();

          setStatus(
            inputStatus,
            "idle",
            "جاهز"
          );

        }
      );


      /* ======================================
         OUTPUT LINE NUMBERS
      ======================================= */

      sqlOutput.addEventListener(
        "scroll",
        () => {

          outputLines.scrollTop =
            sqlOutput.scrollTop;

        }
      );


      sqlInput.addEventListener(
        "scroll",
        () => {

          inputLines.scrollTop =
            sqlInput.scrollTop;

        }
      );


      /* ======================================
         TAB
      ======================================= */

      sqlInput.addEventListener(
        "keydown",
        (event) => {

          if (event.key === "Tab") {

            event.preventDefault();


            const start =
              sqlInput.selectionStart;

            const end =
              sqlInput.selectionEnd;


            sqlInput.value =
              sqlInput.value.substring(
                0,
                start
              ) +
              "  " +
              sqlInput.value.substring(
                end
              );


            sqlInput.selectionStart =
              sqlInput.selectionEnd =
                start + 2;


            updateInputStats();

          }


          if (
            event.key === "Enter" &&
            (event.ctrlKey ||
             event.metaKey)
          ) {

            event.preventDefault();

            format();

          }

        }
      );


      /* ======================================
         BUTTONS
      ======================================= */

      formatBtn.addEventListener(
        "click",
        format
      );


      copyBtn.addEventListener(
        "click",
        copyResult
      );


      clearBtn.addEventListener(
        "click",
        clearAll
      );


      /* ======================================
         INITIALIZE
      ======================================= */

      updateInputStats();

      updateOutputStats();

    })();
  </script>


  <!-- ======================================
       STYLE
  ======================================= -->

  <style>

    :root {

      --bg:
        #f4f1fa;

      --surface:
        #ffffff;

      --surface-2:
        #f8f6fc;

      --text:
        #111827;

      --muted:
        #6b7280;

      --border:
        rgba(15,23,42,.09);

      --primary:
        #7c3aed;

      --primary-dark:
        #6d28d9;

      --secondary:
        #8b5cf6;

      --purple:
        #7c3aed;

      --blue:
        #2563eb;

      --cyan:
        #0891b2;

      --green:
        #16a34a;

      --orange:
        #ea580c;

      --red:
        #dc2626;

      --indigo:
        #4f46e5;

      --teal:
        #0f766e;

      --shadow:
        0 25px 70px
        rgba(15,23,42,.09);

      --code-bg:
        #080812;

      --code-bg-2:
        #10101d;

      --code-text:
        #e5e7eb;

    }


    html.dark-mode {

      --bg:
        #07060e;

      --surface:
        #0d0b17;

      --surface-2:
        #121020;

      --text:
        #f8fafc;

      --muted:
        #94a3b8;

      --border:
        rgba(148,163,184,.12);

      --primary:
        #a78bfa;

      --primary-dark:
        #8b5cf6;

      --secondary:
        #c4b5fd;

      --shadow:
        0 30px 80px
        rgba(0,0,0,.45);

    }


    * {
      box-sizing:
        border-box;
    }


    body {

      margin:
        0;

      background:
        var(--bg);

      color:
        var(--text);

      font-family:
        "Cairo",
        sans-serif;

      transition:
        background .25s ease,
        color .25s ease;

    }


    button,
    textarea {

      font-family:
        inherit;

    }


    .container {

      width:
        min(
          1120px,
          calc(100% - 32px)
        );

      margin-inline:
        auto;

    }


    /* ======================================
       HERO
    ======================================= */

    .hero {

      position:
        relative;

      overflow:
        hidden;

      padding:
        34px 0 45px;

      text-align:
        center;

    }


    .hero-glow {

      position:
        absolute;

      width:
        320px;

      height:
        320px;

      border-radius:
        50%;

      filter:
        blur(90px);

      opacity:
        .15;

      pointer-events:
        none;

    }


    .glow-one {

      top:
        -170px;

      right:
        8%;

      background:
        #7c3aed;

    }


    .glow-two {

      bottom:
        -210px;

      left:
        8%;

      background:
        #06b6d4;

    }


    .breadcrumb {

      display:
        flex;

      justify-content:
        center;

      align-items:
        center;

      flex-wrap:
        wrap;

      gap:
        8px;

      color:
        var(--muted);

      font-size:
        12px;

      margin-bottom:
        23px;

    }


    .breadcrumb a {

      color:
        var(--muted);

      text-decoration:
        none;

    }


    .breadcrumb a:hover {

      color:
        var(--primary);

    }


    .breadcrumb i {

      font-size:
        8px;

      opacity:
        .6;

    }


    .hero-badge {

      display:
        inline-flex;

      align-items:
        center;

      gap:
        7px;

      padding:
        7px 12px;

      border:
        1px solid
        rgba(124,58,237,.18);

      border-radius:
        999px;

      background:
        rgba(124,58,237,.07);

      color:
        var(--primary);

      font-size:
        11px;

      font-weight:
        700;

      margin-bottom:
        15px;

    }


    .badge-dot {

      width:
        6px;

      height:
        6px;

      border-radius:
        50%;

      background:
        #7c3aed;

      box-shadow:
        0 0 0 4px
        rgba(124,58,237,.1);

    }


    .hero-icon {

      position:
        relative;

      width:
        72px;

      height:
        72px;

      display:
        grid;

      place-items:
        center;

      margin:
        0 auto 15px;

      border-radius:
        20px;

      background:
        linear-gradient(
          135deg,
          #7c3aed,
          #8b5cf6
        );

      color:
        white;

      box-shadow:
        0 15px 35px
        rgba(124,58,237,.25);

      font-size:
        24px;

    }


    .sql-mark {

      position:
        absolute;

      right:
        7px;

      bottom:
        7px;

      display:
        grid;

      place-items:
        center;

      min-width:
        30px;

      height:
        20px;

      padding:
        0 5px;

      border:
        2px solid
        white;

      border-radius:
        6px;

      background:
        #6d28d9;

      font:
        700 8px
        "JetBrains Mono",
        monospace;

    }


    .hero h1 {

      margin:
        0;

      font-size:
        clamp(
          30px,
          5vw,
          46px
        );

      line-height:
        1.2;

      font-weight:
        800;

      letter-spacing:
        -.8px;

    }


    .hero h1 span {

      background:
        linear-gradient(
          90deg,
          var(--primary),
          var(--secondary)
        );

      -webkit-background-clip:
        text;

      background-clip:
        text;

      color:
        transparent;

    }


    .hero p {

      max-width:
        720px;

      margin:
        14px auto 0;

      color:
        var(--muted);

      font-size:
        14px;

      line-height:
        2;

    }


    /* ======================================
       CARD
    ======================================= */

    .formatter-card {

      overflow:
        hidden;

      background:
        var(--surface);

      border:
        1px solid
        var(--border);

      border-radius:
        24px;

      box-shadow:
        var(--shadow);

    }


    .topbar {

      display:
        flex;

      align-items:
        center;

      justify-content:
        space-between;

      gap:
        15px;

      padding:
        17px 20px;

      border-bottom:
        1px solid
        var(--border);

    }


    .project-info {

      display:
        flex;

      align-items:
        center;

      gap:
        11px;

    }


    .project-logo {

      width:
        40px;

      height:
        40px;

      display:
        grid;

      place-items:
        center;

      border-radius:
        11px;

      background:
        linear-gradient(
          135deg,
          #7c3aed,
          #8b5cf6
        );

      color:
        white;

      font-size:
        14px;

    }


    .project-info strong {

      display:
        block;

      font-size:
        13px;

    }


    .project-info span {

      display:
        block;

      color:
        var(--muted);

      font-size:
        10px;

    }


    .topbar-status {

      display:
        flex;

      align-items:
        center;

      gap:
        7px;

      color:
        var(--muted);

      font-size:
        10px;

    }


    .online-dot {

      width:
        7px;

      height:
        7px;

      border-radius:
        50%;

      background:
        #22c55e;

      box-shadow:
        0 0 0 4px
        rgba(34,197,94,.08);

    }


    /* ======================================
       ACTION BAR
    ======================================= */

    .actionbar {

      display:
        flex;

      justify-content:
        space-between;

      align-items:
        center;

      gap:
        10px;

      padding:
        13px 18px;

      background:
        var(--surface-2);

      border-bottom:
        1px solid
        var(--border);

    }


    .action-group {

      display:
        flex;

      gap:
        8px;

    }


    .action {

      display:
        inline-flex;

      align-items:
        center;

      justify-content:
        center;

      gap:
        7px;

      border:
        1px solid
        var(--border);

      border-radius:
        10px;

      padding:
        9px 13px;

      cursor:
        pointer;

      font-size:
        11px;

      font-weight:
        700;

      transition:
        .2s ease;

    }


    .action:hover {

      transform:
        translateY(-1px);

    }


    .action.secondary {

      background:
        var(--surface);

      color:
        var(--text);

    }


    .action.secondary:hover {

      color:
        var(--primary);

      border-color:
        rgba(124,58,237,.25);

    }


    .action.primary {

      border-color:
        transparent;

      background:
        linear-gradient(
          135deg,
          var(--primary),
          var(--secondary)
        );

      color:
        white;

      box-shadow:
        0 8px 20px
        rgba(124,58,237,.2);

    }


    .action.primary:hover {

      box-shadow:
        0 11px 25px
        rgba(124,58,237,.3);

    }


    /* ======================================
       WORKSPACE
    ======================================= */

    .workspace {

      display:
        grid;

      grid-template-columns:
        repeat(
          2,
          minmax(0, 1fr)
        );

      gap:
        14px;

      padding:
        16px;

    }


    .editor-card {

      min-width:
        0;

      overflow:
        hidden;

      border:
        1px solid
        rgba(148,163,184,.12);

      border-radius:
        16px;

      background:
        var(--code-bg);

      color:
        var(--code-text);

    }


    .editor-head {

      height:
        52px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        space-between;

      padding:
        0 14px;

      background:
        var(--code-bg-2);

      border-bottom:
        1px solid
        rgba(255,255,255,.06);

    }


    .editor-label {

      display:
        flex;

      align-items:
        center;

      gap:
        9px;

    }


    .file-icon {

      width:
        31px;

      height:
        31px;

      display:
        grid;

      place-items:
        center;

      border-radius:
        8px;

      font-size:
        12px;

    }


    .input-icon {

      background:
        rgba(124,58,237,.14);

      color:
        #a78bfa;

    }


    .output-icon {

      background:
        rgba(139,92,246,.14);

      color:
        #c4b5fd;

    }


    .editor-label strong {

      display:
        block;

      font-size:
        11px;

    }


    .editor-label small {

      display:
        block;

      margin-top:
        1px;

      color:
        #64748b;

      font-size:
        9px;

    }


    .editor-stats {

      color:
        #64748b;

      font:
        9px
        "JetBrains Mono",
        monospace;

    }


    .code-area {

      display:
        flex;

      height:
        430px;

      direction:
        ltr;

      overflow:
        hidden;

    }


    .line-numbers {

      width:
        45px;

      flex:
        0 0 45px;

      padding:
        15px 8px;

      background:
        rgba(0,0,0,.14);

      border-right:
        1px solid
        rgba(255,255,255,.05);

      color:
        #475569;

      text-align:
        right;

      white-space:
        pre;

      overflow:
        hidden;

      user-select:
        none;

      font:
        12px/1.8
        "JetBrains Mono",
        monospace;

    }


    textarea {

      width:
        100%;

      height:
        100%;

      resize:
        none;

      outline:
        none;

      border:
        0;

      padding:
        15px;

      background:
        var(--code-bg);

      color:
        var(--code-text);

      font:
        12px/1.8
        "JetBrains Mono",
        monospace;

      direction:
        ltr;

      text-align:
        left;

      tab-size:
        2;

    }


    textarea::placeholder {

      color:
        #475569;

    }


    .editor-foot {

      min-height:
        38px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        space-between;

      padding:
        0 13px;

      border-top:
        1px solid
        rgba(255,255,255,.06);

      color:
        #64748b;

      font-size:
        9px;

    }


    .editor-foot span {

      display:
        flex;

      align-items:
        center;

      gap:
        5px;

    }


    /* ======================================
       STATUS
    ======================================= */

    .status {

      display:
        flex;

      align-items:
        center;

      gap:
        6px;

    }


    .status-dot {

      width:
        6px;

      height:
        6px;

      border-radius:
        50%;

      background:
        #64748b;

    }


    .status.success {

      color:
        #4ade80;

    }


    .status.success .status-dot {

      background:
        #22c55e;

    }


    .status.warning {

      color:
        #fbbf24;

    }


    .status.warning .status-dot {

      background:
        #f59e0b;

    }


    .status.error {

      color:
        #f87171;

    }


    .status.error .status-dot {

      background:
        #ef4444;

    }


    /* ======================================
       RESULT
    ======================================= */

    .result-section {

      margin:
        0 16px 14px;

      overflow:
        hidden;

      border:
        1px solid
        rgba(124,58,237,.14);

      border-radius:
        15px;

      background:
        var(--surface-2);

    }


    .result-head {

      display:
        flex;

      align-items:
        center;

      justify-content:
        space-between;

      gap:
        12px;

      padding:
        12px 14px;

      border-bottom:
        1px solid
        var(--border);

    }


    .result-title {

      display:
        flex;

      align-items:
        center;

      gap:
        9px;

    }


    .result-icon {

      width:
        32px;

      height:
        32px;

      display:
        grid;

      place-items:
        center;

      border-radius:
        8px;

      background:
        rgba(124,58,237,.1);

      color:
        var(--primary);

      font-size:
        12px;

    }


    .result-title strong {

      display:
        block;

      font-size:
        11px;

    }


    .result-title span {

      display:
        block;

      margin-top:
        1px;

      color:
        var(--muted);

      font-size:
        9px;

    }


    .result-badge {

      display:
        flex;

      align-items:
        center;

      gap:
        6px;

      padding:
        5px 8px;

      border:
        1px solid
        rgba(124,58,237,.16);

      border-radius:
        999px;

      color:
        var(--primary);

      font:
        600 9px
        "JetBrains Mono",
        monospace;

    }


    .result-dot {

      width:
        5px;

      height:
        5px;

      border-radius:
        50%;

      background:
        var(--primary);

    }


    .result-content {

      padding:
        14px;

    }


    .result-message {

      display:
        flex;

      align-items:
        flex-start;

      gap:
        10px;

      padding:
        12px;

      border:
        1px solid
        var(--border);

      border-radius:
        12px;

      background:
        var(--surface);

    }


    .message-icon {

      width:
        35px;

      height:
        35px;

      flex:
        0 0 35px;

      display:
        grid;

      place-items:
        center;

      border-radius:
        9px;

      background:
        rgba(124,58,237,.09);

      color:
        var(--primary);

      font-size:
        12px;

    }


    .result-message strong {

      display:
        block;

      font-size:
        11px;

    }


    .result-message p {

      margin:
        3px 0 0;

      color:
        var(--muted);

      font-size:
        9px;

      line-height:
        1.8;

    }


    .result-metrics {

      display:
        grid;

      grid-template-columns:
        repeat(
          4,
          1fr
        );

      gap:
        9px;

      margin-top:
        10px;

    }


    .metric {

      padding:
        11px;

      border:
        1px solid
        var(--border);

      border-radius:
        11px;

      background:
        var(--surface);

      text-align:
        center;

    }


    .metric span {

      display:
        block;

      color:
        var(--muted);

      font-size:
        8px;

    }


    .metric strong {

      display:
        block;

      margin-top:
        3px;

      font:
        700 13px
        "JetBrains Mono",
        monospace;

    }


    /* ======================================
       TOOL STATS
    ======================================= */

    .tool-stats {

      display:
        grid;

      grid-template-columns:
        repeat(
          4,
          1fr
        );

      gap:
        10px;

      padding:
        0 16px 16px;

    }


    .tool-stat {

      display:
        flex;

      align-items:
        center;

      gap:
        9px;

      padding:
        12px;

      border:
        1px solid
        var(--border);

      border-radius:
        13px;

      background:
        var(--surface-2);

    }


    .tool-stat-icon {

      width:
        34px;

      height:
        34px;

      flex:
        0 0 34px;

      display:
        grid;

      place-items:
        center;

      border-radius:
        9px;

      background:
        rgba(124,58,237,.09);

      color:
        var(--primary);

      font-size:
        11px;

    }


    .tool-stat strong {

      display:
        block;

      font:
        700 11px
        "JetBrains Mono",
        monospace;

    }


    .tool-stat span {

      display:
        block;

      margin-top:
        2px;

      color:
        var(--muted);

      font-size:
        8px;

    }


    /* ======================================
       INFORMATION
    ======================================= */

    .information {

      margin-top:
        55px;

    }


    .section-title {

      max-width:
        700px;

      margin:
        0 auto 25px;

      text-align:
        center;

    }


    .section-title > span {

      color:
        var(--primary);

      font-size:
        11px;

      font-weight:
        800;

      letter-spacing:
        .5px;

    }


    .section-title h2 {

      margin:
        5px 0 7px;

      font-size:
        clamp(
          21px,
          4vw,
          28px
        );

    }


    .section-title p {

      margin:
        0;

      color:
        var(--muted);

      font-size:
        12px;

      line-height:
        1.9;

    }


    .info-grid {

      display:
        grid;

      grid-template-columns:
        repeat(
          3,
          1fr
        );

      gap:
        14px;

    }


    .info-card {

      position:
        relative;

      overflow:
        hidden;

      padding:
        24px;

      border:
        1px solid
        var(--border);

      border-radius:
        18px;

      background:
        var(--surface);

    }


    .info-number {

      position:
        absolute;

      top:
        15px;

      left:
        18px;

      color:
        var(--muted);

      opacity:
        .25;

      font:
        700 28px
        "JetBrains Mono",
        monospace;

    }


    .info-card-icon {

      width:
        42px;

      height:
        42px;

      display:
        grid;

      place-items:
        center;

      margin-bottom:
        13px;

      border-radius:
        11px;

      background:
        rgba(124,58,237,.1);

      color:
        var(--primary);

    }


    .info-card h3 {

      margin:
        0 0 7px;

      font-size:
        14px;

    }


    .info-card p {

      margin:
        0;

      color:
        var(--muted);

      font-size:
        11px;

      line-height:
        1.9;

    }


    /* ======================================
       HOW
    ======================================= */

    .how-section {

      margin-top:
        55px;

    }


    .steps {

      display:
        grid;

      grid-template-columns:
        repeat(
          3,
          1fr
        );

      gap:
        14px;

    }


    .step {

      display:
        flex;

      align-items:
        flex-start;

      gap:
        12px;

      padding:
        20px;

      border:
        1px solid
        var(--border);

      border-radius:
        17px;

      background:
        var(--surface);

    }


    .step-icon {

      width:
        43px;

      height:
        43px;

      flex:
        0 0 43px;

      display:
        grid;

      place-items:
        center;

      border-radius:
        12px;

      background:
        rgba(124,58,237,.1);

      color:
        var(--primary);

      font:
        700 10px
        "JetBrains Mono",
        monospace;

    }


    .step h3 {

      margin:
        0 0 5px;

      font-size:
        13px;

    }


    .step p {

      margin:
        0;

      color:
        var(--muted);

      font-size:
        10px;

      line-height:
        1.8;

    }


    /* ======================================
       FAQ
    ======================================= */

    .faq-section {

      max-width:
        850px;

      margin:
        55px auto 70px;

    }


    .faq-list details {

      margin-bottom:
        9px;

      overflow:
        hidden;

      border:
        1px solid
        var(--border);

      border-radius:
        14px;

      background:
        var(--surface);

    }


    .faq-list summary {

      display:
        flex;

      align-items:
        center;

      justify-content:
        space-between;

      gap:
        15px;

      padding:
        16px 18px;

      cursor:
        pointer;

      list-style:
        none;

      font-size:
        12px;

      font-weight:
        700;

    }


    .faq-list summary::-webkit-details-marker {

      display:
        none;

    }


    .faq-list summary i {

      color:
        var(--muted);

      font-size:
        10px;

      transition:
        transform .2s ease;

    }


    .faq-list details[open]
    summary i {

      transform:
        rotate(180deg);

    }


    .faq-answer {

      padding:
        0 18px 17px;

      color:
        var(--muted);

      font-size:
        11px;

      line-height:
        1.9;

    }


    /* ======================================
       RESPONSIVE
    ======================================= */

    @media (max-width: 900px) {

      .workspace {

        grid-template-columns:
          1fr;

      }


      .statistics-grid {

        grid-template-columns:
          repeat(
            2,
            1fr
          );

      }

    }


    @media (max-width: 800px) {

      .info-grid,
      .steps {

        grid-template-columns:
          repeat(
            2,
            1fr
          );

      }


      .tool-stats {

        grid-template-columns:
          repeat(
            2,
            1fr
          );

      }

    }


    @media (max-width: 700px) {

      .actionbar {

        flex-direction:
          column;

        align-items:
          stretch;

      }


      .action-group {

        width:
          100%;

      }


      .action {

        flex:
          1;

      }


      .result-metrics {

        grid-template-columns:
          repeat(
            2,
            1fr
          );

      }

    }


    @media (max-width: 600px) {

      .container {

        width:
          calc(
            100% - 20px
          );

      }


      .hero {

        padding:
          25px 0 32px;

      }


      .hero-icon {

        width:
          60px;

        height:
          60px;

        font-size:
          20px;

      }


      .hero p {

        font-size:
          12px;

      }


      .topbar {

        padding:
          14px;

      }


      .topbar-status {

        display:
          none;

      }


      .actionbar {

        padding:
          10px;

      }


      .action {

        padding:
          9px 7px;

        font-size:
          10px;

      }


      .workspace {

        padding:
          10px;

      }


      .code-area {

        height:
          330px;

      }


      .result-section {

        margin:
          0 10px 10px;

      }


      .result-content {

        padding:
          10px;

      }


      .result-metrics {

        gap:
          7px;

      }


      .metric {

        padding:
          9px;

      }


      .tool-stats {

        padding:
          0 10px 10px;

      }


      .info-grid,
      .steps {

        grid-template-columns:
          1fr;

      }


      .faq-section {

        margin-bottom:
          45px;

      }

    }

  </style>

</body>
</html>
