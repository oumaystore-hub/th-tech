---
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const title = "JavaScript Validator - فحص JavaScript أونلاين مجاناً | Th.Tech";

const description =
  "أداة JavaScript Validator مجانية للتحقق من صحة كود JavaScript واكتشاف أخطاء Syntax مباشرة من المتصفح.";

const faqs = [
  {
    q: "ما هو JavaScript Validator؟",
    a: "أداة تساعدك على فحص كود JavaScript والتأكد من خلوه من أخطاء بناء الجملة Syntax Errors."
  },
  {
    q: "هل يتم إرسال الكود إلى الخادم؟",
    a: "لا. يتم فحص الكود مباشرة داخل المتصفح، لذلك لا تحتاج إلى رفع ملفاتك إلى أي خادم."
  },
  {
    q: "هل الأداة تصلحح الأخطاء تلقائياً؟",
    a: "الأداة تحدد أخطاء بناء الجملة وتعرض رسالة تساعدك على معرفة المشكلة، لكنها لا تعدّل الكود تلقائياً."
  },
  {
    q: "هل يمكن فحص JavaScript الحديث؟",
    a: "نعم، يستخدم الفحص محرك JavaScript الموجود في المتصفح ويدعم صياغات JavaScript الحديثة التي يدعمها المتصفح."
  }
];
---

<!doctype html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="robots" content="index, follow" />

    <link rel="icon" href={`${base}/logo1.png`} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <link
      href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
    />

    <script is:inline>
      (() => {
        try {
          const saved = localStorage.getItem("theme");

          if (saved === "dark") {
            document.documentElement.classList.add("dark-mode");
          } else {
            document.documentElement.classList.remove("dark-mode");
          }
        } catch {}
      })();
    </script>
  </head>

  <body>

    <Header />

    <main>

      <!-- HERO -->
      <section class="hero">
        <div class="container">

          <div class="breadcrumb">
            <a href={`${base}/`}>الرئيسية</a>
            <span>/</span>
            <a href={`${base}/tools/`}>الأدوات</a>
            <span>/</span>
            <span>JavaScript Validator</span>
          </div>

          <div class="tool-icon">
            <i class="fa-brands fa-js"></i>
          </div>

          <div class="eyebrow">
            <i class="fa-solid fa-circle-check"></i>
            JavaScript
          </div>

          <h1>
            JavaScript <span>Validator</span>
          </h1>

          <p>
            تحقق من صحة كود JavaScript واكتشف أخطاء Syntax بسرعة
            مباشرة من المتصفح.
          </p>

        </div>
      </section>


      <!-- VALIDATOR -->
      <section class="validator-section">
        <div class="container">

          <div class="validator-card">

            <!-- TOP BAR -->
            <div class="topbar">

              <div class="tool-name">

                <div class="mini-icon">
                  <i class="fa-brands fa-js"></i>
                </div>

                <div>
                  <strong>JavaScript Validator</strong>
                  <small>فحص صحة كود JavaScript</small>
                </div>

              </div>

              <div class="local-badge">
                <span></span>
                يعمل محلياً
              </div>

            </div>


            <!-- TOOLBAR -->
            <div class="toolbar">

              <button
                id="validateBtn"
                class="btn btn-primary"
              >
                <i class="fa-solid fa-circle-check"></i>
                فحص الكود
              </button>

              <button
                id="formatBtn"
                class="btn"
              >
                <i class="fa-solid fa-align-left"></i>
                تنسيق
              </button>

              <button
                id="copyBtn"
                class="btn"
              >
                <i class="fa-regular fa-copy"></i>
                نسخ
              </button>

              <button
                id="clearBtn"
                class="btn btn-clear"
              >
                <i class="fa-solid fa-rotate-left"></i>
                مسح
              </button>

            </div>


            <!-- EDITOR -->
            <div class="editor-wrapper">

              <div class="editor-header">

                <div class="editor-title">

                  <span class="indicator"></span>

                  <strong>JavaScript Code</strong>

                </div>

                <div class="editor-info">

                  <span id="lineCount">
                    0 سطر
                  </span>

                  <span>•</span>

                  <span id="charCount">
                    0 حرف
                  </span>

                  <span>•</span>

                  <span id="byteCount">
                    0 B
                  </span>

                </div>

              </div>


              <div class="editor-body">

                <div
                  class="line-numbers"
                  id="lineNumbers"
                >1</div>

                <textarea
                  id="codeInput"
                  spellcheck="false"
                  autocomplete="off"
                  autocorrect="off"
                  autocapitalize="off"
                  placeholder={`// اكتب أو الصق كود JavaScript هنا

function greet(name) {
  const message = "Hello " + name;
  console.log(message);
}

greet("Ahmed");`}
                ></textarea>

              </div>

            </div>


            <!-- RESULT -->
            <div
              id="result"
              class="result neutral"
            >

              <div class="result-icon">
                <i class="fa-solid fa-circle-info"></i>
              </div>

              <div class="result-content">

                <strong id="resultTitle">
                  جاهز للفحص
                </strong>

                <span id="resultMessage">
                  أدخل كود JavaScript واضغط على "فحص الكود".
                </span>

              </div>

            </div>


            <!-- ERROR DETAILS -->
            <div
              id="errorDetails"
              class="error-details hidden"
            >

              <div class="error-header">

                <div>
                  <i class="fa-solid fa-triangle-exclamation"></i>
                  تفاصيل الخطأ
                </div>

                <span id="errorLine">
                  —
                </span>

              </div>

              <pre id="errorText"></pre>

            </div>


            <!-- STATUS -->
            <div class="bottom-bar">

              <div class="privacy">
                <i class="fa-solid fa-shield-halved"></i>
                تتم معالجة الكود داخل المتصفح
              </div>

              <div class="status-ready">
                <span></span>
                JavaScript Ready
              </div>

            </div>

          </div>

        </div>
      </section>


      <!-- FEATURES -->
      <section class="features-section">
        <div class="container">

          <div class="section-title">

            <span>المميزات</span>

            <h2>
              فحص JavaScript بسهولة
            </h2>

            <p>
              أداة بسيطة وسريعة لاكتشاف أخطاء بناء الجملة في كود JavaScript.
            </p>

          </div>


          <div class="features-grid">

            <div class="feature">

              <div class="feature-icon">
                <i class="fa-solid fa-circle-check"></i>
              </div>

              <div>
                <h3>فحص سريع</h3>

                <p>
                  تحقق من صحة الكود خلال ثوانٍ.
                </p>
              </div>

            </div>


            <div class="feature">

              <div class="feature-icon">
                <i class="fa-solid fa-bug"></i>
              </div>

              <div>
                <h3>اكتشاف الأخطاء</h3>

                <p>
                  اكتشف أخطاء Syntax في الكود بسهولة.
                </p>
              </div>

            </div>


            <div class="feature">

              <div class="feature-icon">
                <i class="fa-solid fa-lock"></i>
              </div>

              <div>
                <h3>خصوصية كاملة</h3>

                <p>
                  لا يتم إرسال الكود إلى أي خادم.
                </p>
              </div>

            </div>


            <div class="feature">

              <div class="feature-icon">
                <i class="fa-solid fa-mobile-screen"></i>
              </div>

              <div>
                <h3>متوافق مع الهاتف</h3>

                <p>
                  يعمل على الهاتف والكمبيوتر والأجهزة اللوحية.
                </p>
              </div>

            </div>

          </div>

        </div>
      </section>


      <!-- HOW TO -->
      <section class="how-section">
        <div class="container">

          <div class="section-title">

            <span>طريقة الاستخدام</span>

            <h2>
              كيف تستخدم JavaScript Validator؟
            </h2>

          </div>


          <div class="steps">

            <div class="step">

              <b>01</b>

              <div>
                <h3>أدخل الكود</h3>

                <p>
                  الصق كود JavaScript في المحرر.
                </p>
              </div>

            </div>


            <div class="step">

              <b>02</b>

              <div>
                <h3>ابدأ الفحص</h3>

                <p>
                  اضغط على زر "فحص الكود".
                </p>
              </div>

            </div>


            <div class="step">

              <b>03</b>

              <div>
                <h3>راجع النتيجة</h3>

                <p>
                  ستظهر لك حالة الكود ورسالة الخطأ عند وجود مشكلة.
                </p>
              </div>

            </div>


            <div class="step">

              <b>04</b>

              <div>
                <h3>أصلح الخطأ</h3>

                <p>
                  عدّل الكود وأعد الفحص حتى يصبح صحيحاً.
                </p>
              </div>

            </div>

          </div>

        </div>
      </section>


      <!-- FAQ -->
      <section class="faq-section">
        <div class="container">

          <div class="section-title">

            <span>FAQ</span>

            <h2>
              الأسئلة الشائعة
            </h2>

          </div>


          <div class="faq-list">

            {
              faqs.map((faq) => (
                <details class="faq-item">

                  <summary>

                    <span>
                      {faq.q}
                    </span>

                    <i class="fa-solid fa-chevron-down"></i>

                  </summary>

                  <p>
                    {faq.a}
                  </p>

                </details>
              ))
            }

          </div>

        </div>
      </section>

    </main>

    <Footer />


    <style>

      :global(*) {
        box-sizing: border-box;
      }

      :global(body) {
        margin: 0;
        font-family: "Cairo", sans-serif;
        background: var(--bg);
        color: var(--text);
      }


      :global(:root) {

        --bg: #f8fafc;
        --section: #f1f5f9;
        --card: #ffffff;
        --editor: #f8fafc;

        --text: #0f172a;
        --muted: #64748b;

        --border:
          rgba(15, 23, 42, .09);

        --primary: #2563eb;
        --primary-hover: #1d4ed8;

        --cyan: #0891b2;
        --green: #16a34a;
        --red: #dc2626;
        --orange: #ea580c;

        --shadow:
          0 18px 55px
          rgba(15, 23, 42, .08);
      }


      :global(html.dark-mode) {

        --bg: #07101f;
        --section: #0b1426;
        --card: #111d32;
        --editor: #0d1729;

        --text: #f8fafc;
        --muted: #94a3b8;

        --border:
          rgba(148, 163, 184, .12);

        --primary: #3b82f6;
        --primary-hover: #2563eb;

        --cyan: #06b6d4;
        --green: #22c55e;
        --red: #ef4444;
        --orange: #fb923c;

        --shadow:
          0 20px 65px
          rgba(0, 0, 0, .3);
      }


      .container {

        width:
          min(1120px, calc(100% - 28px));

        margin-inline: auto;
      }


      /* HERO */

      .hero {

        padding:
          38px 0 30px;

        text-align: center;
      }


      .breadcrumb {

        display: flex;

        justify-content: center;
        align-items: center;

        flex-wrap: wrap;

        gap: 8px;

        margin-bottom: 22px;

        color: var(--muted);

        font-size: 11px;
      }


      .breadcrumb a {

        color: var(--primary);

        text-decoration: none;
      }


      .tool-icon {

        width: 58px;
        height: 58px;

        margin:
          0 auto 13px;

        display: flex;
        align-items: center;
        justify-content: center;

        border-radius: 16px;

        background:
          rgba(37, 99, 235, .09);

        color: var(--primary);

        font-size: 28px;
      }


      .eyebrow {

        display: inline-flex;

        align-items: center;

        gap: 6px;

        color: var(--cyan);

        font-size: 11px;

        font-weight: 700;
      }


      .hero h1 {

        margin:
          5px 0 7px;

        font-size:
          clamp(30px, 5vw, 46px);

        line-height: 1.25;

        font-weight: 800;

        letter-spacing: -.8px;
      }


      .hero h1 span {
        color: var(--primary);
      }


      .hero p {

        max-width: 680px;

        margin: auto;

        color: var(--muted);

        font-size: 13px;

        line-height: 1.9;
      }


      /* TOOL */

      .validator-section {

        padding:
          5px 0 65px;
      }


      .validator-card {

        overflow: hidden;

        background: var(--card);

        border:
          1px solid var(--border);

        border-radius: 20px;

        box-shadow: var(--shadow);
      }


      /* TOPBAR */

      .topbar {

        min-height: 65px;

        padding:
          12px 17px;

        display: flex;

        align-items: center;

        justify-content: space-between;

        gap: 15px;

        border-bottom:
          1px solid var(--border);
      }


      .tool-name {

        display: flex;

        align-items: center;

        gap: 10px;
      }


      .mini-icon {

        width: 38px;
        height: 38px;

        display: flex;

        align-items: center;
        justify-content: center;

        border-radius: 11px;

        background:
          rgba(37, 99, 235, .09);

        color: var(--primary);

        font-size: 19px;
      }


      .tool-name strong {

        display: block;

        font-size: 13px;
      }


      .tool-name small {

        display: block;

        margin-top: 1px;

        color: var(--muted);

        font-size: 9px;
      }


      .local-badge {

        display: flex;

        align-items: center;

        gap: 6px;

        color: var(--green);

        font-size: 10px;

        font-weight: 700;
      }


      .local-badge span {

        width: 7px;
        height: 7px;

        border-radius: 50%;

        background: var(--green);

        box-shadow:
          0 0 0 4px
          rgba(22, 163, 74, .1);
      }


      /* TOOLBAR */

      .toolbar {

        padding:
          10px 13px;

        display: flex;

        align-items: center;

        gap: 7px;

        border-bottom:
          1px solid var(--border);

        background: var(--section);
      }


      .btn {

        height: 36px;

        padding:
          0 13px;

        display: inline-flex;

        align-items: center;
        justify-content: center;

        gap: 7px;

        border:
          1px solid var(--border);

        border-radius: 9px;

        background: var(--card);

        color: var(--text);

        font-family: inherit;

        font-size: 11px;

        font-weight: 700;

        cursor: pointer;

        transition: .2s ease;
      }


      .btn:hover {

        border-color: var(--primary);

        color: var(--primary);

        transform:
          translateY(-1px);
      }


      .btn-primary {

        padding-inline: 17px;

        background: var(--primary);

        border-color: var(--primary);

        color: #fff;

        box-shadow:
          0 5px 15px
          rgba(37, 99, 235, .18);
      }


      .btn-primary:hover {

        background:
          var(--primary-hover);

        color: #fff;
      }


      .btn-clear {
        color: var(--red);
      }


      /* EDITOR */

      .editor-wrapper {

        background: var(--card);
      }


      .editor-header {

        height: 45px;

        padding:
          0 14px;

        display: flex;

        align-items: center;

        justify-content: space-between;

        gap: 10px;

        border-bottom:
          1px solid var(--border);
      }


      .editor-title {

        display: flex;

        align-items: center;

        gap: 8px;
      }


      .editor-title strong {

        font-size: 11px;
      }


      .indicator {

        width: 7px;
        height: 7px;

        border-radius: 50%;

        background: var(--cyan);

        box-shadow:
          0 0 0 4px
          rgba(8, 145, 178, .1);
      }


      .editor-info {

        display: flex;

        gap: 5px;

        color: var(--muted);

        font-size: 9px;
      }


      .editor-body {

        height: 390px;

        display: flex;

        overflow: hidden;

        background: var(--editor);
      }


      .line-numbers {

        width: 43px;

        flex-shrink: 0;

        padding:
          17px 8px;

        overflow: hidden;

        border-left:
          1px solid var(--border);

        color: var(--muted);

        opacity: .5;

        font:
          12px/1.8
          Consolas,
          Monaco,
          monospace;

        text-align: center;

        user-select: none;
      }


      textarea {

        width:
          calc(100% - 43px);

        height: 100%;

        padding:
          17px 14px;

        border: 0;

        outline: none;

        resize: none;

        background: transparent;

        color: var(--text);

        font:
          12px/1.8
          Consolas,
          Monaco,
          "Courier New",
          monospace;

        direction: ltr;

        text-align: left;

        tab-size: 2;

        white-space: pre;

        overflow: auto;
      }


      textarea::placeholder {

        color: var(--muted);

        opacity: .55;
      }


      /* RESULT */

      .result {

        margin:
          13px;

        padding:
          13px;

        display: flex;

        align-items: center;

        gap: 12px;

        border:
          1px solid var(--border);

        border-radius: 12px;

        transition: .25s ease;
      }


      .result-icon {

        width: 40px;
        height: 40px;

        flex-shrink: 0;

        display: flex;

        align-items: center;
        justify-content: center;

        border-radius: 10px;

        font-size: 17px;
      }


      .result-content {

        min-width: 0;
      }


      .result-content strong {

        display: block;

        margin-bottom: 2px;

        font-size: 11px;
      }


      .result-content span {

        display: block;

        color: var(--muted);

        font-size: 9px;

        line-height: 1.7;
      }


      .result.neutral {

        background:
          rgba(37, 99, 235, .05);

        border-color:
          rgba(37, 99, 235, .12);
      }


      .result.neutral
      .result-icon {

        background:
          rgba(37, 99, 235, .1);

        color: var(--primary);
      }


      .result.success {

        background:
          rgba(22, 163, 74, .06);

        border-color:
          rgba(22, 163, 74, .15);
      }


      .result.success
      .result-icon {

        background:
          rgba(22, 163, 74, .1);

        color: var(--green);
      }


      .result.error {

        background:
          rgba(220, 38, 38, .06);

        border-color:
          rgba(220, 38, 38, .15);
      }


      .result.error
      .result-icon {

        background:
          rgba(220, 38, 38, .1);

        color: var(--red);
      }


      /* ERROR */

      .error-details {

        margin:
          -3px 13px 13px;

        overflow: hidden;

        border:
          1px solid
          rgba(220, 38, 38, .16);

        border-radius: 11px;

        background:
          rgba(220, 38, 38, .035);
      }


      .error-details.hidden {
        display: none;
      }


      .error-header {

        padding:
          9px 12px;

        display: flex;

        align-items: center;

        justify-content: space-between;

        gap: 10px;

        border-bottom:
          1px solid
          rgba(220, 38, 38, .1);

        color: var(--red);

        font-size: 10px;

        font-weight: 700;
      }


      .error-header div {

        display: flex;

        align-items: center;

        gap: 7px;
      }


      .error-header span {

        font-size: 9px;
      }


      .error-details pre {

        margin: 0;

        padding:
          11px 13px;

        overflow: auto;

        color: var(--red);

        font:
          11px/1.7
          Consolas,
          Monaco,
          monospace;

        direction: ltr;

        text-align: left;

        white-space: pre-wrap;
      }


      /* BOTTOM */

      .bottom-bar {

        min-height: 51px;

        padding:
          10px 14px;

        display: flex;

        align-items: center;

        justify-content: space-between;

        gap: 15px;

        border-top:
          1px solid var(--border);
      }


      .privacy {

        display: flex;

        align-items: center;

        gap: 7px;

        color: var(--muted);

        font-size: 9px;
      }


      .privacy i {
        color: var(--green);
      }


      .status-ready {

        display: flex;

        align-items: center;

        gap: 6px;

        color: var(--muted);

        font-size: 9px;
      }


      .status-ready span {

        width: 6px;
        height: 6px;

        border-radius: 50%;

        background: var(--green);

        box-shadow:
          0 0 0 3px
          rgba(22, 163, 74, .1);
      }


      /* SECTIONS */

      .features-section {

        padding:
          65px 0;

        background: var(--section);
      }


      .how-section,
      .faq-section {

        padding:
          65px 0;
      }


      .section-title {

        margin-bottom: 30px;

        text-align: center;
      }


      .section-title > span {

        color: var(--primary);

        font-size: 11px;

        font-weight: 800;
      }


      .section-title h2 {

        margin:
          5px 0 6px;

        font-size:
          clamp(23px, 4vw, 31px);

        font-weight: 800;
      }


      .section-title p {

        margin: auto;

        color: var(--muted);

        font-size: 12px;
      }


      /* FEATURES */

      .features-grid {

        display: grid;

        grid-template-columns:
          repeat(4, 1fr);

        gap: 13px;
      }


      .feature {

        padding: 19px;

        display: flex;

        align-items: flex-start;

        gap: 12px;

        background: var(--card);

        border:
          1px solid var(--border);

        border-radius: 14px;

        transition: .2s ease;
      }


      .feature:hover {

        transform:
          translateY(-3px);

        box-shadow: var(--shadow);
      }


      .feature-icon {

        width: 38px;
        height: 38px;

        flex-shrink: 0;

        display: flex;

        align-items: center;
        justify-content: center;

        border-radius: 10px;

        background:
          rgba(37, 99, 235, .09);

        color: var(--primary);
      }


      .feature h3 {

        margin:
          0 0 3px;

        font-size: 13px;
      }


      .feature p {

        margin: 0;

        color: var(--muted);

        font-size: 10px;

        line-height: 1.7;
      }


      /* STEPS */

      .steps {

        max-width: 820px;

        margin: auto;

        display: grid;

        grid-template-columns:
          1fr 1fr;

        gap: 12px;
      }


      .step {

        padding: 18px;

        display: flex;

        align-items: flex-start;

        gap: 12px;

        border:
          1px solid var(--border);

        border-radius: 14px;

        background: var(--card);
      }


      .step > b {

        width: 38px;
        height: 38px;

        flex-shrink: 0;

        display: flex;

        align-items: center;
        justify-content: center;

        border-radius: 10px;

        background:
          rgba(37, 99, 235, .09);

        color: var(--primary);

        font-size: 9px;
      }


      .step h3 {

        margin:
          0 0 3px;

        font-size: 13px;
      }


      .step p {

        margin: 0;

        color: var(--muted);

        font-size: 10px;

        line-height: 1.7;
      }


      /* FAQ */

      .faq-list {

        max-width: 820px;

        margin: auto;

        display: grid;

        gap: 8px;
      }


      .faq-item {

        overflow: hidden;

        border:
          1px solid var(--border);

        border-radius: 13px;

        background: var(--card);
      }


      .faq-item summary {

        padding:
          15px 17px;

        display: flex;

        align-items: center;

        justify-content: space-between;

        gap: 12px;

        cursor: pointer;

        list-style: none;

        font-size: 12px;

        font-weight: 700;
      }


      .faq-item summary::-webkit-details-marker {
        display: none;
      }


      .faq-item summary i {

        color: var(--primary);

        font-size: 9px;

        transition: .2s;
      }


      .faq-item[open]
      summary i {

        transform:
          rotate(180deg);
      }


      .faq-item p {

        margin: 0;

        padding:
          0 17px 16px;

        color: var(--muted);

        font-size: 11px;

        line-height: 1.8;
      }


      /* TABLET */

      @media (max-width: 900px) {

        .features-grid {
          grid-template-columns:
            repeat(2, 1fr);
        }

      }


      /* MOBILE */

      @media (max-width: 700px) {

        .editor-body {
          height: 300px;
        }

        .local-badge {
          display: none;
        }

        .steps {
          grid-template-columns: 1fr;
        }

      }


      @media (max-width: 520px) {

        .container {
          width:
            calc(100% - 18px);
        }


        .hero {
          padding:
            28px 0 24px;
        }


        .hero h1 {
          font-size: 31px;
        }


        .hero p {
          font-size: 11px;
        }


        .validator-card {
          border-radius: 16px;
        }


        .topbar {
          padding: 11px;
        }


        .mini-icon {

          width: 34px;
          height: 34px;
        }


        .tool-name strong {
          font-size: 11px;
        }


        .toolbar {

          display: grid;

          grid-template-columns:
            1.35fr 1fr 1fr 1fr;

          gap: 5px;

          padding: 8px;
        }


        .btn {

          padding:
            0 5px;

          height: 34px;

          font-size: 9px;

          gap: 4px;
        }


        .editor-header {
          padding: 0 10px;
        }


        .editor-title strong {
          font-size: 10px;
        }


        .editor-info {
          font-size: 8px;
        }


        .editor-body {
          height: 270px;
        }


        .line-numbers {

          width: 34px;

          padding-inline: 4px;

          font-size: 10px;
        }


        textarea {

          width:
            calc(100% - 34px);

          padding:
            14px 10px;

          font-size: 11px;
        }


        .result {

          margin: 9px;

          padding: 10px;
        }


        .result-icon {

          width: 35px;
          height: 35px;
        }


        .error-details {

          margin:
            -1px 9px 9px;
        }


        .bottom-bar {

          padding:
            9px 11px;
        }


        .privacy {

          font-size: 8px;
        }


        .status-ready {
          font-size: 8px;
        }


        .features-grid {

          grid-template-columns:
            1fr 1fr;

          gap: 8px;
        }


        .feature {

          padding: 13px;

          gap: 8px;

          flex-direction: column;
        }


        .feature-icon {

          width: 34px;
          height: 34px;
        }

      }


      @media (max-width: 360px) {

        .features-grid {
          grid-template-columns: 1fr;
        }


        .toolbar .btn {
          font-size: 0;
        }


        .toolbar .btn i {
          font-size: 12px;
        }

      }

    </style>


    <script is:inline>
      (() => {

        const input =
          document.getElementById("codeInput");

        const validateBtn =
          document.getElementById("validateBtn");

        const formatBtn =
          document.getElementById("formatBtn");

        const copyBtn =
          document.getElementById("copyBtn");

        const clearBtn =
          document.getElementById("clearBtn");

        const result =
          document.getElementById("result");

        const resultTitle =
          document.getElementById("resultTitle");

        const resultMessage =
          document.getElementById("resultMessage");

        const resultIcon =
          result.querySelector(".result-icon i");

        const errorDetails =
          document.getElementById("errorDetails");

        const errorText =
          document.getElementById("errorText");

        const errorLine =
          document.getElementById("errorLine");

        const lineNumbers =
          document.getElementById("lineNumbers");

        const lineCount =
          document.getElementById("lineCount");

        const charCount =
          document.getElementById("charCount");

        const byteCount =
          document.getElementById("byteCount");


        function getBytes(text) {

          return new Blob([text]).size;
        }


        function formatBytes(bytes) {

          if (!bytes) return "0 B";

          if (bytes < 1024) {
            return `${bytes} B`;
          }

          if (bytes < 1024 * 1024) {
            return `${(bytes / 1024).toFixed(1)} KB`;
          }

          return `${(
            bytes /
            (1024 * 1024)
          ).toFixed(2)} MB`;
        }


        function getLines(text) {

          if (!text) return 0;

          return text
            .split(/\r\n|\r|\n/)
            .length;
        }


        function updateLineNumbers() {

          const lines =
            Math.max(
              1,
              getLines(input.value)
            );

          let html = "";

          for (
            let i = 1;
            i <= lines;
            i++
          ) {

            html += `${i}<br>`;
          }

          lineNumbers.innerHTML =
            html;

          lineCount.textContent =
            `${lines} ${lines === 1 ? "سطر" : "أسطر"}`;

          charCount.textContent =
            `${input.value.length.toLocaleString()} حرف`;

          byteCount.textContent =
            formatBytes(
              getBytes(input.value)
            );
        }


        function setResult(
          type,
          titleText,
          message
        ) {

          result.className =
            `result ${type}`;

          resultTitle.textContent =
            titleText;

          resultMessage.textContent =
            message;


          if (type === "success") {

            resultIcon.className =
              "fa-solid fa-circle-check";

          } else if (type === "error") {

            resultIcon.className =
              "fa-solid fa-circle-xmark";

          } else {

            resultIcon.className =
              "fa-solid fa-circle-info";
          }
        }


        function hideError() {

          errorDetails.classList.add(
            "hidden"
          );

          errorText.textContent =
            "";

          errorLine.textContent =
            "—";
        }


        function showError(error) {

          errorDetails.classList.remove(
            "hidden"
          );


          let message =
            error &&
            error.message
              ? error.message
              : "JavaScript Syntax Error";


          /*
           * Remove browser-specific prefixes
           * where possible.
           */

          message =
            message
              .replace(/^Uncaught\s*/i, "")
              .trim();


          errorText.textContent =
            message;


          const match =
            message.match(
              /(?:line\s+|:)(\d+)/i
            );


          if (match) {

            errorLine.textContent =
              `السطر ${match[1]}`;

          } else {

            errorLine.textContent =
              "تحقق من الكود";
          }
        }


        function validateJavaScript() {

          const code =
            input.value.trim();


          if (!code) {

            hideError();

            setResult(
              "neutral",
              "لا يوجد كود",
              "أدخل كود JavaScript أولاً ثم اضغط على فحص الكود."
            );

            input.focus();

            return;
          }


          hideError();


          try {

            /*
             * Function constructor performs
             * JavaScript syntax parsing without
             * executing the supplied function body.
             *
             * The created function is never called.
             */

            new Function(code);


            setResult(
              "success",
              "الكود صحيح",
              "لم يتم العثور على أخطاء في بناء جملة JavaScript."
            );

          } catch (error) {

            setResult(
              "error",
              "تم العثور على خطأ",
              "يوجد خطأ في بناء جملة JavaScript."
            );

            showError(error);
          }
        }


        /*
         * Lightweight formatter.
         * It intentionally avoids aggressive
         * transformations that could alter code.
         */

        function formatJavaScript(code) {

          let result = "";

          let indent = 0;

          let quote = null;

          let template = false;

          let escaped = false;

          let line = "";

          const pushLine = () => {

            const clean =
              line.trim();

            if (clean) {

              result +=
                "  ".repeat(
                  Math.max(0, indent)
                ) +
                clean +
                "\n";
            }

            line = "";
          };


          for (
            let i = 0;
            i < code.length;
            i++
          ) {

            const ch = code[i];


            if (quote) {

              line += ch;

              if (escaped) {

                escaped = false;

              } else if (
                ch === "\\"
              ) {

                escaped = true;

              } else if (
                ch === quote
              ) {

                quote = null;
              }

              continue;
            }


            if (template) {

              line += ch;

              if (escaped) {

                escaped = false;

              } else if (
                ch === "\\"
              ) {

                escaped = true;

              } else if (
                ch === "`"
              ) {

                template = false;
              }

              continue;
            }


            if (
              ch === "'" ||
              ch === '"'
            ) {

              quote = ch;

              line += ch;

              continue;
            }


            if (ch === "`") {

              template = true;

              line += ch;

              continue;
            }


            if (ch === "{") {

              line += " {";

              pushLine();

              indent++;

              continue;
            }


            if (ch === "}") {

              if (line.trim()) {
                pushLine();
              }

              indent =
                Math.max(
                  0,
                  indent - 1
                );

              line = "}";

              const next =
                code[i + 1];

              if (
                next === ";" ||
                next === ","
              ) {

                continue;
              }

              pushLine();

              continue;
            }


            if (ch === ";") {

              line += ";";

              pushLine();

              continue;
            }


            if (ch === "\n" || ch === "\r") {

              pushLine();

              continue;
            }


            line += ch;
          }


          if (line.trim()) {
            pushLine();
          }


          return result.trim();
        }


        validateBtn.addEventListener(
          "click",
          validateJavaScript
        );


        formatBtn.addEventListener(
          "click",
          () => {

            const code =
              input.value.trim();


            if (!code) {

              setResult(
                "neutral",
                "لا يوجد كود",
                "أدخل كود JavaScript أولاً."
              );

              input.focus();

              return;
            }


            try {

              /*
               * Validate first so the formatter
               * doesn't make an invalid script
               * appear valid.
               */

              new Function(code);

              input.value =
                formatJavaScript(code);

              updateLineNumbers();

              setResult(
                "success",
                "تم تنسيق الكود",
                "تم ترتيب الكود مع الحفاظ على بنيته."
              );

            } catch (error) {

              setResult(
                "error",
                "الكود يحتوي على خطأ",
                "صحح خطأ JavaScript قبل محاولة التنسيق."
              );

              showError(error);
            }

          }
        );


        copyBtn.addEventListener(
          "click",
          async () => {

            if (!input.value) {

              setResult(
                "neutral",
                "لا يوجد كود",
                "أدخل الكود أولاً."
              );

              return;
            }


            try {

              await navigator.clipboard.writeText(
                input.value
              );

              setResult(
                "success",
                "تم النسخ",
                "تم نسخ كود JavaScript إلى الحافظة."
              );

            } catch {

              input.focus();

              input.select();

              document.execCommand(
                "copy"
              );

              setResult(
                "success",
                "تم النسخ",
                "تم نسخ الكود إلى الحافظة."
              );
            }
          }
        );


        clearBtn.addEventListener(
          "click",
          () => {

            input.value = "";

            updateLineNumbers();

            hideError();

            setResult(
              "neutral",
              "جاهز للفحص",
              "أدخل كود JavaScript واضغط على فحص الكود."
            );

            input.focus();
          }
        );


        input.addEventListener(
          "input",
          () => {

            updateLineNumbers();

            hideError();

            setResult(
              "neutral",
              "جاهز للفحص",
              "اضغط على فحص الكود للتحقق من JavaScript."
            );
          }
        );


        input.addEventListener(
          "scroll",
          () => {

            lineNumbers.scrollTop =
              input.scrollTop;
          }
        );


        input.addEventListener(
          "keydown",
          (event) => {

            if (event.key === "Tab") {

              event.preventDefault();

              const start =
                input.selectionStart;

              const end =
                input.selectionEnd;

              input.value =
                input.value.substring(
                  0,
                  start
                ) +
                "  " +
                input.value.substring(
                  end
                );

              input.selectionStart =
                input.selectionEnd =
                  start + 2;

              updateLineNumbers();
            }


            if (
              (event.ctrlKey ||
                event.metaKey) &&
              event.key === "Enter"
            ) {

              event.preventDefault();

              validateJavaScript();
            }

          }
        );


        updateLineNumbers();

      })();
    </script>

  </body>
</html>
