---
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const title = "URL Decoder - فك ترميز URL أونلاين مجاناً | Th.Tech";

const description =
  "أداة URL Decoder مجانية لفك ترميز الروابط والنصوص المشفرة باستخدام URL Encoding مباشرة من المتصفح.";

const faqs = [
  {
    q: "ما هو URL Decoder؟",
    a: "URL Decoder هو أداة لفك ترميز النصوص والروابط التي تم تحويلها باستخدام URL Encoding وإعادتها إلى شكلها الأصلي."
  },
  {
    q: "هل يمكن فك ترميز الروابط العربية؟",
    a: "نعم، تدعم الأداة النصوص العربية وأحرف Unicode باستخدام UTF-8."
  },
  {
    q: "هل يتم إرسال البيانات إلى الخادم؟",
    a: "لا، تتم عملية فك الترميز بالكامل داخل المتصفح ولا يتم إرسال النص إلى أي خادم."
  },
  {
    q: "هل يمكن استخدام الأداة لفك ترميز Query Parameters؟",
    a: "نعم، يمكنك إدخال قيمة مشفرة من URL Query Parameters وستقوم الأداة بإعادتها إلى النص المقروء."
  }
];
---

<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>{title}</title>
  <meta name="description" content={description} />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

  <link
    href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap"
    rel="stylesheet"
  />

  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
  />

  <script is:inline>
    (() => {
      try {
        const saved = localStorage.getItem("theme");

        if (saved === "dark") {
          document.documentElement.classList.add("dark-mode");
        }
      } catch {}
    })();
  </script>
</head>

<body>
  <Header />

  <main>

    <!-- HERO -->
    <section class="hero">
      <div class="container">

        <div class="breadcrumb">
          <a href={`${base}/`}>الرئيسية</a>
          <i class="fa-solid fa-chevron-left"></i>
          <a href={`${base}/tools/`}>الأدوات</a>
          <i class="fa-solid fa-chevron-left"></i>
          <span>URL Decoder</span>
        </div>

        <div class="hero-content">

          <div class="hero-icon">
            <i class="fa-solid fa-link-slash"></i>
            <span>URL</span>
          </div>

          <div class="hero-text">

            <div class="badge">
              <i class="fa-solid fa-code"></i>
              Developer Tools
            </div>

            <h1>URL Decoder</h1>

            <p>
              فك ترميز الروابط والنصوص المشفرة وإعادتها إلى
              شكلها الأصلي بسهولة وسرعة.
            </p>

            <div class="tags">
              <span>URL Decode</span>
              <span>UTF-8</span>
              <span>Web</span>
              <span>Local</span>
            </div>

          </div>

        </div>
      </div>
    </section>


    <!-- TOOL -->
    <section class="tool-section">
      <div class="container">

        <div class="decoder-card">

          <!-- TOP BAR -->
          <div class="card-topbar">

            <div class="window-dots">
              <span></span>
              <span></span>
              <span></span>
            </div>

            <div class="tool-name">
              <i class="fa-solid fa-link-slash"></i>
              URL Decoder
            </div>

            <div class="secure">
              <i class="fa-solid fa-shield-halved"></i>
              Local
            </div>

          </div>


          <!-- ACTION BAR -->
          <div class="actionbar">

            <button id="exampleBtn" class="btn btn-secondary">
              <i class="fa-solid fa-flask"></i>
              مثال
            </button>

            <button id="clearBtn" class="btn btn-secondary">
              <i class="fa-solid fa-eraser"></i>
              مسح
            </button>

            <button id="copyBtn" class="btn btn-secondary">
              <i class="fa-regular fa-copy"></i>
              نسخ
            </button>

            <button id="decodeBtn" class="btn btn-primary">
              <i class="fa-solid fa-link-slash"></i>
              فك ترميز URL
            </button>

          </div>


          <!-- WORKSPACE -->
          <div class="workspace">

            <!-- INPUT -->
            <div class="editor-panel">

              <div class="editor-header">

                <div>
                  <i class="fa-solid fa-link"></i>
                  النص المرمّز
                </div>

                <span id="inputCount">0 حرف</span>

              </div>

              <div class="editor">

                <div
                  class="line-numbers"
                  id="inputLines"
                >1</div>

                <textarea
                  id="inputText"
                  spellcheck="false"
                  placeholder="أدخل الرابط أو النص المرمّز..."
                ></textarea>

              </div>

            </div>


            <!-- ARROW -->
            <div class="convert-arrow">
              <i class="fa-solid fa-arrow-down"></i>
            </div>


            <!-- OUTPUT -->
            <div class="editor-panel output-panel">

              <div class="editor-header">

                <div>
                  <i class="fa-solid fa-file-lines"></i>
                  النص الأصلي
                </div>

                <span id="outputCount">0 حرف</span>

              </div>

              <div class="editor">

                <div
                  class="line-numbers"
                  id="outputLines"
                >1</div>

                <textarea
                  id="outputText"
                  readonly
                  spellcheck="false"
                  placeholder="ستظهر النتيجة هنا..."
                ></textarea>

              </div>

            </div>

          </div>


          <!-- RESULT -->
          <div
            class="result-box"
            id="resultBox"
          >

            <div
              class="result-icon"
              id="resultIcon"
            >
              <i class="fa-solid fa-link-slash"></i>
            </div>

            <div>

              <strong id="resultTitle">
                جاهز لفك الترميز
              </strong>

              <p id="resultMessage">
                أدخل نصاً مشفراً ثم اضغط على زر فك ترميز URL.
              </p>

            </div>

          </div>


          <!-- STATS -->
          <div class="stats-grid">

            <div class="stat">

              <div class="stat-icon">
                <i class="fa-solid fa-link"></i>
              </div>

              <div>
                <span>النص المرمّز</span>
                <strong id="statInput">0</strong>
              </div>

            </div>


            <div class="stat">

              <div class="stat-icon">
                <i class="fa-solid fa-font"></i>
              </div>

              <div>
                <span>النص الأصلي</span>
                <strong id="statOutput">0</strong>
              </div>

            </div>


            <div class="stat">

              <div class="stat-icon">
                <i class="fa-solid fa-percent"></i>
              </div>

              <div>
                <span>نسبة الانخفاض</span>
                <strong id="statDecrease">0%</strong>
              </div>

            </div>


            <div class="stat">

              <div class="stat-icon">
                <i class="fa-solid fa-bolt"></i>
              </div>

              <div>
                <span>الحالة</span>
                <strong id="statStatus">جاهز</strong>
              </div>

            </div>

          </div>

        </div>

      </div>
    </section>


    <!-- INFO -->
    <section class="info-section">
      <div class="container">

        <div class="section-heading">

          <span>URL DECODING</span>

          <h2>
            ما هو URL Decoder؟
          </h2>

          <p>
            أداة تساعدك على تحويل النصوص المشفرة باستخدام
            URL Encoding إلى نصوص مقروءة يمكن استخدامها مرة أخرى.
          </p>

        </div>


        <div class="info-grid">

          <article class="info-card">

            <div class="info-icon">
              <i class="fa-solid fa-link-slash"></i>
            </div>

            <h3>
              فك ترميز الروابط
            </h3>

            <p>
              حوّل الرموز مثل %20 و%2F و%3F إلى الأحرف
              الأصلية التي تمثلها.
            </p>

          </article>


          <article class="info-card">

            <div class="info-icon">
              <i class="fa-solid fa-language"></i>
            </div>

            <h3>
              دعم UTF-8
            </h3>

            <p>
              يدعم النصوص العربية واللغات المختلفة وأحرف Unicode.
            </p>

          </article>


          <article class="info-card">

            <div class="info-icon">
              <i class="fa-solid fa-shield-halved"></i>
            </div>

            <h3>
              معالجة محلية
            </h3>

            <p>
              تتم عملية فك الترميز داخل المتصفح دون إرسال
              بياناتك إلى خادم خارجي.
            </p>

          </article>

        </div>

      </div>
    </section>


    <!-- HOW TO -->
    <section class="how-section">
      <div class="container">

        <div class="section-heading">

          <span>HOW IT WORKS</span>

          <h2>
            كيفية استخدام URL Decoder
          </h2>

        </div>


        <div class="steps">

          <div class="step">

            <div class="step-number">
              1
            </div>

            <div>
              <h3>
                أدخل النص المشفر
              </h3>

              <p>
                الصق الرابط أو النص الذي يحتوي على URL Encoding.
              </p>
            </div>

          </div>


          <div class="step">

            <div class="step-number">
              2
            </div>

            <div>
              <h3>
                اضغط فك الترميز
              </h3>

              <p>
                ستقوم الأداة بتحويل الرموز المشفرة إلى أحرفها الأصلية.
              </p>
            </div>

          </div>


          <div class="step">

            <div class="step-number">
              3
            </div>

            <div>
              <h3>
                انسخ النتيجة
              </h3>

              <p>
                اضغط على نسخ لاستخدام النص الناتج في مشروعك.
              </p>
            </div>

          </div>

        </div>

      </div>
    </section>


    <!-- FAQ -->
    <section class="faq-section">
      <div class="container">

        <div class="section-heading">

          <span>FAQ</span>

          <h2>
            الأسئلة الشائعة
          </h2>

        </div>


        <div class="faq-list">

          {faqs.map((faq) => (
            <details>

              <summary>

                <span>
                  {faq.q}
                </span>

                <i class="fa-solid fa-chevron-down"></i>

              </summary>

              <p>
                {faq.a}
              </p>

            </details>
          ))}

        </div>

      </div>
    </section>

  </main>


  <Footer />


  <!-- JAVASCRIPT -->
  <script is:inline>
    (() => {

      const input =
        document.getElementById("inputText");

      const output =
        document.getElementById("outputText");

      const decodeBtn =
        document.getElementById("decodeBtn");

      const exampleBtn =
        document.getElementById("exampleBtn");

      const clearBtn =
        document.getElementById("clearBtn");

      const copyBtn =
        document.getElementById("copyBtn");


      const inputCount =
        document.getElementById("inputCount");

      const outputCount =
        document.getElementById("outputCount");


      const inputLines =
        document.getElementById("inputLines");

      const outputLines =
        document.getElementById("outputLines");


      const resultBox =
        document.getElementById("resultBox");

      const resultIcon =
        document.getElementById("resultIcon");

      const resultTitle =
        document.getElementById("resultTitle");

      const resultMessage =
        document.getElementById("resultMessage");


      const statInput =
        document.getElementById("statInput");

      const statOutput =
        document.getElementById("statOutput");

      const statDecrease =
        document.getElementById("statDecrease");

      const statStatus =
        document.getElementById("statStatus");


      function updateEditor(
        textarea,
        counter,
        lines
      ) {

        const value = textarea.value;

        const count = value.length;

        counter.textContent =
          `${count.toLocaleString()} حرف`;


        const lineCount =
          Math.max(
            1,
            value.split("\n").length
          );


        lines.textContent =
          Array.from(
            {
              length: lineCount
            },
            (_, index) => index + 1
          ).join("\n");
      }


      function updateStats(decoded = "") {

        const encodedLength =
          input.value.length;

        const decodedLength =
          decoded.length;


        statInput.textContent =
          encodedLength.toLocaleString();

        statOutput.textContent =
          decodedLength.toLocaleString();


        if (encodedLength > 0) {

          const decrease =
            ((encodedLength - decodedLength) /
              encodedLength) *
            100;


          statDecrease.textContent =
            `${decrease >= 0 ? "" : "+"}${decrease.toFixed(1)}%`;

        } else {

          statDecrease.textContent =
            "0%";

        }
      }


      function setSuccess(message) {

        resultBox.classList.remove("error");

        resultBox.classList.add("success");


        resultIcon.innerHTML =
          '<i class="fa-solid fa-circle-check"></i>';


        resultTitle.textContent =
          "تم فك الترميز بنجاح";


        resultMessage.textContent =
          message;


        statStatus.textContent =
          "تم";
      }


      function setError(message) {

        resultBox.classList.remove("success");

        resultBox.classList.add("error");


        resultIcon.innerHTML =
          '<i class="fa-solid fa-circle-exclamation"></i>';


        resultTitle.textContent =
          "تعذر فك الترميز";


        resultMessage.textContent =
          message;


        statStatus.textContent =
          "خطأ";
      }


      function decodeURL() {

        const value =
          input.value;


        if (!value.trim()) {

          output.value = "";

          updateEditor(
            output,
            outputCount,
            outputLines
          );

          updateStats("");

          setError(
            "أدخل نصاً أو رابطاً مشفراً أولاً."
          );

          return;
        }


        try {

          const start =
            performance.now();


          const decoded =
            decodeURIComponent(value);


          const end =
            performance.now();


          output.value =
            decoded;


          updateEditor(
            output,
            outputCount,
            outputLines
          );


          updateStats(decoded);


          setSuccess(
            `تم فك ترميز ${value.length.toLocaleString()} حرف بنجاح خلال ${(end - start).toFixed(2)} ms.`
          );

        } catch (error) {

          output.value = "";

          updateEditor(
            output,
            outputCount,
            outputLines
          );

          updateStats("");

          setError(
            "النص يحتوي على ترميز URL غير صالح أو رمز % غير مكتمل."
          );
        }
      }


      function clearAll() {

        input.value = "";

        output.value = "";


        updateEditor(
          input,
          inputCount,
          inputLines
        );


        updateEditor(
          output,
          outputCount,
          outputLines
        );


        updateStats("");


        resultBox.classList.remove(
          "success",
          "error"
        );


        resultIcon.innerHTML =
          '<i class="fa-solid fa-link-slash"></i>';


        resultTitle.textContent =
          "جاهز لفك الترميز";


        resultMessage.textContent =
          "أدخل نصاً مشفراً ثم اضغط على زر فك ترميز URL.";


        statStatus.textContent =
          "جاهز";


        input.focus();
      }


      function loadExample() {

        input.value =
          "https%3A%2F%2Fth.tech%2Fsearch%3Fq%3D%D8%A3%D8%AF%D9%88%D8%A7%D8%AA%20%D8%AA%D9%82%D9%86%D9%8A%D8%A9%26lang%3Dar";


        updateEditor(
          input,
          inputCount,
          inputLines
        );


        decodeURL();
      }


      async function copyResult() {

        if (!output.value) {

          setError(
            "لا توجد نتيجة لنسخها."
          );

          return;
        }


        try {

          await navigator.clipboard.writeText(
            output.value
          );


          const original =
            copyBtn.innerHTML;


          copyBtn.innerHTML =
            '<i class="fa-solid fa-check"></i> تم النسخ';


          setTimeout(() => {

            copyBtn.innerHTML =
              original;

          }, 1500);

        } catch {

          output.select();

          document.execCommand("copy");

          setSuccess(
            "تم نسخ النتيجة إلى الحافظة."
          );
        }
      }


      input.addEventListener(
        "input",
        () => {

          updateEditor(
            input,
            inputCount,
            inputLines
          );


          if (!input.value) {

            output.value = "";


            updateEditor(
              output,
              outputCount,
              outputLines
            );


            updateStats("");
          }

        }
      );


      input.addEventListener(
        "scroll",
        () => {

          inputLines.scrollTop =
            input.scrollTop;

        }
      );


      output.addEventListener(
        "scroll",
        () => {

          outputLines.scrollTop =
            output.scrollTop;

        }
      );


      decodeBtn.addEventListener(
        "click",
        decodeURL
      );


      exampleBtn.addEventListener(
        "click",
        loadExample
      );


      clearBtn.addEventListener(
        "click",
        clearAll
      );


      copyBtn.addEventListener(
        "click",
        copyResult
      );


      input.addEventListener(
        "keydown",
        (event) => {

          if (event.key === "Tab") {

            event.preventDefault();


            const start =
              input.selectionStart;

            const end =
              input.selectionEnd;


            input.value =
              input.value.substring(
                0,
                start
              ) +
              "  " +
              input.value.substring(
                end
              );


            input.selectionStart =
              input.selectionEnd =
                start + 2;


            updateEditor(
              input,
              inputCount,
              inputLines
            );
          }


          if (
            event.ctrlKey &&
            event.key === "Enter"
          ) {

            event.preventDefault();

            decodeURL();
          }

        }
      );


      updateEditor(
        input,
        inputCount,
        inputLines
      );


      updateEditor(
        output,
        outputCount,
        outputLines
      );

    })();
  </script>


  <style>

    :root {

      --bg: #f7fafc;
      --surface: #ffffff;
      --surface-2: #f8fafc;

      --text: #172033;
      --muted: #718096;

      --border:
        rgba(15, 23, 42, 0.09);

      --primary: #7c3aed;
      --primary-dark: #6d28d9;
      --secondary: #a78bfa;

      --success: #16a34a;
      --danger: #dc2626;

      --shadow:
        0 25px 70px
        rgba(15, 23, 42, 0.10);
    }


    html.dark-mode {

      --bg: #080710;
      --surface: #0f0c17;
      --surface-2: #151020;

      --text: #f8fafc;
      --muted: #94a3b8;

      --border:
        rgba(148, 163, 184, 0.13);

      --primary: #a78bfa;
      --primary-dark: #8b5cf6;
      --secondary: #c4b5fd;

      --shadow:
        0 30px 80px
        rgba(0, 0, 0, 0.45);
    }


    * {
      box-sizing: border-box;
    }


    body {

      margin: 0;

      background:
        var(--bg);

      color:
        var(--text);

      font-family:
        "Cairo",
        sans-serif;
    }


    .container {

      width:
        min(
          1180px,
          calc(100% - 32px)
        );

      margin:
        auto;
    }


    /* HERO */

    .hero {

      padding:
        38px 0 55px;

      background:
        radial-gradient(
          circle at 10% 20%,
          rgba(124, 58, 237, 0.13),
          transparent 35%
        ),
        var(--bg);
    }


    .breadcrumb {

      display:
        flex;

      align-items:
        center;

      gap:
        10px;

      flex-wrap:
        wrap;

      color:
        var(--muted);

      font-size:
        13px;

      margin-bottom:
        38px;
    }


    .breadcrumb a {

      color:
        var(--muted);

      text-decoration:
        none;
    }


    .breadcrumb i {

      font-size:
        9px;
    }


    .hero-content {

      display:
        flex;

      align-items:
        center;

      gap:
        28px;
    }


    .hero-icon {

      width:
        112px;

      height:
        112px;

      flex:
        0 0 112px;

      border-radius:
        30px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      flex-direction:
        column;

      gap:
        4px;

      background:
        rgba(124, 58, 237, 0.12);

      color:
        var(--primary);

      box-shadow:
        inset 0 0 0 1px
        rgba(124, 58, 237, 0.10);
    }


    .hero-icon i {

      font-size:
        42px;
    }


    .hero-icon span {

      font:
        700 13px
        "JetBrains Mono",
        monospace;
    }


    .badge {

      display:
        inline-flex;

      align-items:
        center;

      gap:
        8px;

      padding:
        7px 13px;

      border-radius:
        999px;

      background:
        rgba(124, 58, 237, 0.10);

      color:
        var(--primary);

      font-size:
        12px;

      font-weight:
        700;
    }


    h1 {

      margin:
        12px 0 8px;

      font-size:
        clamp(
          32px,
          5vw,
          52px
        );

      line-height:
        1.1;
    }


    .hero-text p {

      margin:
        0;

      max-width:
        700px;

      color:
        var(--muted);

      font-size:
        16px;

      line-height:
        1.9;
    }


    .tags {

      display:
        flex;

      flex-wrap:
        wrap;

      gap:
        8px;

      margin-top:
        17px;
    }


    .tags span {

      padding:
        5px 10px;

      border:
        1px solid
        var(--border);

      border-radius:
        8px;

      color:
        var(--muted);

      background:
        var(--surface);

      font:
        500 11px
        "JetBrains Mono",
        monospace;
    }


    /* TOOL */

    .tool-section {

      padding-bottom:
        70px;
    }


    .decoder-card {

      overflow:
        hidden;

      background:
        var(--surface);

      border:
        1px solid
        var(--border);

      border-radius:
        22px;

      box-shadow:
        var(--shadow);
    }


    .card-topbar {

      min-height:
        55px;

      padding:
        0 20px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        space-between;

      border-bottom:
        1px solid
        var(--border);

      background:
        var(--surface-2);
    }


    .window-dots {

      display:
        flex;

      gap:
        6px;
    }


    .window-dots span {

      width:
        9px;

      height:
        9px;

      border-radius:
        50%;

      background:
        var(--border);
    }


    .tool-name {

      display:
        flex;

      align-items:
        center;

      gap:
        8px;

      color:
        var(--text);

      font:
        600 13px
        "JetBrains Mono",
        monospace;
    }


    .tool-name i {

      color:
        var(--primary);
    }


    .secure {

      display:
        flex;

      align-items:
        center;

      gap:
        6px;

      color:
        var(--success);

      font-size:
        11px;

      font-weight:
        700;
    }


    .actionbar {

      display:
        flex;

      flex-wrap:
        wrap;

      gap:
        9px;

      padding:
        15px 18px;

      border-bottom:
        1px solid
        var(--border);
    }


    .btn {

      border:
        1px solid
        var(--border);

      border-radius:
        10px;

      padding:
        10px 15px;

      font-family:
        "Cairo",
        sans-serif;

      font-size:
        12px;

      font-weight:
        700;

      cursor:
        pointer;

      transition:
        0.2s;
    }


    .btn:hover {

      transform:
        translateY(-1px);
    }


    .btn i {

      margin-left:
        6px;
    }


    .btn-secondary {

      color:
        var(--text);

      background:
        var(--surface-2);
    }


    .btn-primary {

      color:
        white;

      background:
        var(--primary);

      border-color:
        var(--primary);
    }


    .btn-primary:hover {

      background:
        var(--primary-dark);
    }


    .workspace {

      display:
        grid;

      grid-template-columns:
        1fr 42px 1fr;

      align-items:
        stretch;

      padding:
        20px;

      gap:
        15px;
    }


    .editor-panel {

      min-width:
        0;

      overflow:
        hidden;

      border:
        1px solid
        var(--border);

      border-radius:
        15px;

      background:
        var(--surface-2);
    }


    .editor-header {

      display:
        flex;

      align-items:
        center;

      justify-content:
        space-between;

      padding:
        11px 14px;

      border-bottom:
        1px solid
        var(--border);

      color:
        var(--muted);

      font-size:
        12px;

      font-weight:
        700;
    }


    .editor-header div {

      display:
        flex;

      align-items:
        center;

      gap:
        8px;

      color:
        var(--text);
    }


    .editor-header i {

      color:
        var(--primary);
    }


    .editor-header span {

      font:
        10px
        "JetBrains Mono",
        monospace;
    }


    .editor {

      position:
        relative;

      display:
        flex;

      min-height:
        300px;

      max-height:
        480px;
    }


    .line-numbers {

      width:
        48px;

      flex:
        0 0 48px;

      padding:
        17px 10px;

      overflow:
        hidden;

      text-align:
        left;

      user-select:
        none;

      border-left:
        1px solid
        var(--border);

      color:
        var(--muted);

      font:
        12px/1.75
        "JetBrains Mono",
        monospace;

      white-space:
        pre;
    }


    textarea {

      width:
        100%;

      min-height:
        300px;

      resize:
        vertical;

      padding:
        16px;

      border:
        0;

      outline:
        0;

      background:
        transparent;

      color:
        var(--text);

      font:
        13px/1.75
        "JetBrains Mono",
        monospace;

      direction:
        ltr;

      text-align:
        left;
    }


    textarea::placeholder {

      color:
        var(--muted);

      opacity:
        0.7;
    }


    .output-panel textarea {

      color:
        var(--primary);
    }


    .convert-arrow {

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      color:
        var(--primary);
    }


    .convert-arrow i {

      width:
        38px;

      height:
        38px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      border-radius:
        50%;

      background:
        rgba(124, 58, 237, 0.10);
    }


    /* RESULT */

    .result-box {

      margin:
        0 20px 20px;

      display:
        flex;

      align-items:
        center;

      gap:
        14px;

      padding:
        15px 17px;

      border:
        1px solid
        var(--border);

      border-radius:
        14px;

      background:
        var(--surface-2);
    }


    .result-icon {

      width:
        40px;

      height:
        40px;

      flex:
        0 0 40px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      border-radius:
        11px;

      background:
        rgba(124, 58, 237, 0.10);

      color:
        var(--primary);
    }


    .result-box strong {

      display:
        block;

      font-size:
        13px;
    }


    .result-box p {

      margin:
        3px 0 0;

      color:
        var(--muted);

      font-size:
        11px;
    }


    .result-box.success {

      border-color:
        rgba(22, 163, 74, 0.25);
    }


    .result-box.success .result-icon {

      color:
        var(--success);

      background:
        rgba(22, 163, 74, 0.10);
    }


    .result-box.error {

      border-color:
        rgba(220, 38, 38, 0.25);
    }


    .result-box.error .result-icon {

      color:
        var(--danger);

      background:
        rgba(220, 38, 38, 0.10);
    }


    /* STATS */

    .stats-grid {

      display:
        grid;

      grid-template-columns:
        repeat(4, 1fr);

      gap:
        12px;

      padding:
        0 20px 20px;
    }


    .stat {

      display:
        flex;

      align-items:
        center;

      gap:
        11px;

      padding:
        14px;

      border:
        1px solid
        var(--border);

      border-radius:
        13px;

      background:
        var(--surface-2);
    }


    .stat-icon {

      width:
        36px;

      height:
        36px;

      flex:
        0 0 36px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      border-radius:
        9px;

      color:
        var(--primary);

      background:
        rgba(124, 58, 237, 0.10);
    }


    .stat span {

      display:
        block;

      color:
        var(--muted);

      font-size:
        9px;
    }


    .stat strong {

      display:
        block;

      margin-top:
        2px;

      font:
        600 12px
        "JetBrains Mono",
        monospace;
    }


    /* SECTIONS */

    .info-section,
    .how-section,
    .faq-section {

      padding:
        70px 0;
    }


    .info-section {

      background:
        var(--surface-2);

      border-top:
        1px solid
        var(--border);

      border-bottom:
        1px solid
        var(--border);
    }


    .section-heading {

      max-width:
        700px;

      margin:
        0 auto 35px;

      text-align:
        center;
    }


    .section-heading > span {

      color:
        var(--primary);

      font:
        700 11px
        "JetBrains Mono",
        monospace;

      letter-spacing:
        1px;
    }


    .section-heading h2 {

      margin:
        8px 0;

      font-size:
        clamp(
          25px,
          4vw,
          34px
        );
    }


    .section-heading p {

      margin:
        0;

      color:
        var(--muted);

      line-height:
        1.9;

      font-size:
        14px;
    }


    .info-grid {

      display:
        grid;

      grid-template-columns:
        repeat(3, 1fr);

      gap:
        18px;
    }


    .info-card {

      padding:
        25px;

      border:
        1px solid
        var(--border);

      border-radius:
        17px;

      background:
        var(--surface);
    }


    .info-icon {

      width:
        48px;

      height:
        48px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      margin-bottom:
        17px;

      border-radius:
        13px;

      color:
        var(--primary);

      background:
        rgba(124, 58, 237, 0.10);
    }


    .info-card h3 {

      margin:
        0 0 7px;

      font-size:
        16px;
    }


    .info-card p {

      margin:
        0;

      color:
        var(--muted);

      font-size:
        13px;

      line-height:
        1.9;
    }


    /* STEPS */

    .steps {

      display:
        grid;

      grid-template-columns:
        repeat(3, 1fr);

      gap:
        18px;
    }


    .step {

      display:
        flex;

      gap:
        15px;

      padding:
        22px;

      border:
        1px solid
        var(--border);

      border-radius:
        16px;

      background:
        var(--surface);
    }


    .step-number {

      width:
        38px;

      height:
        38px;

      flex:
        0 0 38px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      border-radius:
        10px;

      color:
        white;

      background:
        var(--primary);

      font-weight:
        800;
    }


    .step h3 {

      margin:
        0 0 5px;

      font-size:
        14px;
    }


    .step p {

      margin:
        0;

      color:
        var(--muted);

      font-size:
        12px;

      line-height:
        1.8;
    }


    /* FAQ */

    .faq-list {

      max-width:
        850px;

      margin:
        auto;
    }


    details {

      margin-bottom:
        10px;

      border:
        1px solid
        var(--border);

      border-radius:
        13px;

      background:
        var(--surface);

      overflow:
        hidden;
    }


    summary {

      display:
        flex;

      align-items:
        center;

      justify-content:
        space-between;

      padding:
        17px 19px;

      cursor:
        pointer;

      font-size:
        13px;

      font-weight:
        700;

      list-style:
        none;
    }


    summary::-webkit-details-marker {

      display:
        none;
    }


    summary i {

      color:
        var(--primary);

      transition:
        transform 0.2s;
    }


    details[open] summary i {

      transform:
        rotate(180deg);
    }


    details p {

      margin:
        0;

      padding:
        0 19px 18px;

      color:
        var(--muted);

      font-size:
        12px;

      line-height:
        1.9;
    }


    /* RESPONSIVE */

    @media (max-width: 850px) {

      .workspace {

        grid-template-columns:
          1fr;
      }


      .convert-arrow {

        height:
          30px;
      }


      .stats-grid {

        grid-template-columns:
          repeat(2, 1fr);
      }


      .info-grid,
      .steps {

        grid-template-columns:
          1fr;
      }
    }


    @media (max-width: 600px) {

      .container {

        width:
          min(
            100% - 22px,
            1180px
          );
      }


      .hero {

        padding-top:
          25px;
      }


      .hero-content {

        align-items:
          flex-start;

        flex-direction:
          column;
      }


      .hero-icon {

        width:
          88px;

        height:
          88px;

        flex-basis:
          88px;

        border-radius:
          23px;
      }


      .hero-icon i {

        font-size:
          31px;
      }


      .card-topbar {

        padding:
          0 13px;
      }


      .actionbar {

        padding:
          12px;
      }


      .btn {

        flex:
          1 1
          calc(50% - 8px);

        padding:
          9px 8px;
      }


      .workspace {

        padding:
          12px;

        gap:
          8px;
      }


      .editor {

        min-height:
          260px;
      }


      textarea {

        min-height:
          260px;
      }


      .result-box {

        margin:
          0 12px 12px;
      }


      .stats-grid {

        padding:
          0 12px 12px;

        gap:
          8px;
      }


      .stat {

        padding:
          11px 9px;
      }


      .stat-icon {

        width:
          31px;

        height:
          31px;

        flex-basis:
          31px;
      }


      .stat span {

        font-size:
          8px;
      }


      .stat strong {

        font-size:
          10px;
      }


      .info-section,
      .how-section,
      .faq-section {

        padding:
          50px 0;
      }

    }

  </style>

</body>
</html>
