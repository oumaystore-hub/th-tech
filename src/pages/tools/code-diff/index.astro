---
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const title =
  "Code Diff - مقارنة الأكواد أونلاين مجاناً | Th.Tech";

const description =
  "أداة Code Diff مجانية لمقارنة نسختين من الأكواد ومعرفة الأسطر المضافة والمحذوفة والمتطابقة مباشرة من المتصفح.";

const faqs = [
  {
    q: "ما هي أداة Code Diff؟",
    a: "أداة تساعدك على مقارنة نسختين من الكود ومعرفة الأسطر التي تمت إضافتها أو حذفها أو بقيت كما هي."
  },
  {
    q: "هل يتم إرسال الكود إلى الخادم؟",
    a: "لا. تتم مقارنة الأكواد مباشرة داخل المتصفح، ولا تحتاج إلى إرسال الكود إلى خادم خارجي."
  },
  {
    q: "ما فائدة مقارنة الأكواد؟",
    a: "تساعد مقارنة الأكواد على اكتشاف التغييرات والأخطاء ومراجعة الفرق بين إصدارين من الملف بسرعة."
  },
  {
    q: "هل يمكن مقارنة أي نوع من الأكواد؟",
    a: "نعم. يمكن استخدام الأداة لمقارنة JavaScript وTypeScript وHTML وCSS وJSON وPython وغيرها من النصوص البرمجية."
  },
  {
    q: "هل الأداة مجانية؟",
    a: "نعم. الأداة مجانية ويمكن استخدامها مباشرة من المتصفح."
  }
];
---

<!doctype html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />

    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />

    <title>{title}</title>

    <meta
      name="description"
      content={description}
    />

    <meta
      name="robots"
      content="index, follow"
    />

    <meta
      name="theme-color"
      content="#4f46e5"
    />

    <link
      rel="icon"
      href={`${base}/logo1.png`}
    />

    <link
      rel="preconnect"
      href="https://fonts.googleapis.com"
    />

    <link
      rel="preconnect"
      href="https://fonts.gstatic.com"
      crossorigin
    />

    <link
      href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap"
      rel="stylesheet"
    />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
    />

    <script is:inline>
      (() => {
        try {
          const saved = localStorage.getItem("theme");

          if (saved === "dark") {
            document.documentElement.classList.add("dark-mode");
          } else {
            document.documentElement.classList.remove("dark-mode");
          }
        } catch {}
      })();
    </script>
  </head>

  <body>

    <Header />

    <main>

      <!-- ======================================
           HERO
      ======================================= -->

      <section class="hero">

        <div class="hero-glow glow-one"></div>
        <div class="hero-glow glow-two"></div>

        <div class="container">

          <div class="breadcrumb">

            <a href={`${base}/`}>
              الرئيسية
            </a>

            <i class="fa-solid fa-chevron-left"></i>

            <a href={`${base}/tools/`}>
              الأدوات
            </a>

            <i class="fa-solid fa-chevron-left"></i>

            <span>
              Code Diff
            </span>

          </div>


          <div class="hero-badge">

            <span class="badge-dot"></span>

            Developer Tools

            <i class="fa-solid fa-code-compare"></i>

          </div>


          <div class="hero-icon">

            <i class="fa-solid fa-code-compare"></i>

            <span class="diff-mark">
              ±
            </span>

          </div>


          <h1>

            Code

            <span>Diff</span>

          </h1>


          <p>

            قارن بين نسختين من الكود بسرعة واحترافية،
            واكتشف الأسطر المضافة والمحذوفة والمتطابقة مباشرة من المتصفح.

          </p>

        </div>

      </section>


      <!-- ======================================
           TOOL
      ======================================= -->

      <section class="tool-section">

        <div class="container">

          <div class="diff-card">


            <!-- TOP BAR -->

            <div class="topbar">

              <div class="project-info">

                <div class="project-logo">

                  <i class="fa-solid fa-code-compare"></i>

                </div>

                <div>

                  <strong>
                    Code Diff
                  </strong>

                  <span>
                    Online Code Comparison Tool
                  </span>

                </div>

              </div>


              <div class="topbar-status">

                <span class="online-dot"></span>

                Local Processing

              </div>

            </div>


            <!-- ACTION BAR -->

            <div class="actionbar">

              <div class="action-group">

                <button
                  id="exampleBtn"
                  class="action secondary"
                  type="button"
                >

                  <i class="fa-solid fa-wand-magic-sparkles"></i>

                  مثال

                </button>


                <button
                  id="swapBtn"
                  class="action secondary"
                  type="button"
                >

                  <i class="fa-solid fa-right-left"></i>

                  تبديل

                </button>


                <button
                  id="clearBtn"
                  class="action secondary"
                  type="button"
                >

                  <i class="fa-solid fa-trash-can"></i>

                  مسح

                </button>

              </div>


              <div class="action-group">

                <button
                  id="copyBtn"
                  class="action secondary"
                  type="button"
                >

                  <i class="fa-regular fa-copy"></i>

                  نسخ النتيجة

                </button>


                <button
                  id="compareBtn"
                  class="action primary"
                  type="button"
                >

                  <i class="fa-solid fa-code-compare"></i>

                  مقارنة الكود

                </button>

              </div>

            </div>


            <!-- EDITORS -->

            <div class="editor-grid">


              <!-- ORIGINAL -->

              <div class="editor-card">

                <div class="editor-head">

                  <div class="editor-label">

                    <span class="file-icon original-icon">

                      <i class="fa-solid fa-file-code"></i>

                    </span>

                    <div>

                      <strong>
                        Original
                      </strong>

                      <small>
                        Original Code
                      </small>

                    </div>

                  </div>


                  <div
                    id="oldStats"
                    class="editor-stats"
                  >
                    0 حرف
                  </div>

                </div>


                <div class="code-area">

                  <div
                    id="oldLines"
                    class="line-numbers"
                  >
                    1
                  </div>


                  <textarea
                    id="oldCode"
                    spellcheck="false"
                    autocomplete="off"
                    autocorrect="off"
                    autocapitalize="off"
                    placeholder="// الكود الأصلي..."
                  ></textarea>

                </div>


                <div class="editor-foot">

                  <span>

                    <i class="fa-solid fa-keyboard"></i>

                    Ctrl + Enter

                  </span>

                  <span>
                    Original
                  </span>

                </div>

              </div>


              <!-- MODIFIED -->

              <div class="editor-card">

                <div class="editor-head">

                  <div class="editor-label">

                    <span class="file-icon modified-icon">

                      <i class="fa-solid fa-file-pen"></i>

                    </span>

                    <div>

                      <strong>
                        Modified
                      </strong>

                      <small>
                        Modified Code
                      </small>

                    </div>

                  </div>


                  <div
                    id="newStats"
                    class="editor-stats"
                  >
                    0 حرف
                  </div>

                </div>


                <div class="code-area">

                  <div
                    id="newLines"
                    class="line-numbers"
                  >
                    1
                  </div>


                  <textarea
                    id="newCode"
                    spellcheck="false"
                    autocomplete="off"
                    autocorrect="off"
                    autocapitalize="off"
                    placeholder="// الكود المعدل..."
                  ></textarea>

                </div>


                <div class="editor-foot">

                  <span
                    id="status"
                    class="status idle"
                  >

                    <span class="status-dot"></span>

                    جاهز

                  </span>

                  <span id="newSize">
                    0 B
                  </span>

                </div>

              </div>

            </div>


            <!-- DIFF RESULT -->

            <div class="result-section">

              <div class="result-head">

                <div class="result-title">

                  <div class="result-icon">

                    <i class="fa-solid fa-code-compare"></i>

                  </div>

                  <div>

                    <strong>
                      Comparison Result
                    </strong>

                    <span>
                      نتائج مقارنة الأكواد
                    </span>

                  </div>

                </div>


                <div class="result-badge">

                  <span class="result-dot"></span>

                  Diff

                </div>

              </div>


              <div
                id="diffOutput"
                class="diff-output"
              >

                <div class="empty-result">

                  <i class="fa-solid fa-code-compare"></i>

                  <strong>
                    لا توجد مقارنة بعد
                  </strong>

                  <span>
                    أدخل الكودين ثم اضغط على "مقارنة الكود"
                  </span>

                </div>

              </div>

            </div>


            <!-- DIFF STATS -->

            <div class="diff-stats">

              <div class="mini-stat">

                <div class="mini-icon added">

                  <i class="fa-solid fa-plus"></i>

                </div>

                <div>

                  <strong id="addedCount">
                    0
                  </strong>

                  <span>
                    أسطر مضافة
                  </span>

                </div>

              </div>


              <div class="mini-stat">

                <div class="mini-icon removed">

                  <i class="fa-solid fa-minus"></i>

                </div>

                <div>

                  <strong id="removedCount">
                    0
                  </strong>

                  <span>
                    أسطر محذوفة
                  </span>

                </div>

              </div>


              <div class="mini-stat">

                <div class="mini-icon same">

                  <i class="fa-solid fa-equals"></i>

                </div>

                <div>

                  <strong id="sameCount">
                    0
                  </strong>

                  <span>
                    أسطر متطابقة
                  </span>

                </div>

              </div>


              <div class="mini-stat">

                <div class="mini-icon blue">

                  <i class="fa-solid fa-code"></i>

                </div>

                <div>

                  <strong id="totalCount">
                    0
                  </strong>

                  <span>
                    إجمالي الأسطر
                  </span>

                </div>

              </div>

            </div>


          </div>


          <!-- ======================================
               INFORMATION
          ======================================= -->

          <section class="information">

            <div class="section-title">

              <span>
                Developer Tools
              </span>

              <h2>
                أداة مقارنة الأكواد الاحترافية
              </h2>

              <p>
                اكتشف التغييرات بين نسختين من الكود
                بطريقة واضحة وسريعة ومنظمة.
              </p>

            </div>


            <div class="info-grid">


              <article class="info-card">

                <div class="info-number">
                  01
                </div>

                <div class="info-card-icon">

                  <i class="fa-solid fa-code-compare"></i>

                </div>

                <h3>
                  مقارنة دقيقة
                </h3>

                <p>
                  مقارنة الأسطر بين النسخة الأصلية
                  والنسخة المعدلة وإظهار الاختلافات.
                </p>

              </article>


              <article class="info-card">

                <div class="info-number">
                  02
                </div>

                <div class="info-card-icon">

                  <i class="fa-solid fa-list-check"></i>

                </div>

                <h3>
                  معرفة التغييرات
                </h3>

                <p>
                  تعرف بسهولة على الأسطر التي تمت إضافتها
                  أو حذفها أو بقيت متطابقة.
                </p>

              </article>


              <article class="info-card">

                <div class="info-number">
                  03
                </div>

                <div class="info-card-icon">

                  <i class="fa-solid fa-shield-halved"></i>

                </div>

                <h3>
                  معالجة محلية
                </h3>

                <p>
                  تتم المقارنة داخل المتصفح دون إرسال
                  الأكواد إلى خادم خارجي.
                </p>

              </article>

            </div>

          </section>


          <!-- ======================================
               HOW TO
          ======================================= -->

          <section class="how-section">

            <div class="section-title">

              <span>
                How It Works
              </span>

              <h2>
                طريقة استخدام الأداة
              </h2>

            </div>


            <div class="steps">


              <div class="step">

                <div class="step-icon">

                  <span>
                    01
                  </span>

                </div>

                <div>

                  <h3>
                    أدخل الكود الأصلي
                  </h3>

                  <p>
                    ضع النسخة الأصلية من الكود داخل مربع Original.
                  </p>

                </div>

              </div>


              <div class="step">

                <div class="step-icon">

                  <span>
                    02
                  </span>

                </div>

                <div>

                  <h3>
                    أدخل الكود المعدل
                  </h3>

                  <p>
                    ضع النسخة الجديدة داخل مربع Modified.
                  </p>

                </div>

              </div>


              <div class="step">

                <div class="step-icon">

                  <span>
                    03
                  </span>

                </div>

                <div>

                  <h3>
                    اعرض الفرق
                  </h3>

                  <p>
                    اضغط على مقارنة الكود لرؤية جميع التغييرات.
                  </p>

                </div>

              </div>

            </div>

          </section>


          <!-- ======================================
               FAQ
          ======================================= -->

          <section class="faq-section">

            <div class="section-title">

              <span>
                FAQ
              </span>

              <h2>
                الأسئلة الشائعة
              </h2>

            </div>


            <div class="faq-list">

              {
                faqs.map((faq) => (
                  <details>

                    <summary>

                      <span>
                        {faq.q}
                      </span>

                      <i class="fa-solid fa-chevron-down"></i>

                    </summary>

                    <div class="faq-answer">
                      {faq.a}
                    </div>

                  </details>
                ))
              }

            </div>

          </section>

        </div>

      </section>

    </main>


    <Footer />


    <!-- ======================================
         JAVASCRIPT
    ======================================= -->

    <script is:inline>
      (() => {

        const oldCode =
          document.getElementById("oldCode");

        const newCode =
          document.getElementById("newCode");

        const oldLines =
          document.getElementById("oldLines");

        const newLines =
          document.getElementById("newLines");

        const oldStats =
          document.getElementById("oldStats");

        const newStats =
          document.getElementById("newStats");

        const newSize =
          document.getElementById("newSize");

        const status =
          document.getElementById("status");

        const compareBtn =
          document.getElementById("compareBtn");

        const copyBtn =
          document.getElementById("copyBtn");

        const clearBtn =
          document.getElementById("clearBtn");

        const swapBtn =
          document.getElementById("swapBtn");

        const exampleBtn =
          document.getElementById("exampleBtn");

        const diffOutput =
          document.getElementById("diffOutput");

        const addedCount =
          document.getElementById("addedCount");

        const removedCount =
          document.getElementById("removedCount");

        const sameCount =
          document.getElementById("sameCount");

        const totalCount =
          document.getElementById("totalCount");


        /* ======================================
           EXAMPLE
        ======================================= */

        const originalExample = `function calculateTotal(items) {
  let total = 0;

  for (const item of items) {
    total += item.price;
  }

  return total;
}

const products = [
  { name: "Laptop", price: 800 },
  { name: "Mouse", price: 25 }
];

console.log(calculateTotal(products));`;


        const modifiedExample = `function calculateTotal(items) {
  let total = 0;

  for (const item of items) {
    total += item.price * item.quantity;
  }

  return total;
}

const products = [
  { name: "Laptop", price: 800, quantity: 1 },
  { name: "Mouse", price: 25, quantity: 2 }
];

console.log("Total:", calculateTotal(products));`;


        /* ======================================
           STATS
        ======================================= */

        function updateStats() {

          const oldValue =
            oldCode.value;

          const newValue =
            newCode.value;


          const oldLineCount =
            oldValue
              ? oldValue.split("\n").length
              : 1;


          const newLineCount =
            newValue
              ? newValue.split("\n").length
              : 1;


          oldLines.textContent =
            Array.from(
              { length: oldLineCount },
              (_, i) => i + 1
            ).join("\n");


          newLines.textContent =
            Array.from(
              { length: newLineCount },
              (_, i) => i + 1
            ).join("\n");


          oldStats.textContent =
            `${oldValue.length} حرف`;


          newStats.textContent =
            `${newValue.length} حرف`;


          const bytes =
            new Blob([newValue]).size;


          if (bytes < 1024) {

            newSize.textContent =
              `${bytes} B`;

          } else {

            newSize.textContent =
              `${(bytes / 1024).toFixed(1)} KB`;

          }

        }


        /* ======================================
           STATUS
        ======================================= */

        function setStatus(type, text) {

          status.className =
            `status ${type}`;

          status.innerHTML = `
            <span class="status-dot"></span>
            ${text}
          `;

        }


        /* ======================================
           ESCAPE HTML
        ======================================= */

        function escapeHtml(value) {

          return value
            .replace(/&/g, "&amp;")
            .replace(/</g, "&lt;")
            .replace(/>/g, "&gt;")
            .replace(/"/g, "&quot;")
            .replace(/'/g, "&#039;");

        }


        /* ======================================
           GET LINES
        ======================================= */

        function getLines(value) {

          if (!value) {
            return [];
          }

          return value
            .replace(/\r\n/g, "\n")
            .replace(/\r/g, "\n")
            .split("\n");

        }


        /* ======================================
           LCS DIFF
        ======================================= */

        function createDiff(oldArr, newArr) {

          const rows =
            oldArr.length + 1;

          const cols =
            newArr.length + 1;


          const matrix =
            Array.from(
              { length: rows },
              () =>
                new Array(cols).fill(0)
            );


          for (
            let i = oldArr.length - 1;
            i >= 0;
            i--
          ) {

            for (
              let j = newArr.length - 1;
              j >= 0;
              j--
            ) {

              if (
                oldArr[i] === newArr[j]
              ) {

                matrix[i][j] =
                  matrix[i + 1][j + 1] + 1;

              } else {

                matrix[i][j] =
                  Math.max(
                    matrix[i + 1][j],
                    matrix[i][j + 1]
                  );

              }

            }

          }


          const result = [];

          let i = 0;
          let j = 0;


          while (
            i < oldArr.length &&
            j < newArr.length
          ) {

            if (
              oldArr[i] === newArr[j]
            ) {

              result.push({
                type: "same",
                oldLine: i + 1,
                newLine: j + 1,
                text: oldArr[i]
              });

              i++;
              j++;

            } else if (
              matrix[i + 1][j] >=
              matrix[i][j + 1]
            ) {

              result.push({
                type: "removed",
                oldLine: i + 1,
                newLine: null,
                text: oldArr[i]
              });

              i++;

            } else {

              result.push({
                type: "added",
                oldLine: null,
                newLine: j + 1,
                text: newArr[j]
              });

              j++;

            }

          }


          while (
            i < oldArr.length
          ) {

            result.push({
              type: "removed",
              oldLine: i + 1,
              newLine: null,
              text: oldArr[i]
            });

            i++;

          }


          while (
            j < newArr.length
          ) {

            result.push({
              type: "added",
              oldLine: null,
              newLine: j + 1,
              text: newArr[j]
            });

            j++;

          }


          return result;

        }


        /* ======================================
           RENDER DIFF
        ======================================= */

        function renderDiff(diff) {

          if (!diff.length) {

            diffOutput.innerHTML = `
              <div class="empty-result">
                <i class="fa-solid fa-code-compare"></i>
                <strong>لا توجد مقارنة بعد</strong>
                <span>أدخل الكودين ثم اضغط على "مقارنة الكود"</span>
              </div>
            `;

            return;

          }


          diffOutput.innerHTML =
            diff
              .map((item) => {

                let symbol = " ";

                if (item.type === "added") {
                  symbol = "+";
                }

                if (item.type === "removed") {
                  symbol = "−";
                }


                const oldNumber =
                  item.oldLine === null
                    ? ""
                    : item.oldLine;


                const newNumber =
                  item.newLine === null
                    ? ""
                    : item.newLine;


                return `
                  <div class="diff-row ${item.type}">

                    <span class="diff-old-number">
                      ${oldNumber}
                    </span>

                    <span class="diff-new-number">
                      ${newNumber}
                    </span>

                    <span class="diff-symbol">
                      ${symbol}
                    </span>

                    <code>
                      ${escapeHtml(item.text)}
                    </code>

                  </div>
                `;

              })
              .join("");

        }


        /* ======================================
           COMPARE
        ======================================= */

        function compare() {

          const oldValue =
            oldCode.value;

          const newValue =
            newCode.value;


          if (
            !oldValue.trim() &&
            !newValue.trim()
          ) {

            addedCount.textContent =
              "0";

            removedCount.textContent =
              "0";

            sameCount.textContent =
              "0";

            totalCount.textContent =
              "0";


            renderDiff([]);

            setStatus(
              "warning",
              "أدخل الكود أولاً"
            );

            oldCode.focus();

            return;

          }


          const started =
            performance.now();


          try {

            const oldArr =
              getLines(oldValue);

            const newArr =
              getLines(newValue);


            const diff =
              createDiff(
                oldArr,
                newArr
              );


            let added = 0;
            let removed = 0;
            let same = 0;


            diff.forEach((item) => {

              if (item.type === "added") {
                added++;
              }

              if (item.type === "removed") {
                removed++;
              }

              if (item.type === "same") {
                same++;
              }

            });


            addedCount.textContent =
              added;

            removedCount.textContent =
              removed;

            sameCount.textContent =
              same;

            totalCount.textContent =
              oldArr.length +
              newArr.length;


            renderDiff(diff);


            const elapsed =
              performance.now() -
              started;


            setStatus(
              "success",
              `تمت المقارنة خلال ${elapsed.toFixed(1)} ms`
            );

          } catch {

            renderDiff([]);

            setStatus(
              "error",
              "تعذر مقارنة الأكواد"
            );

          }

        }


        /* ======================================
           COPY
        ======================================= */

        async function copyResult() {

          const rows =
            diffOutput.querySelectorAll(
              ".diff-row"
            );


          if (!rows.length) {

            setStatus(
              "warning",
              "لا توجد نتيجة لنسخها"
            );

            return;

          }


          const result =
            Array.from(rows)
              .map((row) => {

                const symbol =
                  row.querySelector(
                    ".diff-symbol"
                  )?.textContent
                    .trim() || "";

                const code =
                  row.querySelector(
                    "code"
                  )?.textContent || "";

                return `${symbol} ${code}`;

              })
              .join("\n");


          try {

            await navigator.clipboard.writeText(
              result
            );


            const oldText =
              copyBtn.innerHTML;


            copyBtn.innerHTML = `
              <i class="fa-solid fa-check"></i>
              تم النسخ
            `;


            setStatus(
              "success",
              "تم نسخ نتيجة المقارنة"
            );


            setTimeout(() => {

              copyBtn.innerHTML =
                oldText;

            }, 1500);

          } catch {

            setStatus(
              "error",
              "تعذر نسخ النتيجة"
            );

          }

        }


        /* ======================================
           CLEAR
        ======================================= */

        function clearAll() {

          oldCode.value = "";

          newCode.value = "";


          addedCount.textContent =
            "0";

          removedCount.textContent =
            "0";

          sameCount.textContent =
            "0";

          totalCount.textContent =
            "0";


          renderDiff([]);

          updateStats();


          setStatus(
            "idle",
            "جاهز"
          );


          oldCode.focus();

        }


        /* ======================================
           SWAP
        ======================================= */

        function swapCodes() {

          const temporary =
            oldCode.value;


          oldCode.value =
            newCode.value;

          newCode.value =
            temporary;


          updateStats();

          compare();

        }


        /* ======================================
           EXAMPLE
        ======================================= */

        exampleBtn.addEventListener(
          "click",
          () => {

            oldCode.value =
              originalExample;

            newCode.value =
              modifiedExample;


            updateStats();

            compare();

          }
        );


        /* ======================================
           EVENTS
        ======================================= */

        compareBtn.addEventListener(
          "click",
          compare
        );


        copyBtn.addEventListener(
          "click",
          copyResult
        );


        clearBtn.addEventListener(
          "click",
          clearAll
        );


        swapBtn.addEventListener(
          "click",
          swapCodes
        );


        oldCode.addEventListener(
          "input",
          updateStats
        );


        newCode.addEventListener(
          "input",
          updateStats
        );


        oldCode.addEventListener(
          "scroll",
          () => {

            oldLines.scrollTop =
              oldCode.scrollTop;

          }
        );


        newCode.addEventListener(
          "scroll",
          () => {

            newLines.scrollTop =
              newCode.scrollTop;

          }
        );


        /* ======================================
           TAB + CTRL ENTER
        ======================================= */

        function setupEditor(editor) {

          editor.addEventListener(
            "keydown",
            (event) => {

              if (
                event.key === "Tab"
              ) {

                event.preventDefault();


                const start =
                  editor.selectionStart;

                const end =
                  editor.selectionEnd;


                editor.value =
                  editor.value.substring(
                    0,
                    start
                  ) +
                  "  " +
                  editor.value.substring(
                    end
                  );


                editor.selectionStart =
                  editor.selectionEnd =
                    start + 2;


                updateStats();

              }


              if (
                event.key === "Enter" &&
                (event.ctrlKey ||
                 event.metaKey)
              ) {

                event.preventDefault();

                compare();

              }

            }
          );

        }


        setupEditor(oldCode);
        setupEditor(newCode);


        /* ======================================
           INITIAL
        ======================================= */

        updateStats();

      })();
    </script>


    <!-- ======================================
         STYLE
    ======================================= -->

    <style>

      :root {

        --bg:
          #f3f5fb;

        --surface:
          #ffffff;

        --surface-2:
          #f7f8fc;

        --text:
          #111827;

        --muted:
          #6b7280;

        --border:
          rgba(15,23,42,.09);

        --primary:
          #4f46e5;

        --primary-dark:
          #4338ca;

        --secondary:
          #6366f1;

        --cyan:
          #06b6d4;

        --blue:
          #2563eb;

        --purple:
          #7c3aed;

        --green:
          #16a34a;

        --orange:
          #ea580c;

        --red:
          #dc2626;

        --shadow:
          0 25px 70px
          rgba(15,23,42,.09);

        --code-bg:
          #080b18;

        --code-bg-2:
          #0d1122;

        --code-text:
          #e5e7eb;

      }


      html.dark-mode {

        --bg:
          #060812;

        --surface:
          #0c1020;

        --surface-2:
          #11172a;

        --text:
          #f8fafc;

        --muted:
          #94a3b8;

        --border:
          rgba(148,163,184,.12);

        --primary:
          #818cf8;

        --primary-dark:
          #6366f1;

        --secondary:
          #a5b4fc;

        --shadow:
          0 30px 80px
          rgba(0,0,0,.4);

      }


      * {
        box-sizing:
          border-box;
      }


      body {

        margin:
          0;

        background:
          var(--bg);

        color:
          var(--text);

        font-family:
          "Cairo",
          sans-serif;

        transition:
          background .25s ease,
          color .25s ease;

      }


      button,
      textarea {

        font-family:
          inherit;

      }


      .container {

        width:
          min(
            1120px,
            calc(100% - 32px)
          );

        margin-inline:
          auto;

      }


      /* ======================================
         HERO
      ======================================= */

      .hero {

        position:
          relative;

        overflow:
          hidden;

        padding:
          34px 0 45px;

        text-align:
          center;

      }


      .hero-glow {

        position:
          absolute;

        width:
          320px;

        height:
          320px;

        border-radius:
          50%;

        filter:
          blur(90px);

        opacity:
          .15;

        pointer-events:
          none;

      }


      .glow-one {

        top:
          -170px;

        right:
          8%;

        background:
          #6366f1;

      }


      .glow-two {

        bottom:
          -210px;

        left:
          8%;

        background:
          #06b6d4;

      }


      .breadcrumb {

        display:
          flex;

        justify-content:
          center;

        align-items:
          center;

        flex-wrap:
          wrap;

        gap:
          8px;

        color:
          var(--muted);

        font-size:
          12px;

        margin-bottom:
          23px;

      }


      .breadcrumb a {

        color:
          var(--muted);

        text-decoration:
          none;

      }


      .breadcrumb a:hover {

        color:
          var(--primary);

      }


      .breadcrumb i {

        font-size:
          8px;

        opacity:
          .6;

      }


      .hero-badge {

        display:
          inline-flex;

        align-items:
          center;

        gap:
          7px;

        padding:
          7px 12px;

        border:
          1px solid
          rgba(79,70,229,.18);

        border-radius:
          999px;

        background:
          rgba(79,70,229,.07);

        color:
          var(--primary);

        font-size:
          11px;

        font-weight:
          700;

        margin-bottom:
          15px;

      }


      .badge-dot {

        width:
          6px;

        height:
          6px;

        border-radius:
          50%;

        background:
          #6366f1;

        box-shadow:
          0 0 0 4px
          rgba(99,102,241,.1);

      }


      .hero-icon {

        position:
          relative;

        width:
          72px;

        height:
          72px;

        display:
          grid;

        place-items:
          center;

        margin:
          0 auto 15px;

        border-radius:
          20px;

        background:
          linear-gradient(
            135deg,
            #4f46e5,
            #6366f1
          );

        color:
          white;

        box-shadow:
          0 15px 35px
          rgba(79,70,229,.25);

        font-size:
          24px;

      }


      .diff-mark {

        position:
          absolute;

        right:
          12px;

        bottom:
          10px;

        width:
          23px;

        height:
          23px;

        display:
          grid;

        place-items:
          center;

        border:
          2px solid
          white;

        border-radius:
          7px;

        background:
          #4338ca;

        font:
          800 14px
          "JetBrains Mono",
          monospace;

      }


      .hero h1 {

        margin:
          0;

        font-size:
          clamp(
            30px,
            5vw,
            46px
          );

        line-height:
          1.2;

        font-weight:
          800;

        letter-spacing:
          -.8px;

      }


      .hero h1 span {

        background:
          linear-gradient(
            90deg,
            var(--primary),
            var(--secondary)
          );

        -webkit-background-clip:
          text;

        background-clip:
          text;

        color:
          transparent;

      }


      .hero p {

        max-width:
          720px;

        margin:
          14px auto 0;

        color:
          var(--muted);

        font-size:
          14px;

        line-height:
          2;

      }


      /* ======================================
         MAIN CARD
      ======================================= */

      .diff-card {

        background:
          var(--surface);

        border:
          1px solid
          var(--border);

        border-radius:
          24px;

        box-shadow:
          var(--shadow);

        overflow:
          hidden;

      }


      .topbar {

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        gap:
          15px;

        padding:
          17px 20px;

        border-bottom:
          1px solid
          var(--border);

      }


      .project-info {

        display:
          flex;

        align-items:
          center;

        gap:
          11px;

      }


      .project-logo {

        width:
          40px;

        height:
          40px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          11px;

        background:
          linear-gradient(
            135deg,
            #4f46e5,
            #6366f1
          );

        color:
          white;

        font-size:
          14px;

      }


      .project-info strong {

        display:
          block;

        font-size:
          13px;

      }


      .project-info span {

        display:
          block;

        color:
          var(--muted);

        font-size:
          10px;

      }


      .topbar-status {

        display:
          flex;

        align-items:
          center;

        gap:
          7px;

        color:
          var(--muted);

        font-size:
          10px;

      }


      .online-dot {

        width:
          7px;

        height:
          7px;

        border-radius:
          50%;

        background:
          #22c55e;

        box-shadow:
          0 0 0 4px
          rgba(34,197,94,.08);

      }


      /* ======================================
         ACTION BAR
      ======================================= */

      .actionbar {

        display:
          flex;

        justify-content:
          space-between;

        align-items:
          center;

        gap:
          10px;

        padding:
          13px 18px;

        background:
          var(--surface-2);

        border-bottom:
          1px solid
          var(--border);

      }


      .action-group {

        display:
          flex;

        gap:
          8px;

      }


      .action {

        display:
          inline-flex;

        align-items:
          center;

        justify-content:
          center;

        gap:
          7px;

        border:
          1px solid
          var(--border);

        border-radius:
          10px;

        padding:
          9px 13px;

        cursor:
          pointer;

        font-size:
          11px;

        font-weight:
          700;

        transition:
          .2s ease;

      }


      .action:hover {

        transform:
          translateY(-1px);

      }


      .action.secondary {

        background:
          var(--surface);

        color:
          var(--text);

      }


      .action.secondary:hover {

        color:
          var(--primary);

        border-color:
          rgba(79,70,229,.25);

      }


      .action.primary {

        border-color:
          transparent;

        background:
          linear-gradient(
            135deg,
            var(--primary),
            var(--secondary)
          );

        color:
          white;

        box-shadow:
          0 8px 20px
          rgba(79,70,229,.2);

      }


      .action.primary:hover {

        box-shadow:
          0 11px 25px
          rgba(79,70,229,.3);

      }


      /* ======================================
         EDITORS
      ======================================= */

      .editor-grid {

        display:
          grid;

        grid-template-columns:
          repeat(
            2,
            minmax(
              0,
              1fr
            )
          );

        gap:
          14px;

        padding:
          16px;

      }


      .editor-card {

        min-width:
          0;

        overflow:
          hidden;

        border:
          1px solid
          rgba(148,163,184,.12);

        border-radius:
          16px;

        background:
          var(--code-bg);

        color:
          var(--code-text);

      }


      .editor-head {

        height:
          52px;

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        padding:
          0 14px;

        background:
          var(--code-bg-2);

        border-bottom:
          1px solid
          rgba(255,255,255,.06);

      }


      .editor-label {

        display:
          flex;

        align-items:
          center;

        gap:
          9px;

      }


      .file-icon {

        width:
          31px;

        height:
          31px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          8px;

        font-size:
          12px;

      }


      .original-icon {

        background:
          rgba(99,102,241,.13);

        color:
          #818cf8;

      }


      .modified-icon {

        background:
          rgba(6,182,212,.12);

        color:
          #67e8f9;

      }


      .editor-label strong {

        display:
          block;

        font-size:
          11px;

      }


      .editor-label small {

        display:
          block;

        margin-top:
          1px;

        color:
          #64748b;

        font-size:
          9px;

      }


      .editor-stats {

        color:
          #64748b;

        font:
          9px
          "JetBrains Mono",
          monospace;

      }


      .code-area {

        display:
          flex;

        height:
          420px;

        direction:
          ltr;

        overflow:
          hidden;

      }


      .line-numbers {

        width:
          45px;

        flex:
          0 0 45px;

        padding:
          15px 8px;

        background:
          rgba(0,0,0,.14);

        border-right:
          1px solid
          rgba(255,255,255,.05);

        color:
          #475569;

        text-align:
          right;

        white-space:
          pre;

        overflow:
          hidden;

        user-select:
          none;

        font:
          12px/1.8
          "JetBrains Mono",
          monospace;

      }


      textarea {

        width:
          100%;

        height:
          100%;

        resize:
          none;

        outline:
          none;

        border:
          0;

        padding:
          15px;

        background:
          var(--code-bg);

        color:
          var(--code-text);

        font:
          12px/1.8
          "JetBrains Mono",
          monospace;

        direction:
          ltr;

        text-align:
          left;

        tab-size:
          2;

      }


      textarea::placeholder {

        color:
          #475569;

      }


      .editor-foot {

        min-height:
          38px;

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        padding:
          0 13px;

        border-top:
          1px solid
          rgba(255,255,255,.06);

        color:
          #64748b;

        font-size:
          9px;

      }


      .editor-foot span {

        display:
          flex;

        align-items:
          center;

        gap:
          5px;

      }


      /* ======================================
         STATUS
      ======================================= */

      .status {

        display:
          flex;

        align-items:
          center;

        gap:
          6px;

      }


      .status-dot {

        width:
          6px;

        height:
          6px;

        border-radius:
          50%;

        background:
          #64748b;

      }


      .status.success {

        color:
          #4ade80;

      }


      .status.success .status-dot {

        background:
          #22c55e;

      }


      .status.warning {

        color:
          #fbbf24;

      }


      .status.warning .status-dot {

        background:
          #f59e0b;

      }


      .status.error {

        color:
          #f87171;

      }


      .status.error .status-dot {

        background:
          #ef4444;

      }


      /* ======================================
         RESULT
      ======================================= */

      .result-section {

        margin:
          0 16px 14px;

        overflow:
          hidden;

        border:
          1px solid
          rgba(79,70,229,.14);

        border-radius:
          15px;

        background:
          var(--code-bg);

        color:
          var(--code-text);

      }


      .result-head {

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        gap:
          12px;

        padding:
          12px 14px;

        background:
          var(--code-bg-2);

        border-bottom:
          1px solid
          rgba(255,255,255,.06);

      }


      .result-title {

        display:
          flex;

        align-items:
          center;

        gap:
          9px;

      }


      .result-icon {

        width:
          32px;

        height:
          32px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          8px;

        background:
          rgba(99,102,241,.13);

        color:
          #818cf8;

        font-size:
          12px;

      }


      .result-title strong {

        display:
          block;

        font-size:
          11px;

      }


      .result-title span {

        display:
          block;

        margin-top:
          1px;

        color:
          #64748b;

        font-size:
          9px;

      }


      .result-badge {

        display:
          flex;

        align-items:
          center;

        gap:
          6px;

        padding:
          5px 8px;

        border:
          1px solid
          rgba(99,102,241,.2);

        border-radius:
          999px;

        color:
          #a5b4fc;

        font:
          600 9px
          "JetBrains Mono",
          monospace;

      }


      .result-dot {

        width:
          5px;

        height:
          5px;

        border-radius:
          50%;

        background:
          #818cf8;

      }


      .diff-output {

        min-height:
          120px;

        max-height:
          460px;

        overflow:
          auto;

        direction:
          ltr;

        font:
          11px/1.8
          "JetBrains Mono",
          monospace;

      }


      .empty-result {

        min-height:
          150px;

        display:
          flex;

        flex-direction:
          column;

        align-items:
          center;

        justify-content:
          center;

        gap:
          5px;

        padding:
          25px;

        text-align:
          center;

        color:
          #64748b;

      }


      .empty-result i {

        margin-bottom:
          5px;

        color:
          #6366f1;

        font-size:
          22px;

      }


      .empty-result strong {

        color:
          #cbd5e1;

        font-size:
          11px;

      }


      .empty-result span {

        font-size:
          9px;

      }


      .diff-row {

        display:
          grid;

        grid-template-columns:
          45px 45px 28px minmax(0,1fr);

        min-height:
          28px;

        border-bottom:
          1px solid
          rgba(255,255,255,.025);

      }


      .diff-row > span {

        display:
          flex;

        align-items:
          center;

        min-width:
          0;

      }


      .diff-old-number,
      .diff-new-number {

        justify-content:
          center;

        color:
          #475569;

        background:
          rgba(0,0,0,.12);

        user-select:
          none;

        font-size:
          9px;

      }


      .diff-symbol {

        justify-content:
          center;

        color:
          #64748b;

        font-weight:
          700;

      }


      .diff-row code {

        display:
          block;

        min-width:
          0;

        padding:
          4px 10px;

        color:
          #d1d5db;

        white-space:
          pre;

        overflow-x:
          auto;

        font:
          inherit;

      }


      .diff-row.added {

        background:
          rgba(34,197,94,.09);

      }


      .diff-row.added .diff-symbol {

        color:
          #4ade80;

      }


      .diff-row.added code {

        color:
          #bbf7d0;

      }


      .diff-row.removed {

        background:
          rgba(239,68,68,.09);

      }


      .diff-row.removed .diff-symbol {

        color:
          #f87171;

      }


      .diff-row.removed code {

        color:
          #fecaca;

      }


      .diff-row.same:hover {

        background:
          rgba(255,255,255,.025);

      }


      /* ======================================
         STATS
      ======================================= */

      .diff-stats {

        display:
          grid;

        grid-template-columns:
          repeat(
            4,
            1fr
          );

        gap:
          10px;

        padding:
          0 16px 16px;

      }


      .mini-stat {

        display:
          flex;

        align-items:
          center;

        gap:
          10px;

        padding:
          12px;

        border:
          1px solid
          var(--border);

        border-radius:
          13px;

        background:
          var(--surface-2);

      }


      .mini-icon {

        width:
          35px;

        height:
          35px;

        flex:
          0 0 35px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          9px;

      }


      .mini-icon.added {

        background:
          rgba(22,163,74,.1);

        color:
          var(--green);

      }


      .mini-icon.removed {

        background:
          rgba(220,38,38,.1);

        color:
          var(--red);

      }


      .mini-icon.same {

        background:
          rgba(124,58,237,.1);

        color:
          var(--purple);

      }


      .mini-icon.blue {

        background:
          rgba(37,99,235,.1);

        color:
          var(--blue);

      }


      .mini-stat strong {

        display:
          block;

        font:
          700 12px
          "JetBrains Mono",
          monospace;

      }


      .mini-stat span {

        display:
          block;

        margin-top:
          2px;

        color:
          var(--muted);

        font-size:
          8px;

      }


      /* ======================================
         INFORMATION
      ======================================= */

      .information {

        margin-top:
          55px;

      }


      .section-title {

        text-align:
          center;

        max-width:
          700px;

        margin:
          0 auto 25px;

      }


      .section-title > span {

        color:
          var(--primary);

        font-size:
          11px;

        font-weight:
          800;

        letter-spacing:
          .5px;

      }


      .section-title h2 {

        margin:
          5px 0 7px;

        font-size:
          clamp(
            21px,
            4vw,
            28px
          );

      }


      .section-title p {

        margin:
          0;

        color:
          var(--muted);

        font-size:
          12px;

        line-height:
          1.9;

      }


      .info-grid {

        display:
          grid;

        grid-template-columns:
          repeat(
            3,
            1fr
          );

        gap:
          14px;

      }


      .info-card {

        position:
          relative;

        padding:
          24px;

        overflow:
          hidden;

        border:
          1px solid
          var(--border);

        border-radius:
          18px;

        background:
          var(--surface);

      }


      .info-number {

        position:
          absolute;

        top:
          15px;

        left:
          18px;

        color:
          var(--muted);

        opacity:
          .25;

        font:
          700 28px
          "JetBrains Mono",
          monospace;

      }


      .info-card-icon {

        width:
          42px;

        height:
          42px;

        display:
          grid;

        place-items:
          center;

        margin-bottom:
          13px;

        border-radius:
          11px;

        background:
          rgba(79,70,229,.1);

        color:
          var(--primary);

      }


      .info-card h3 {

        margin:
          0 0 7px;

        font-size:
          14px;

      }


      .info-card p {

        margin:
          0;

        color:
          var(--muted);

        font-size:
          11px;

        line-height:
          1.9;

      }


      /* ======================================
         HOW
      ======================================= */

      .how-section {

        margin-top:
          55px;

      }


      .steps {

        display:
          grid;

        grid-template-columns:
          repeat(
            3,
            1fr
          );

        gap:
          14px;

      }


      .step {

        display:
          flex;

        align-items:
          flex-start;

        gap:
          12px;

        padding:
          20px;

        background:
          var(--surface);

        border:
          1px solid
          var(--border);

        border-radius:
          17px;

      }


      .step-icon {

        width:
          43px;

        height:
          43px;

        flex:
          0 0 43px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          12px;

        background:
          linear-gradient(
            135deg,
            rgba(79,70,229,.12),
            rgba(99,102,241,.12)
          );

        color:
          var(--primary);

        font:
          700 10px
          "JetBrains Mono",
          monospace;

      }


      .step h3 {

        margin:
          0 0 5px;

        font-size:
          13px;

      }


      .step p {

        margin:
          0;

        color:
          var(--muted);

        font-size:
          10px;

        line-height:
          1.8;

      }


      /* ======================================
         FAQ
      ======================================= */

      .faq-section {

        margin:
          55px auto 70px;

        max-width:
          850px;

      }


      .faq-list details {

        margin-bottom:
          9px;

        overflow:
          hidden;

        border:
          1px solid
          var(--border);

        border-radius:
          14px;

        background:
          var(--surface);

      }


      .faq-list summary {

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        gap:
          15px;

        padding:
          16px 18px;

        cursor:
          pointer;

        list-style:
          none;

        font-size:
          12px;

        font-weight:
          700;

      }


      .faq-list summary::-webkit-details-marker {

        display:
          none;

      }


      .faq-list summary i {

        color:
          var(--muted);

        font-size:
          10px;

        transition:
          transform .2s ease;

      }


      .faq-list details[open]
      summary i {

        transform:
          rotate(180deg);

      }


      .faq-answer {

        padding:
          0 18px 17px;

        color:
          var(--muted);

        font-size:
          11px;

        line-height:
          1.9;

      }


      /* ======================================
         RESPONSIVE
      ======================================= */

      @media (max-width: 900px) {

        .editor-grid {

          grid-template-columns:
            1fr;

        }


        .info-grid,
        .steps {

          grid-template-columns:
            repeat(
              2,
              1fr
            );

        }

      }


      @media (max-width: 700px) {

        .diff-stats {

          grid-template-columns:
            repeat(
              2,
              1fr
            );

        }


        .actionbar {

          flex-direction:
            column;

          align-items:
            stretch;

        }


        .action-group {

          width:
            100%;

        }


        .action {

          flex:
            1;

        }


        .diff-row {

          grid-template-columns:
            36px 36px 25px minmax(0,1fr);

        }

      }


      @media (max-width: 600px) {

        .container {

          width:
            calc(
              100% - 20px
            );

        }


        .hero {

          padding:
            25px 0 32px;

        }


        .hero-icon {

          width:
            60px;

          height:
            60px;

          font-size:
            20px;

        }


        .hero p {

          font-size:
            12px;

        }


        .topbar {

          padding:
            14px;

        }


        .topbar-status {

          display:
            none;

        }


        .actionbar {

          padding:
            10px;

        }


        .action {

          padding:
            9px 7px;

          font-size:
            10px;

        }


        .editor-grid {

          padding:
            10px;

        }


        .code-area {

          height:
            330px;

        }


        .result-section {

          margin:
            0 10px 10px;

        }


        .diff-stats {

          padding:
            0 10px 10px;

        }


        .diff-output {

          max-height:
            360px;

        }


        .diff-row {

          grid-template-columns:
            31px 31px 24px minmax(0,1fr);

          font-size:
            10px;

        }


        .diff-row code {

          padding:
            4px 7px;

        }


        .info-grid,
        .steps {

          grid-template-columns:
            1fr;

        }


        .faq-section {

          margin-bottom:
            45px;

        }

      }

    </style>

  </body>
</html>
