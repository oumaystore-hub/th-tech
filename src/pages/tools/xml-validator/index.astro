---
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const title = "XML Validator - التحقق من XML أونلاين مجاناً | Th.Tech";

const description =
  "أداة XML Validator مجانية للتحقق من صحة XML واكتشاف أخطاء البنية والعناصر والخصائص مباشرة من المتصفح.";

const faqs = [
  {
    q: "ما هي أداة XML Validator؟",
    a: "أداة XML Validator تساعدك على التحقق من صحة بنية XML واكتشاف الأخطاء قبل استخدام الملف في مشروعك."
  },
  {
    q: "هل يتم إرسال XML إلى الخادم؟",
    a: "لا. تتم عملية التحقق مباشرة داخل المتصفح، لذلك لا تحتاج إلى رفع XML إلى خادم خارجي."
  },
  {
    q: "ما الأخطاء التي يمكن اكتشافها؟",
    a: "يمكن اكتشاف أخطاء مثل العناصر غير المغلقة، العناصر المتداخلة بشكل غير صحيح، الخصائص غير الصالحة، ومشاكل بناء XML."
  },
  {
    q: "هل يمكن استخدام الأداة مع XML كبير؟",
    a: "نعم، يمكن استخدامها مع معظم ملفات XML المعتادة، ويعتمد الحد العملي على حجم الملف وإمكانيات جهازك."
  }
];
---

<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>{title}</title>
    <meta name="description" content={description} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <link
      href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap"
      rel="stylesheet"
    />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
    />

    <script is:inline>
      (() => {
        try {
          const saved = localStorage.getItem("theme");

          if (saved === "dark") {
            document.documentElement.classList.add("dark-mode");
          }
        } catch {}
      })();
    </script>
  </head>

  <body>
    <Header />

    <main class="page">

      <!-- HERO -->
      <section class="hero">

        <div class="breadcrumb">
          <a href={`${base}/`}>الرئيسية</a>
          <i class="fa-solid fa-chevron-left"></i>

          <a href={`${base}/tools/`}>الأدوات</a>
          <i class="fa-solid fa-chevron-left"></i>

          <span>XML Validator</span>
        </div>

        <div class="hero-badge">
          <i class="fa-solid fa-code"></i>
          Developer Tools
        </div>

        <div class="hero-icon">
          <div class="icon-box">
            <i class="fa-solid fa-circle-check"></i>

            <span class="xml-mark">&lt;/&gt;</span>
          </div>
        </div>

        <h1>XML Validator</h1>

        <p>
          تحقق من صحة XML واكتشف أخطاء البنية والعناصر والخصائص
          بسرعة مباشرة من متصفحك.
        </p>

        <div class="hero-tags">

          <span>
            <i class="fa-solid fa-bolt"></i>
            سريع
          </span>

          <span>
            <i class="fa-solid fa-shield-halved"></i>
            آمن
          </span>

          <span>
            <i class="fa-solid fa-lock"></i>
            محلي 100%
          </span>

        </div>

      </section>

      <!-- TOOL -->
      <section class="tool-section">

        <div class="validator-card">

          <div class="card-topbar">

            <div class="topbar-title">
              <span class="status-dot"></span>
              XML Validator
            </div>

            <div class="topbar-type">
              <i class="fa-solid fa-file-code"></i>
              XML
            </div>

          </div>

          <!-- ACTION BAR -->
          <div class="actionbar">

            <button id="exampleBtn" class="action-btn secondary">
              <i class="fa-solid fa-flask"></i>
              مثال
            </button>

            <button id="clearBtn" class="action-btn danger">
              <i class="fa-solid fa-trash"></i>
              مسح
            </button>

            <button id="copyBtn" class="action-btn secondary">
              <i class="fa-solid fa-copy"></i>
              نسخ
            </button>

            <button id="validateBtn" class="action-btn primary">
              <i class="fa-solid fa-circle-check"></i>
              تحقق من XML
            </button>

          </div>

          <!-- EDITOR -->
          <div class="editor-panel">

            <div class="editor-header">

              <div class="editor-title">

                <span class="file-icon">
                  <i class="fa-solid fa-file-code"></i>
                </span>

                <div>
                  <strong>XML Input</strong>
                  <small>XML للتحقق</small>
                </div>

              </div>

              <span id="languageBadge" class="language-badge">
                XML
              </span>

            </div>

            <div class="editor-wrap">

              <div id="lineNumbers" class="line-numbers">1</div>

              <textarea
                id="xmlInput"
                spellcheck="false"
                placeholder={`<?xml version="1.0" encoding="UTF-8"?>
<root>
  <item>
    <name>Th.Tech</name>
  </item>
</root>`}
              ></textarea>

            </div>

            <div class="editor-footer">

              <span>
                <i class="fa-solid fa-keyboard"></i>
                Ctrl + Enter للتحقق
              </span>

              <span id="inputStats">
                0 حرف
              </span>

            </div>

          </div>

          <!-- VALIDATION RESULT -->
          <div id="validationResult" class="validation-result hidden">

            <div id="validationIcon" class="validation-icon">
              <i class="fa-solid fa-check"></i>
            </div>

            <div class="validation-content">

              <strong id="validationTitle">
                XML صالح
              </strong>

              <p id="validationMessage">
                بنية XML صحيحة.
              </p>

            </div>

          </div>

          <!-- DETAILS -->
          <div id="detailsBox" class="details-box hidden">

            <div class="detail-item">
              <i class="fa-solid fa-circle-info"></i>

              <div>
                <strong>الحالة</strong>
                <span id="detailStatus">—</span>
              </div>
            </div>

            <div class="detail-item">
              <i class="fa-solid fa-file-code"></i>

              <div>
                <strong>العناصر</strong>
                <span id="detailElements">0</span>
              </div>
            </div>

            <div class="detail-item">
              <i class="fa-solid fa-layer-group"></i>

              <div>
                <strong>المستوى</strong>
                <span id="detailDepth">0</span>
              </div>
            </div>

          </div>

          <!-- STATS -->
          <div class="stats-grid">

            <div class="stat-card">

              <div class="stat-icon">
                <i class="fa-solid fa-file-code"></i>
              </div>

              <div>
                <strong id="statLines">0</strong>
                <span>سطر</span>
              </div>

            </div>

            <div class="stat-card">

              <div class="stat-icon">
                <i class="fa-solid fa-font"></i>
              </div>

              <div>
                <strong id="statChars">0</strong>
                <span>حرف</span>
              </div>

            </div>

            <div class="stat-card">

              <div class="stat-icon">
                <i class="fa-solid fa-code"></i>
              </div>

              <div>
                <strong id="statElements">0</strong>
                <span>عنصر</span>
              </div>

            </div>

            <div class="stat-card">

              <div class="stat-icon">
                <i class="fa-solid fa-clock"></i>
              </div>

              <div>
                <strong id="statTime">0 ms</strong>
                <span>الوقت</span>
              </div>

            </div>

          </div>

        </div>

      </section>

      <!-- INFO -->
      <section class="info-section">

        <div class="section-heading">

          <span class="section-badge">
            <i class="fa-solid fa-circle-info"></i>
            معلومات
          </span>

          <h2>ما هي أداة XML Validator؟</h2>

          <p>
            أداة مجانية للتحقق من صحة ملفات XML واكتشاف المشاكل
            في البنية قبل استخدامها في التطبيقات والمشاريع.
          </p>

        </div>

        <div class="info-grid">

          <article class="info-card">

            <div class="info-icon">
              <i class="fa-solid fa-circle-check"></i>
            </div>

            <h3>تحقق دقيق</h3>

            <p>
              استخدم محلل XML المدمج في المتصفح للتحقق من البنية
              واكتشاف الأخطاء الأساسية.
            </p>

          </article>

          <article class="info-card">

            <div class="info-icon">
              <i class="fa-solid fa-shield-halved"></i>
            </div>

            <h3>خصوصية كاملة</h3>

            <p>
              تتم معالجة البيانات محلياً داخل المتصفح دون إرسال
              محتوى XML إلى خادم خارجي.
            </p>

          </article>

          <article class="info-card">

            <div class="info-icon">
              <i class="fa-solid fa-gauge-high"></i>
            </div>

            <h3>نتيجة فورية</h3>

            <p>
              احصل على نتيجة التحقق والإحصائيات خلال لحظات
              دون تثبيت أي برنامج.
            </p>

          </article>

        </div>

      </section>

      <!-- HOW TO -->
      <section class="how-section">

        <div class="section-heading">

          <span class="section-badge">
            <i class="fa-solid fa-list-ol"></i>
            طريقة الاستخدام
          </span>

          <h2>كيف تستخدم XML Validator؟</h2>

        </div>

        <div class="steps">

          <div class="step">

            <div class="step-number">1</div>

            <div>
              <h3>أدخل XML</h3>

              <p>
                الصق كود XML في محرر الأداة.
              </p>
            </div>

          </div>

          <div class="step">

            <div class="step-number">2</div>

            <div>
              <h3>ابدأ التحقق</h3>

              <p>
                اضغط على «تحقق من XML» أو استخدم Ctrl + Enter.
              </p>
            </div>

          </div>

          <div class="step">

            <div class="step-number">3</div>

            <div>
              <h3>راجع النتيجة</h3>

              <p>
                ستظهر حالة XML والإحصائيات الخاصة بالملف.
              </p>
            </div>

          </div>

        </div>

      </section>

      <!-- FAQ -->
      <section class="faq-section">

        <div class="section-heading">

          <span class="section-badge">
            <i class="fa-solid fa-circle-question"></i>
            FAQ
          </span>

          <h2>الأسئلة الشائعة</h2>

        </div>

        <div class="faq-list">

          {faqs.map((faq) => (
            <details class="faq-item">

              <summary>
                <span>{faq.q}</span>
                <i class="fa-solid fa-chevron-down"></i>
              </summary>

              <div class="faq-answer">
                {faq.a}
              </div>

            </details>
          ))}

        </div>

      </section>

    </main>

    <Footer />

    <script is:inline>
      (() => {
        const input = document.getElementById("xmlInput");

        const validateBtn = document.getElementById("validateBtn");
        const clearBtn = document.getElementById("clearBtn");
        const copyBtn = document.getElementById("copyBtn");
        const exampleBtn = document.getElementById("exampleBtn");

        const lineNumbers = document.getElementById("lineNumbers");

        const inputStats = document.getElementById("inputStats");

        const validationResult =
          document.getElementById("validationResult");

        const validationIcon =
          document.getElementById("validationIcon");

        const validationTitle =
          document.getElementById("validationTitle");

        const validationMessage =
          document.getElementById("validationMessage");

        const detailsBox =
          document.getElementById("detailsBox");

        const detailStatus =
          document.getElementById("detailStatus");

        const detailElements =
          document.getElementById("detailElements");

        const detailDepth =
          document.getElementById("detailDepth");

        const statLines =
          document.getElementById("statLines");

        const statChars =
          document.getElementById("statChars");

        const statElements =
          document.getElementById("statElements");

        const statTime =
          document.getElementById("statTime");

        const exampleXML = `<?xml version="1.0" encoding="UTF-8"?>
<catalog>
  <book id="1">
    <title>Learn JavaScript</title>
    <author>Th.Tech</author>
    <category>Programming</category>
    <price currency="USD">29.99</price>
  </book>
  <book id="2">
    <title>Modern Web Development</title>
    <author>Th.Tech</author>
    <category>Web</category>
    <price currency="USD">39.99</price>
  </book>
</catalog>`;

        function updateLines() {
          const value = input.value || "";

          const count = value
            ? value.split(/\r?\n/).length
            : 1;

          lineNumbers.textContent = Array.from(
            { length: count },
            (_, index) => index + 1
          ).join("\n");

          inputStats.textContent =
            `${value.length.toLocaleString()} حرف`;

          statLines.textContent =
            value ? count.toLocaleString() : "0";

          statChars.textContent =
            value.length.toLocaleString();
        }

        function calculateDepth(documentXML) {
          let maxDepth = 0;

          function walk(element, depth) {
            maxDepth = Math.max(maxDepth, depth);

            Array.from(element.children).forEach((child) => {
              walk(child, depth + 1);
            });
          }

          if (documentXML.documentElement) {
            walk(documentXML.documentElement, 1);
          }

          return maxDepth;
        }

        function showValid(
          elements,
          depth,
          elapsed
        ) {
          validationResult.classList.remove(
            "hidden",
            "invalid"
          );

          validationIcon.className =
            "validation-icon valid";

          validationIcon.innerHTML =
            '<i class="fa-solid fa-check"></i>';

          validationTitle.textContent =
            "XML صالح";

          validationMessage.textContent =
            "تم التحقق من بنية XML بنجاح ولا توجد أخطاء نحوية.";

          detailsBox.classList.remove("hidden");

          detailStatus.textContent =
            "Valid";

          detailStatus.className =
            "valid-text";

          detailElements.textContent =
            elements.toLocaleString();

          detailDepth.textContent =
            depth.toLocaleString();

          statElements.textContent =
            elements.toLocaleString();

          statTime.textContent =
            `${elapsed.toFixed(1)} ms`;
        }

        function showInvalid(message, elapsed) {
          validationResult.classList.remove(
            "hidden"
          );

          validationResult.classList.add(
            "invalid"
          );

          validationIcon.className =
            "validation-icon invalid-icon";

          validationIcon.innerHTML =
            '<i class="fa-solid fa-xmark"></i>';

          validationTitle.textContent =
            "XML غير صالح";

          validationMessage.textContent =
            message ||
            "تعذر التحقق من بنية XML. تأكد من إغلاق العناصر بشكل صحيح.";

          detailsBox.classList.remove("hidden");

          detailStatus.textContent =
            "Invalid";

          detailStatus.className =
            "invalid-text";

          detailElements.textContent =
            "—";

          detailDepth.textContent =
            "—";

          statElements.textContent =
            "—";

          statTime.textContent =
            `${elapsed.toFixed(1)} ms`;
        }

        function validateXML() {
          const source = input.value.trim();

          if (!source) {
            validationResult.classList.remove(
              "hidden"
            );

            validationResult.classList.add(
              "invalid"
            );

            validationIcon.className =
              "validation-icon invalid-icon";

            validationIcon.innerHTML =
              '<i class="fa-solid fa-triangle-exclamation"></i>';

            validationTitle.textContent =
              "لا يوجد XML";

            validationMessage.textContent =
              "أدخل كود XML أولاً ثم حاول مرة أخرى.";

            detailsBox.classList.add("hidden");

            statElements.textContent = "0";
            statTime.textContent = "0 ms";

            return;
          }

          const start = performance.now();

          try {
            const parser = new DOMParser();

            const documentXML =
              parser.parseFromString(
                source,
                "application/xml"
              );

            const parserError =
              documentXML.querySelector(
                "parsererror"
              );

            if (
              parserError ||
              !documentXML.documentElement
            ) {
              throw new Error(
                "XML غير صالح"
              );
            }

            const root =
              documentXML.documentElement;

            const elements =
              documentXML.querySelectorAll("*").length;

            const depth =
              calculateDepth(documentXML);

            const elapsed =
              performance.now() - start;

            showValid(
              elements,
              depth,
              elapsed
            );

          } catch (error) {
            const elapsed =
              performance.now() - start;

            showInvalid(
              "تم اكتشاف خطأ في بناء XML. تحقق من العناصر والخصائص والأقواس والإغلاق.",
              elapsed
            );
          }
        }

        function clearAll() {
          input.value = "";

          validationResult.classList.add(
            "hidden"
          );

          validationResult.classList.remove(
            "invalid"
          );

          detailsBox.classList.add(
            "hidden"
          );

          statLines.textContent = "0";
          statChars.textContent = "0";
          statElements.textContent = "0";
          statTime.textContent = "0 ms";

          updateLines();

          input.focus();
        }

        async function copyXML() {
          const value = input.value;

          if (!value.trim()) {
            return;
          }

          try {
            await navigator.clipboard.writeText(
              value
            );

            copyBtn.innerHTML =
              '<i class="fa-solid fa-check"></i> تم النسخ';

            setTimeout(() => {
              copyBtn.innerHTML =
                '<i class="fa-solid fa-copy"></i> نسخ';
            }, 1500);

          } catch {
            input.select();
            document.execCommand("copy");
          }
        }

        exampleBtn.addEventListener(
          "click",
          () => {
            input.value = exampleXML;
            updateLines();
            validateXML();
          }
        );

        validateBtn.addEventListener(
          "click",
          validateXML
        );

        clearBtn.addEventListener(
          "click",
          clearAll
        );

        copyBtn.addEventListener(
          "click",
          copyXML
        );

        input.addEventListener(
          "input",
          updateLines
        );

        input.addEventListener(
          "keydown",
          (event) => {

            if (event.key === "Tab") {
              event.preventDefault();

              const start =
                input.selectionStart;

              const end =
                input.selectionEnd;

              input.value =
                input.value.substring(0, start) +
                "  " +
                input.value.substring(end);

              input.selectionStart =
                input.selectionEnd =
                  start + 2;

              updateLines();
            }

            if (
              event.ctrlKey &&
              event.key === "Enter"
            ) {
              event.preventDefault();
              validateXML();
            }
          }
        );

        input.addEventListener(
          "scroll",
          () => {
            lineNumbers.scrollTop =
              input.scrollTop;
          }
        );

        updateLines();
      })();
    </script>

    <style>
      :global(*) {
        box-sizing: border-box;
      }

      :global(html) {
        --bg: #f5f8ff;
        --surface: #ffffff;
        --surface-2: #f8fafc;
        --text: #172033;
        --muted: #718096;
        --border: rgba(37, 99, 235, 0.12);
        --primary: #2563eb;
        --primary-dark: #1d4ed8;
        --secondary: #60a5fa;
        --shadow: 0 25px 70px rgba(15, 23, 42, 0.09);
      }

      :global(html.dark-mode) {
        --bg: #070a12;
        --surface: #0d111c;
        --surface-2: #121827;
        --text: #f8fafc;
        --muted: #94a3b8;
        --border: rgba(96, 165, 250, 0.14);
        --primary: #60a5fa;
        --primary-dark: #3b82f6;
        --secondary: #93c5fd;
        --shadow: 0 30px 80px rgba(0, 0, 0, 0.48);
      }

      :global(body) {
        margin: 0;
        background: var(--bg);
        color: var(--text);
        font-family: "Cairo", sans-serif;
        transition:
          background 0.25s ease,
          color 0.25s ease;
      }

      .page {
        min-height: 100vh;
        overflow: hidden;
      }

      /* HERO */

      .hero {
        max-width: 1100px;
        margin: auto;
        padding: 38px 20px 34px;
        text-align: center;
      }

      .breadcrumb {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-wrap: wrap;
        gap: 9px;
        margin-bottom: 24px;
        color: var(--muted);
        font-size: 13px;
      }

      .breadcrumb a {
        color: var(--muted);
        text-decoration: none;
      }

      .breadcrumb a:hover {
        color: var(--primary);
      }

      .breadcrumb i {
        font-size: 9px;
      }

      .hero-badge,
      .section-badge {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 7px 13px;
        border-radius: 999px;
        background: rgba(37, 99, 235, 0.08);
        border: 1px solid rgba(37, 99, 235, 0.15);
        color: var(--primary);
        font-size: 12px;
        font-weight: 800;
      }

      .hero-icon {
        margin: 22px auto 12px;
      }

      .icon-box {
        position: relative;
        width: 84px;
        height: 84px;
        margin: auto;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 25px;
        color: var(--primary);
        background: linear-gradient(
          145deg,
          rgba(37, 99, 235, 0.14),
          rgba(96, 165, 250, 0.05)
        );
        box-shadow:
          inset 0 0 0 1px rgba(37, 99, 235, 0.12),
          0 15px 40px rgba(37, 99, 235, 0.09);
      }

      .icon-box > i {
        font-size: 38px;
      }

      .xml-mark {
        position: absolute;
        right: 8px;
        bottom: 8px;
        color: var(--secondary);
        font: 700 9px "JetBrains Mono", monospace;
      }

      .hero h1 {
        margin: 10px 0 8px;
        font-size: clamp(30px, 5vw, 46px);
        line-height: 1.2;
        font-weight: 800;
      }

      .hero p {
        max-width: 700px;
        margin: auto;
        color: var(--muted);
        font-size: 15px;
        line-height: 1.9;
      }

      .hero-tags {
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
        gap: 9px;
        margin-top: 18px;
      }

      .hero-tags span {
        display: inline-flex;
        align-items: center;
        gap: 7px;
        padding: 6px 11px;
        border-radius: 10px;
        background: var(--surface);
        border: 1px solid var(--border);
        color: var(--muted);
        font-size: 11px;
        font-weight: 700;
      }

      .hero-tags i {
        color: var(--primary);
      }

      /* TOOL */

      .tool-section {
        max-width: 1180px;
        margin: auto;
        padding: 0 18px 50px;
      }

      .validator-card {
        overflow: hidden;
        background: var(--surface);
        border: 1px solid var(--border);
        border-radius: 24px;
        box-shadow: var(--shadow);
      }

      .card-topbar {
        min-height: 54px;
        padding: 0 20px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 15px;
        background: var(--surface-2);
        border-bottom: 1px solid var(--border);
      }

      .topbar-title,
      .topbar-type {
        display: flex;
        align-items: center;
        gap: 9px;
        font-size: 13px;
        font-weight: 800;
      }

      .topbar-type {
        color: var(--muted);
        font: 600 11px "JetBrains Mono", monospace;
      }

      .status-dot {
        width: 9px;
        height: 9px;
        border-radius: 50%;
        background: #22c55e;
        box-shadow: 0 0 0 4px rgba(34, 197, 94, 0.1);
      }

      .actionbar {
        display: flex;
        flex-wrap: wrap;
        gap: 9px;
        padding: 14px 16px;
        border-bottom: 1px solid var(--border);
      }

      .action-btn {
        min-height: 40px;
        padding: 0 14px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
        border: 1px solid var(--border);
        border-radius: 10px;
        cursor: pointer;
        font-family: "Cairo", sans-serif;
        font-size: 12px;
        font-weight: 800;
        transition: 0.2s ease;
      }

      .action-btn:hover {
        transform: translateY(-2px);
      }

      .action-btn.primary {
        margin-right: auto;
        background: linear-gradient(
          135deg,
          var(--primary),
          var(--primary-dark)
        );
        border-color: transparent;
        color: white;
      }

      .action-btn.secondary {
        background: var(--surface);
        color: var(--text);
      }

      .action-btn.secondary:hover {
        border-color: var(--primary);
        color: var(--primary);
      }

      .action-btn.danger {
        background: rgba(239, 68, 68, 0.06);
        border-color: rgba(239, 68, 68, 0.13);
        color: #ef4444;
      }

      /* EDITOR */

      .editor-panel {
        background: var(--surface);
      }

      .editor-header {
        min-height: 63px;
        padding: 10px 15px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
        background: var(--surface-2);
        border-bottom: 1px solid var(--border);
      }

      .editor-title {
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .editor-title > div {
        display: flex;
        flex-direction: column;
        gap: 1px;
      }

      .editor-title strong {
        font-size: 12px;
      }

      .editor-title small {
        color: var(--muted);
        font-size: 10px;
      }

      .file-icon {
        width: 36px;
        height: 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 10px;
        background: rgba(37, 99, 235, 0.1);
        color: var(--primary);
      }

      .language-badge {
        padding: 4px 8px;
        border-radius: 7px;
        background: rgba(37, 99, 235, 0.09);
        color: var(--primary);
        font: 700 9px "JetBrains Mono", monospace;
      }

      .editor-wrap {
        display: grid;
        grid-template-columns: 48px 1fr;
        min-height: 430px;
        direction: ltr;
        background: #0d1117;
      }

      .line-numbers {
        padding: 18px 10px 18px 5px;
        overflow: hidden;
        border-right: 1px solid rgba(255, 255, 255, 0.06);
        background: #090c11;
        color: #64748b;
        text-align: right;
        white-space: pre;
        user-select: none;
        font: 12px/1.75 "JetBrains Mono", monospace;
      }

      textarea {
        width: 100%;
        height: 430px;
        padding: 18px;
        resize: none;
        outline: none;
        border: 0;
        background: #0d1117;
        color: #e6edf3;
        direction: ltr;
        text-align: left;
        tab-size: 2;
        font: 13px/1.75 "JetBrains Mono", monospace;
      }

      textarea::placeholder {
        color: #64748b;
      }

      .editor-footer {
        min-height: 39px;
        padding: 0 14px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        color: var(--muted);
        background: var(--surface-2);
        border-top: 1px solid var(--border);
        font-size: 10px;
      }

      .editor-footer span {
        display: inline-flex;
        align-items: center;
        gap: 6px;
      }

      .editor-footer i {
        color: var(--primary);
      }

      /* RESULT */

      .validation-result {
        margin: 18px;
        padding: 15px 16px;
        display: flex;
        align-items: center;
        gap: 13px;
        border-radius: 14px;
        background: rgba(34, 197, 94, 0.07);
        border: 1px solid rgba(34, 197, 94, 0.16);
      }

      .validation-result.hidden {
        display: none;
      }

      .validation-result.invalid {
        background: rgba(239, 68, 68, 0.07);
        border-color: rgba(239, 68, 68, 0.16);
      }

      .validation-icon {
        width: 40px;
        height: 40px;
        flex: 0 0 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 11px;
        background: rgba(34, 197, 94, 0.12);
        color: #16a34a;
      }

      .validation-icon.invalid-icon {
        background: rgba(239, 68, 68, 0.12);
        color: #ef4444;
      }

      .validation-content strong {
        display: block;
        margin-bottom: 3px;
        font-size: 13px;
      }

      .validation-content p {
        margin: 0;
        color: var(--muted);
        font-size: 11px;
        line-height: 1.7;
      }

      /* DETAILS */

      .details-box {
        margin: 0 18px 18px;
        padding: 12px;
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 10px;
        border-radius: 13px;
        background: var(--surface-2);
        border: 1px solid var(--border);
      }

      .details-box.hidden {
        display: none;
      }

      .detail-item {
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .detail-item > i {
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 8px;
        background: rgba(37, 99, 235, 0.09);
        color: var(--primary);
        font-size: 12px;
      }

      .detail-item div {
        display: flex;
        flex-direction: column;
      }

      .detail-item strong {
        font-size: 10px;
      }

      .detail-item span {
        color: var(--muted);
        font: 600 10px "JetBrains Mono", monospace;
      }

      .valid-text {
        color: #16a34a !important;
      }

      .invalid-text {
        color: #ef4444 !important;
      }

      /* STATS */

      .stats-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 10px;
        padding: 0 18px 18px;
      }

      .stat-card {
        min-height: 76px;
        padding: 12px;
        display: flex;
        align-items: center;
        gap: 11px;
        border-radius: 13px;
        background: var(--surface-2);
        border: 1px solid var(--border);
      }

      .stat-icon {
        width: 36px;
        height: 36px;
        flex: 0 0 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 9px;
        background: rgba(37, 99, 235, 0.1);
        color: var(--primary);
      }

      .stat-card div:last-child {
        display: flex;
        flex-direction: column;
      }

      .stat-card strong {
        font: 700 16px "JetBrains Mono", monospace;
      }

      .stat-card span {
        color: var(--muted);
        font-size: 9px;
      }

      /* SECTIONS */

      .info-section,
      .how-section,
      .faq-section {
        max-width: 1100px;
        margin: auto;
        padding: 40px 20px;
      }

      .section-heading {
        margin-bottom: 25px;
        text-align: center;
      }

      .section-heading h2 {
        margin: 12px 0 7px;
        font-size: clamp(23px, 4vw, 31px);
        font-weight: 800;
      }

      .section-heading > p {
        max-width: 650px;
        margin: auto;
        color: var(--muted);
        font-size: 13px;
        line-height: 1.9;
      }

      .info-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 15px;
      }

      .info-card {
        padding: 24px;
        border-radius: 18px;
        background: var(--surface);
        border: 1px solid var(--border);
        transition: 0.2s ease;
      }

      .info-card:hover {
        transform: translateY(-4px);
        border-color: rgba(37, 99, 235, 0.28);
      }

      .info-icon {
        width: 44px;
        height: 44px;
        margin-bottom: 15px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 12px;
        background: rgba(37, 99, 235, 0.1);
        color: var(--primary);
      }

      .info-card h3 {
        margin: 0 0 7px;
        font-size: 15px;
      }

      .info-card p {
        margin: 0;
        color: var(--muted);
        font-size: 12px;
        line-height: 1.9;
      }

      /* STEPS */

      .steps {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 15px;
      }

      .step {
        padding: 20px;
        display: flex;
        align-items: flex-start;
        gap: 13px;
        border-radius: 17px;
        background: var(--surface);
        border: 1px solid var(--border);
      }

      .step-number {
        width: 36px;
        height: 36px;
        flex: 0 0 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 10px;
        background: linear-gradient(
          135deg,
          var(--primary),
          var(--primary-dark)
        );
        color: white;
        font: 700 14px "JetBrains Mono", monospace;
      }

      .step h3 {
        margin: 0 0 4px;
        font-size: 14px;
      }

      .step p {
        margin: 0;
        color: var(--muted);
        font-size: 11px;
        line-height: 1.8;
      }

      /* FAQ */

      .faq-list {
        display: grid;
        gap: 10px;
      }

      .faq-item {
        overflow: hidden;
        border-radius: 14px;
        background: var(--surface);
        border: 1px solid var(--border);
      }

      .faq-item summary {
        padding: 17px 18px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 15px;
        cursor: pointer;
        list-style: none;
        font-size: 13px;
        font-weight: 700;
      }

      .faq-item summary::-webkit-details-marker {
        display: none;
      }

      .faq-item summary i {
        color: var(--primary);
        font-size: 11px;
        transition: transform 0.2s ease;
      }

      .faq-item[open] summary i {
        transform: rotate(180deg);
      }

      .faq-answer {
        padding: 0 18px 18px;
        color: var(--muted);
        font-size: 12px;
        line-height: 1.9;
      }

      /* MOBILE */

      @media (max-width: 850px) {
        .info-grid,
        .steps {
          grid-template-columns: 1fr;
        }
      }

      @media (max-width: 650px) {
        .hero {
          padding: 25px 14px 28px;
        }

        .tool-section {
          padding: 0 10px 35px;
        }

        .validator-card {
          border-radius: 18px;
        }

        .actionbar {
          padding: 11px;
        }

        .action-btn {
          flex: 1 1 calc(50% - 9px);
        }

        .action-btn.primary {
          order: -1;
          flex-basis: 100%;
          margin-right: 0;
        }

        .editor-wrap {
          grid-template-columns: 38px 1fr;
          min-height: 320px;
        }

        textarea {
          height: 320px;
          padding: 15px 12px;
          font-size: 12px;
        }

        .line-numbers {
          padding-right: 7px;
          font-size: 11px;
        }

        .details-box {
          grid-template-columns: 1fr;
        }

        .stats-grid {
          grid-template-columns: repeat(2, 1fr);
          padding: 0 11px 11px;
        }

        .validation-result {
          margin: 12px;
        }

        .details-box {
          margin: 0 12px 12px;
        }

        .info-section,
        .how-section,
        .faq-section {
          padding: 32px 14px;
        }

        .info-card {
          padding: 20px;
        }

        .hero h1 {
          font-size: 30px;
        }

        .hero p {
          font-size: 13px;
        }
      }

      @media (max-width: 390px) {
        .action-btn {
          padding: 0 9px;
          font-size: 11px;
        }

        .topbar-title {
          font-size: 11px;
        }

        .topbar-type {
          font-size: 9px;
        }
      }
    </style>

  </body>
</html>
