---
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const title = "JavaScript Playground - تشغيل JavaScript أونلاين مجاناً | Th.Tech";

const description =
  "JavaScript Playground مجانية لتجربة وتشغيل أكواد JavaScript مباشرة من المتصفح مع عرض النتائج والأخطاء.";

const faqs = [
  {
    q: "ما هو JavaScript Playground؟",
    a: "أداة تفاعلية تسمح لك بكتابة وتشغيل JavaScript مباشرة من المتصفح ومشاهدة النتائج فوراً."
  },
  {
    q: "هل أحتاج إلى تثبيت JavaScript؟",
    a: "لا. يتم تشغيل JavaScript بواسطة المتصفح مباشرة، ولا تحتاج إلى تثبيت أي برنامج إضافي."
  },
  {
    q: "هل يمكن استخدام console.log؟",
    a: "نعم. يمكنك استخدام console.log وconsole.warn وconsole.error وconsole.info وستظهر المخرجات داخل قسم النتائج."
  },
  {
    q: "هل الكود الخاص بي يُرسل إلى الخادم؟",
    a: "لا. يتم تنفيذ الكود داخل المتصفح باستخدام Web Worker، ولا يتم إرسال الكود إلى خادم خارجي."
  },
  {
    q: "هل يمكن إيقاف كود لا نهائي؟",
    a: "نعم. يتم تشغيل الكود داخل Web Worker ويمكن إيقافه تلقائياً عند تجاوز وقت التنفيذ المحدد."
  }
];
---

<!doctype html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>{title}</title>
    <meta name="description" content={description} />

    <meta name="robots" content="index, follow" />
    <meta name="theme-color" content="#2563eb" />

    <link rel="icon" href={`${base}/logo1.png`} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
    />

    <script is:inline>
      (() => {
        try {
          const saved = localStorage.getItem('theme');

          if (saved === 'dark') {
            document.documentElement.classList.add('dark-mode');
          } else {
            document.documentElement.classList.remove('dark-mode');
          }
        } catch {}
      })();
    </script>
  </head>

  <body>
    <Header />

    <main class="page">

      <!-- HERO -->
      <section class="hero">
        <div class="container">

          <div class="breadcrumb">
            <a href={`${base}/`}>الرئيسية</a>
            <span>/</span>
            <a href={`${base}/tools/`}>الأدوات</a>
            <span>/</span>
            <span>JavaScript Playground</span>
          </div>

          <div class="hero-icon">
            <i class="fa-brands fa-js"></i>
          </div>

          <div class="hero-kicker">
            <i class="fa-solid fa-terminal"></i>
            JavaScript Tools
          </div>

          <h1>
            JavaScript
            <span>Playground</span>
          </h1>

          <p>
            اكتب وشغّل أكواد JavaScript مباشرة من المتصفح
            وشاهد النتائج والأخطاء في الوقت الحقيقي.
          </p>

        </div>
      </section>


      <!-- TOOL -->
      <section class="tool-section">
        <div class="container">

          <div class="tool-card">

            <!-- TOOL HEADER -->
            <div class="tool-top">

              <div class="tool-title">
                <div class="tool-title-icon">
                  <i class="fa-brands fa-js"></i>
                </div>

                <div>
                  <h2>JavaScript Playground</h2>
                  <p>محرر JavaScript تفاعلي لتجربة الأكواد بسرعة.</p>
                </div>
              </div>

              <div class="tool-actions">

                <button id="exampleBtn" class="btn btn-secondary">
                  <i class="fa-solid fa-lightbulb"></i>
                  مثال
                </button>

                <button id="clearBtn" class="btn btn-secondary">
                  <i class="fa-solid fa-trash"></i>
                  مسح
                </button>

                <button id="runBtn" class="btn btn-primary">
                  <i class="fa-solid fa-play"></i>
                  تشغيل
                </button>

              </div>

            </div>


            <!-- WORKSPACE -->
            <div class="workspace">

              <!-- EDITOR -->
              <div class="panel editor-panel">

                <div class="panel-header">

                  <div class="panel-name">
                    <span class="status-dot"></span>
                    JavaScript
                  </div>

                  <div class="panel-meta">
                    <span id="lineCount">1 سطر</span>
                    <span id="charCount">0 حرف</span>
                  </div>

                </div>

                <div class="editor-wrapper">

                  <div id="lineNumbers" class="line-numbers">1</div>

                  <textarea
                    id="codeInput"
                    spellcheck="false"
                    autocomplete="off"
                    autocorrect="off"
                    autocapitalize="off"
                    placeholder="// اكتب كود JavaScript هنا..."
                  ></textarea>

                </div>

                <div class="editor-footer">
                  <span>
                    <i class="fa-solid fa-keyboard"></i>
                    Ctrl + Enter للتشغيل
                  </span>

                  <span>
                    <i class="fa-solid fa-code"></i>
                    JavaScript
                  </span>
                </div>

              </div>


              <!-- OUTPUT -->
              <div class="panel output-panel">

                <div class="panel-header">

                  <div class="panel-name">
                    <span class="output-icon">
                      <i class="fa-solid fa-terminal"></i>
                    </span>
                    Console
                  </div>

                  <button id="clearOutputBtn" class="clear-output">
                    <i class="fa-solid fa-broom"></i>
                    مسح النتائج
                  </button>

                </div>

                <div id="output" class="console">
                  <div class="console-empty">
                    <i class="fa-solid fa-play"></i>
                    <p>اضغط على "تشغيل" لعرض النتائج</p>
                  </div>
                </div>

                <div class="output-footer">

                  <div id="status" class="status status-idle">
                    <span class="status-circle"></span>
                    جاهز للتشغيل
                  </div>

                  <div id="executionTime">
                    0 ms
                  </div>

                </div>

              </div>

            </div>


            <!-- STATS -->
            <div class="stats-grid">

              <div class="stat-card">
                <div class="stat-icon blue">
                  <i class="fa-solid fa-bolt"></i>
                </div>

                <div>
                  <strong>فوري</strong>
                  <span>تشغيل مباشر</span>
                </div>
              </div>


              <div class="stat-card">
                <div class="stat-icon cyan">
                  <i class="fa-solid fa-terminal"></i>
                </div>

                <div>
                  <strong>Console</strong>
                  <span>عرض النتائج</span>
                </div>
              </div>


              <div class="stat-card">
                <div class="stat-icon red">
                  <i class="fa-solid fa-bug"></i>
                </div>

                <div>
                  <strong>Errors</strong>
                  <span>كشف الأخطاء</span>
                </div>
              </div>


              <div class="stat-card">
                <div class="stat-icon green">
                  <i class="fa-solid fa-shield-halved"></i>
                </div>

                <div>
                  <strong>محلي</strong>
                  <span>داخل المتصفح</span>
                </div>
              </div>

            </div>

          </div>


          <!-- INFO -->
          <div class="info-grid">

            <div class="info-card">
              <div class="info-icon blue">
                <i class="fa-solid fa-play"></i>
              </div>

              <h3>تشغيل JavaScript</h3>

              <p>
                اكتب أي كود JavaScript ثم اضغط على زر التشغيل
                لمشاهدة النتيجة مباشرة.
              </p>
            </div>


            <div class="info-card">
              <div class="info-icon cyan">
                <i class="fa-solid fa-terminal"></i>
              </div>

              <h3>Console تفاعلي</h3>

              <p>
                استخدم console.log وconsole.warn وconsole.error
                لمتابعة مخرجات الكود.
              </p>
            </div>


            <div class="info-card">
              <div class="info-icon purple">
                <i class="fa-solid fa-bug"></i>
              </div>

              <h3>كشف الأخطاء</h3>

              <p>
                تظهر أخطاء JavaScript داخل نافذة النتائج
                مع تفاصيل الخطأ لتسهيل تصحيحه.
              </p>
            </div>

          </div>


          <!-- HOW TO -->
          <section class="content-section">

            <div class="section-heading">
              <span>طريقة الاستخدام</span>
              <h2>كيف تستخدم JavaScript Playground؟</h2>
            </div>

            <div class="steps-grid">

              <div class="step-card">
                <div class="step-number">1</div>

                <div>
                  <h3>اكتب الكود</h3>
                  <p>
                    اكتب JavaScript في محرر الأكواد الموجود في الصفحة.
                  </p>
                </div>
              </div>


              <div class="step-card">
                <div class="step-number">2</div>

                <div>
                  <h3>شغّل الكود</h3>
                  <p>
                    اضغط على تشغيل أو استخدم الاختصار Ctrl + Enter.
                  </p>
                </div>
              </div>


              <div class="step-card">
                <div class="step-number">3</div>

                <div>
                  <h3>شاهد النتيجة</h3>
                  <p>
                    ستظهر المخرجات والأخطاء داخل Console مباشرة.
                  </p>
                </div>
              </div>

            </div>

          </section>


          <!-- FAQ -->
          <section class="content-section faq-section">

            <div class="section-heading">
              <span>الأسئلة الشائعة</span>
              <h2>أسئلة حول JavaScript Playground</h2>
            </div>

            <div class="faq-list">

              {
                faqs.map((faq) => (
                  <details>
                    <summary>
                      <span>{faq.q}</span>
                      <i class="fa-solid fa-chevron-down"></i>
                    </summary>

                    <div class="faq-answer">
                      {faq.a}
                    </div>
                  </details>
                ))
              }

            </div>

          </section>

        </div>
      </section>

    </main>

    <Footer />


    <!-- JAVASCRIPT -->
    <script is:inline>
      (() => {

        const codeInput = document.getElementById('codeInput');
        const output = document.getElementById('output');

        const runBtn = document.getElementById('runBtn');
        const clearBtn = document.getElementById('clearBtn');
        const exampleBtn = document.getElementById('exampleBtn');
        const clearOutputBtn = document.getElementById('clearOutputBtn');

        const lineNumbers = document.getElementById('lineNumbers');
        const lineCount = document.getElementById('lineCount');
        const charCount = document.getElementById('charCount');

        const status = document.getElementById('status');
        const executionTime = document.getElementById('executionTime');

        let activeWorker = null;
        let executionTimer = null;
        let startedAt = 0;


        /* =========================================
           EXAMPLE
        ========================================= */

        const exampleCode = `const name = "Th.Tech";

console.log("مرحباً بك في " + name);

const numbers = [10, 20, 30, 40, 50];

const total = numbers.reduce(
  (sum, number) => sum + number,
  0
);

console.log("المجموع:", total);

const doubled = numbers.map(number => number * 2);

console.log("الأرقام بعد الضرب:", doubled);

total;`;


        /* =========================================
           UPDATE EDITOR STATS
        ========================================= */

        function updateStats() {

          const code = codeInput.value;

          const lines = code.length
            ? code.split('\n').length
            : 1;

          lineCount.textContent =
            `${lines} ${lines === 1 ? 'سطر' : 'أسطر'}`;

          charCount.textContent =
            `${code.length} حرف`;

          const numbers = [];

          for (let i = 1; i <= lines; i++) {
            numbers.push(i);
          }

          lineNumbers.textContent =
            numbers.join('\n');
        }


        /* =========================================
           STATUS
        ========================================= */

        function setStatus(type, text) {

          status.className = `status status-${type}`;

          status.innerHTML = `
            <span class="status-circle"></span>
            ${text}
          `;
        }


        /* =========================================
           ESCAPE HTML
        ========================================= */

        function escapeHtml(value) {

          return String(value)
            .replace(/&/g, '&amp;')
            .replace(/</g, '&lt;')
            .replace(/>/g, '&gt;')
            .replace(/"/g, '&quot;')
            .replace(/'/g, '&#039;');
        }


        /* =========================================
           FORMAT VALUE
        ========================================= */

        function formatValue(value) {

          if (value === undefined) {
            return 'undefined';
          }

          if (value === null) {
            return 'null';
          }

          if (typeof value === 'string') {
            return value;
          }

          if (typeof value === 'function') {
            return value.toString();
          }

          if (typeof value === 'object') {

            try {

              return JSON.stringify(
                value,
                null,
                2
              );

            } catch {

              return String(value);

            }

          }

          return String(value);
        }


        /* =========================================
           ADD OUTPUT
        ========================================= */

        function addOutput(type, values) {

          const empty =
            output.querySelector('.console-empty');

          if (empty) {
            empty.remove();
          }

          const row =
            document.createElement('div');

          row.className =
            `console-row console-${type}`;

          const iconMap = {
            log: 'fa-circle-info',
            info: 'fa-circle-info',
            warn: 'fa-triangle-exclamation',
            error: 'fa-circle-xmark',
            result: 'fa-arrow-right'
          };

          const icon =
            iconMap[type] || 'fa-circle-info';

          const content =
            values
              .map(value => escapeHtml(value))
              .join(' ');

          row.innerHTML = `
            <div class="console-type">
              <i class="fa-solid ${icon}"></i>
            </div>

            <pre>${content}</pre>
          `;

          output.appendChild(row);

          output.scrollTop =
            output.scrollHeight;
        }


        /* =========================================
           RESET OUTPUT
        ========================================= */

        function resetOutput() {

          output.innerHTML = `
            <div class="console-empty">
              <i class="fa-solid fa-play"></i>
              <p>اضغط على "تشغيل" لعرض النتائج</p>
            </div>
          `;

          executionTime.textContent = '0 ms';

        }


        /* =========================================
           STOP WORKER
        ========================================= */

        function stopWorker() {

          if (activeWorker) {

            try {
              activeWorker.terminate();
            } catch {}

            activeWorker = null;
          }

          if (executionTimer) {

            clearTimeout(executionTimer);

            executionTimer = null;
          }

        }


        /* =========================================
           FINISH EXECUTION
        ========================================= */

        function finishExecution(error = false) {

          if (executionTimer) {

            clearTimeout(executionTimer);

            executionTimer = null;
          }

          const elapsed =
            Math.max(
              0,
              performance.now() - startedAt
            );

          executionTime.textContent =
            `${elapsed.toFixed(1)} ms`;

          if (error) {
            setStatus('error', 'حدث خطأ أثناء التنفيذ');
          } else {
            setStatus('success', 'تم التنفيذ بنجاح');
          }

          runBtn.disabled = false;

          runBtn.innerHTML = `
            <i class="fa-solid fa-play"></i>
            تشغيل
          `;

          activeWorker = null;
        }


        /* =========================================
           RUN CODE
        ========================================= */

        function runCode() {

          const code =
            codeInput.value.trim();

          if (!code) {

            resetOutput();

            addOutput(
              'warn',
              ['اكتب كود JavaScript أولاً.']
            );

            setStatus(
              'warning',
              'لا يوجد كود للتشغيل'
            );

            return;
          }


          /* Stop previous execution */

          stopWorker();


          /* Reset */

          resetOutput();

          setStatus(
            'running',
            'جاري تنفيذ الكود...'
          );


          runBtn.disabled = false;

          runBtn.innerHTML = `
            <i class="fa-solid fa-stop"></i>
            إيقاف
          `;


          startedAt =
            performance.now();


          /*
           * Web Worker source.
           *
           * String.raw is intentionally used here so
           * escape sequences such as \n remain valid
           * inside the generated worker script.
           */

          const workerSource = String.raw`

            self.onmessage = async function(event) {

              const code = event.data.code;


              function normalize(value) {

                if (value === undefined) {
                  return 'undefined';
                }

                if (value === null) {
                  return 'null';
                }

                if (typeof value === 'string') {
                  return value;
                }

                if (typeof value === 'function') {
                  return value.toString();
                }

                if (typeof value === 'object') {

                  try {

                    return JSON.stringify(
                      value,
                      null,
                      2
                    );

                  } catch {

                    return String(value);

                  }

                }

                return String(value);
              }


              function send(type, values) {

                self.postMessage({
                  type,
                  values
                });

              }


              console.log = function() {

                send(
                  'log',
                  Array.from(arguments).map(normalize)
                );

              };


              console.info = function() {

                send(
                  'info',
                  Array.from(arguments).map(normalize)
                );

              };


              console.warn = function() {

                send(
                  'warn',
                  Array.from(arguments).map(normalize)
                );

              };


              console.error = function() {

                send(
                  'error',
                  Array.from(arguments).map(normalize)
                );

              };


              try {

                /*
                 * The important part:
                 *
                 * \n is a real JavaScript escape sequence
                 * inside the generated Function source.
                 */

                const fn =
                  new Function(
                    '"use strict";\n' + code
                  );


                const result =
                  await fn();


                if (typeof result !== 'undefined') {

                  send(
                    'result',
                    [normalize(result)]
                  );

                }


                self.postMessage({
                  type: 'done',
                  error: false
                });


              } catch (error) {

                send(
                  'error',
                  [
                    error && error.stack
                      ? error.stack
                      : String(error)
                  ]
                );


                self.postMessage({
                  type: 'done',
                  error: true
                });

              }

            };

          `;


          let workerUrl = null;


          try {

            const blob =
              new Blob(
                [workerSource],
                {
                  type: 'application/javascript'
                }
              );


            workerUrl =
              URL.createObjectURL(blob);


            activeWorker =
              new Worker(workerUrl);


            activeWorker.onmessage =
              function(event) {

                const data =
                  event.data;


                if (
                  data.type === 'log' ||
                  data.type === 'info' ||
                  data.type === 'warn' ||
                  data.type === 'error' ||
                  data.type === 'result'
                ) {

                  addOutput(
                    data.type,
                    data.values || []
                  );

                }


                if (data.type === 'done') {

                  const hasError =
                    Boolean(data.error);

                  finishExecution(
                    hasError
                  );

                  if (activeWorker) {

                    try {
                      activeWorker.terminate();
                    } catch {}

                  }

                  activeWorker = null;

                  if (workerUrl) {

                    URL.revokeObjectURL(
                      workerUrl
                    );

                    workerUrl = null;
                  }

                }

              };


            activeWorker.onerror =
              function(event) {

                addOutput(
                  'error',
                  [
                    event.message ||
                    'حدث خطأ أثناء تنفيذ JavaScript.'
                  ]
                );

                finishExecution(true);

                if (activeWorker) {

                  try {
                    activeWorker.terminate();
                  } catch {}

                }

                activeWorker = null;

                if (workerUrl) {

                  URL.revokeObjectURL(
                    workerUrl
                  );

                  workerUrl = null;
                }

              };


            /*
             * Maximum execution time:
             * 5 seconds.
             */

            executionTimer =
              setTimeout(
                function() {

                  if (!activeWorker) {
                    return;
                  }


                  activeWorker.terminate();

                  activeWorker = null;


                  if (workerUrl) {

                    URL.revokeObjectURL(
                      workerUrl
                    );

                    workerUrl = null;
                  }


                  addOutput(
                    'error',
                    [
                      'تم إيقاف التنفيذ لأن الكود تجاوز الحد الأقصى للوقت (5 ثوانٍ).'
                    ]
                  );


                  executionTime.textContent =
                    'Timeout';


                  setStatus(
                    'error',
                    'تم إيقاف التنفيذ'
                  );


                  runBtn.disabled = false;

                  runBtn.innerHTML = `
                    <i class="fa-solid fa-play"></i>
                    تشغيل
                  `;

                  executionTimer = null;

                },
                5000
              );


            activeWorker.postMessage({
              code
            });


          } catch (error) {

            addOutput(
              'error',
              [
                error && error.message
                  ? error.message
                  : String(error)
              ]
            );

            finishExecution(true);

          }

        }


        /* =========================================
           RUN / STOP BUTTON
        ========================================= */

        runBtn.addEventListener(
          'click',
          function() {

            if (activeWorker) {

              stopWorker();

              setStatus(
                'warning',
                'تم إيقاف التنفيذ'
              );

              executionTime.textContent =
                `${(
                  performance.now() - startedAt
                ).toFixed(1)} ms`;


              runBtn.disabled = false;

              runBtn.innerHTML = `
                <i class="fa-solid fa-play"></i>
                تشغيل
              `;

              return;
            }


            runCode();

          }
        );


        /* =========================================
           EXAMPLE
        ========================================= */

        exampleBtn.addEventListener(
          'click',
          function() {

            stopWorker();

            codeInput.value =
              exampleCode;

            updateStats();

            resetOutput();

            setStatus(
              'idle',
              'تم تحميل المثال'
            );

            codeInput.focus();

          }
        );


        /* =========================================
           CLEAR CODE
        ========================================= */

        clearBtn.addEventListener(
          'click',
          function() {

            stopWorker();

            codeInput.value = '';

            updateStats();

            resetOutput();

            setStatus(
              'idle',
              'تم مسح الكود'
            );

            runBtn.disabled = false;

            runBtn.innerHTML = `
              <i class="fa-solid fa-play"></i>
              تشغيل
            `;

            codeInput.focus();

          }
        );


        /* =========================================
           CLEAR OUTPUT
        ========================================= */

        clearOutputBtn.addEventListener(
          'click',
          function() {

            resetOutput();

            setStatus(
              'idle',
              'جاهز للتشغيل'
            );

          }
        );


        /* =========================================
           INPUT
        ========================================= */

        codeInput.addEventListener(
          'input',
          updateStats
        );


        /* =========================================
           SCROLL LINE NUMBERS
        ========================================= */

        codeInput.addEventListener(
          'scroll',
          function() {

            lineNumbers.scrollTop =
              codeInput.scrollTop;

          }
        );


        /* =========================================
           TAB SUPPORT
        ========================================= */

        codeInput.addEventListener(
          'keydown',
          function(event) {

            if (event.key === 'Tab') {

              event.preventDefault();

              const start =
                codeInput.selectionStart;

              const end =
                codeInput.selectionEnd;

              codeInput.value =
                codeInput.value.substring(
                  0,
                  start
                ) +
                '  ' +
                codeInput.value.substring(
                  end
                );

              codeInput.selectionStart =
                codeInput.selectionEnd =
                  start + 2;

              updateStats();

            }


            /* Ctrl + Enter / Cmd + Enter */

            if (
              event.key === 'Enter' &&
              (event.ctrlKey || event.metaKey)
            ) {

              event.preventDefault();

              if (!activeWorker) {
                runCode();
              }

            }

          }
        );


        /* =========================================
           INITIAL STATE
        ========================================= */

        updateStats();

      })();
    </script>


    <!-- STYLE -->
    <style>

      :root {
        --bg: #f8fafc;
        --bg2: #f1f5f9;
        --card: #ffffff;
        --card2: #f8fafc;

        --text: #0f172a;
        --muted: #64748b;

        --primary: #2563eb;
        --primary-hover: #1d4ed8;

        --cyan: #0891b2;
        --green: #16a34a;
        --red: #dc2626;
        --yellow: #d97706;
        --purple: #7c3aed;

        --border: rgba(15, 23, 42, 0.09);

        --shadow:
          0 20px 60px rgba(15, 23, 42, 0.08);
      }


      html.dark-mode {

        --bg: #07101f;
        --bg2: #0b1426;

        --card: #111d32;
        --card2: #0d1729;

        --text: #f8fafc;
        --muted: #94a3b8;

        --primary: #3b82f6;
        --primary-hover: #2563eb;

        --cyan: #06b6d4;
        --green: #22c55e;
        --red: #ef4444;
        --yellow: #f59e0b;
        --purple: #a78bfa;

        --border:
          rgba(148, 163, 184, 0.12);

        --shadow:
          0 20px 60px rgba(0, 0, 0, 0.28);
      }


      * {
        box-sizing: border-box;
      }


      html {
        scroll-behavior: smooth;
      }


      body {
        margin: 0;

        background:
          radial-gradient(
            circle at 50% -20%,
            rgba(37, 99, 235, 0.08),
            transparent 35%
          ),
          var(--bg);

        color: var(--text);

        font-family:
          "Cairo",
          sans-serif;

        transition:
          background 0.25s ease,
          color 0.25s ease;
      }


      button,
      textarea {
        font-family: inherit;
      }


      .container {
        width: min(
          1120px,
          calc(100% - 32px)
        );

        margin-inline: auto;
      }


      /* =========================================
         HERO
      ========================================= */

      .hero {
        padding:
          36px
          0
          30px;

        text-align: center;
      }


      .breadcrumb {
        display: flex;

        justify-content: center;

        align-items: center;

        gap: 8px;

        flex-wrap: wrap;

        color: var(--muted);

        font-size: 13px;

        margin-bottom: 22px;
      }


      .breadcrumb a {
        color: var(--muted);

        text-decoration: none;

        transition: color 0.2s ease;
      }


      .breadcrumb a:hover {
        color: var(--primary);
      }


      .hero-icon {
        width: 68px;
        height: 68px;

        margin: 0 auto 16px;

        display: grid;

        place-items: center;

        border-radius: 20px;

        background:
          linear-gradient(
            135deg,
            rgba(37, 99, 235, 0.12),
            rgba(8, 145, 178, 0.12)
          );

        color: var(--primary);

        font-size: 32px;

        border:
          1px solid
          rgba(37, 99, 235, 0.12);
      }


      .hero-kicker {
        display: inline-flex;

        align-items: center;

        gap: 7px;

        color: var(--primary);

        font-size: 13px;

        font-weight: 700;

        margin-bottom: 8px;
      }


      .hero h1 {
        margin: 0;

        font-size:
          clamp(30px, 5vw, 46px);

        line-height: 1.2;

        font-weight: 800;
      }


      .hero h1 span {
        color: var(--primary);
      }


      .hero p {
        max-width: 720px;

        margin:
          14px auto 0;

        color: var(--muted);

        font-size: 15px;

        line-height: 1.9;
      }


      /* =========================================
         TOOL
      ========================================= */

      .tool-section {
        padding-bottom: 40px;
      }


      .tool-card {
        background: var(--card);

        border:
          1px solid
          var(--border);

        border-radius: 22px;

        box-shadow: var(--shadow);

        overflow: hidden;
      }


      .tool-top {
        display: flex;

        justify-content: space-between;

        align-items: center;

        gap: 20px;

        padding: 20px;

        border-bottom:
          1px solid
          var(--border);
      }


      .tool-title {
        display: flex;

        align-items: center;

        gap: 13px;
      }


      .tool-title-icon {
        width: 48px;
        height: 48px;

        flex: 0 0 48px;

        display: grid;

        place-items: center;

        border-radius: 14px;

        background:
          rgba(37, 99, 235, 0.1);

        color: var(--primary);

        font-size: 22px;
      }


      .tool-title h2 {
        margin: 0 0 2px;

        font-size: 17px;
      }


      .tool-title p {
        margin: 0;

        color: var(--muted);

        font-size: 12px;
      }


      .tool-actions {
        display: flex;

        gap: 8px;

        flex-wrap: wrap;
      }


      .btn {
        border: 0;

        border-radius: 11px;

        padding:
          10px
          14px;

        display: inline-flex;

        align-items: center;

        justify-content: center;

        gap: 7px;

        cursor: pointer;

        font-size: 13px;

        font-weight: 700;

        transition:
          transform 0.2s ease,
          background 0.2s ease,
          opacity 0.2s ease;
      }


      .btn:hover {
        transform: translateY(-1px);
      }


      .btn-primary {
        background: var(--primary);

        color: white;
      }


      .btn-primary:hover {
        background: var(--primary-hover);
      }


      .btn-secondary {
        background: var(--bg2);

        color: var(--text);

        border:
          1px solid
          var(--border);
      }


      .btn-secondary:hover {
        background:
          rgba(37, 99, 235, 0.08);

        color: var(--primary);
      }


      /* =========================================
         WORKSPACE
      ========================================= */

      .workspace {
        display: grid;

        grid-template-columns:
          minmax(0, 1fr)
          minmax(0, 1fr);

        gap: 16px;

        padding: 18px;
      }


      .panel {
        min-width: 0;

        overflow: hidden;

        border-radius: 16px;

        border:
          1px solid
          var(--border);

        background: #0b1220;

        color: #e5e7eb;
      }


      .panel-header {
        min-height: 48px;

        display: flex;

        align-items: center;

        justify-content: space-between;

        gap: 10px;

        padding:
          0 14px;

        border-bottom:
          1px solid
          rgba(255,255,255,0.08);
      }


      .panel-name {
        display: flex;

        align-items: center;

        gap: 8px;

        font-size: 13px;

        font-weight: 700;
      }


      .status-dot {
        width: 8px;
        height: 8px;

        border-radius: 50%;

        background: #f7df1e;

        box-shadow:
          0 0 0 4px
          rgba(247,223,30,0.08);
      }


      .output-icon {
        color: #67e8f9;
      }


      .panel-meta {
        display: flex;

        gap: 12px;

        color: #94a3b8;

        font-size: 11px;
      }


      .editor-wrapper {
        display: flex;

        height: 410px;

        direction: ltr;

        overflow: hidden;
      }


      .line-numbers {
        width: 48px;

        flex: 0 0 48px;

        padding:
          17px 10px;

        text-align: right;

        color: #64748b;

        background: #08101d;

        border-right:
          1px solid
          rgba(255,255,255,0.06);

        font:
          13px/1.75
          monospace;

        overflow: hidden;

        user-select: none;

        white-space: pre;
      }


      #codeInput {
        width: 100%;

        height: 100%;

        resize: none;

        border: 0;

        outline: 0;

        padding: 17px;

        background: #0b1220;

        color: #e2e8f0;

        font:
          13px/1.75
          "Cairo",
          monospace;

        direction: ltr;

        text-align: left;

        tab-size: 2;
      }


      #codeInput::placeholder {
        color: #64748b;
      }


      .editor-footer {
        min-height: 40px;

        display: flex;

        align-items: center;

        justify-content: space-between;

        padding:
          0 14px;

        border-top:
          1px solid
          rgba(255,255,255,0.08);

        color: #64748b;

        font-size: 10px;
      }


      .editor-footer span {
        display: flex;

        align-items: center;

        gap: 5px;
      }


      /* =========================================
         CONSOLE
      ========================================= */

      .console {
        height: 410px;

        padding: 12px;

        overflow: auto;

        direction: ltr;

        text-align: left;
      }


      .console-empty {
        min-height: 100%;

        display: flex;

        flex-direction: column;

        align-items: center;

        justify-content: center;

        gap: 10px;

        color: #64748b;

        text-align: center;

        direction: rtl;
      }


      .console-empty i {
        font-size: 30px;

        opacity: 0.45;
      }


      .console-empty p {
        margin: 0;

        font-size: 12px;
      }


      .console-row {
        display: grid;

        grid-template-columns: 24px minmax(0, 1fr);

        gap: 7px;

        padding: 8px 6px;

        border-bottom:
          1px solid
          rgba(255,255,255,0.05);

        font:
          12px/1.65
          monospace;

        direction: ltr;
      }


      .console-row pre {
        margin: 0;

        white-space: pre-wrap;

        word-break: break-word;
      }


      .console-type {
        padding-top: 2px;
      }


      .console-log {
        color: #e2e8f0;
      }


      .console-info {
        color: #67e8f9;
      }


      .console-warn {
        color: #fbbf24;
      }


      .console-error {
        color: #f87171;
      }


      .console-result {
        color: #86efac;
      }


      .clear-output {
        border: 0;

        background: transparent;

        color: #64748b;

        cursor: pointer;

        font:
          11px
          "Cairo",
          sans-serif;

        display: flex;

        align-items: center;

        gap: 5px;
      }


      .clear-output:hover {
        color: #f87171;
      }


      .output-footer {
        min-height: 40px;

        display: flex;

        align-items: center;

        justify-content: space-between;

        padding:
          0 14px;

        border-top:
          1px solid
          rgba(255,255,255,0.08);

        color: #64748b;

        font-size: 10px;
      }


      .status {
        display: flex;

        align-items: center;

        gap: 7px;
      }


      .status-circle {
        width: 7px;
        height: 7px;

        border-radius: 50%;

        background: #64748b;
      }


      .status-success {
        color: #4ade80;
      }


      .status-success .status-circle {
        background: #22c55e;
      }


      .status-error {
        color: #f87171;
      }


      .status-error .status-circle {
        background: #ef4444;
      }


      .status-warning {
        color: #fbbf24;
      }


      .status-warning .status-circle {
        background: #f59e0b;
      }


      .status-running {
        color: #60a5fa;
      }


      .status-running .status-circle {
        background: #3b82f6;

        animation:
          pulse 1s infinite;
      }


      @keyframes pulse {

        0%,
        100% {
          opacity: 1;
        }

        50% {
          opacity: 0.35;
        }

      }


      /* =========================================
         STATS
      ========================================= */

      .stats-grid {
        display: grid;

        grid-template-columns:
          repeat(4, 1fr);

        gap: 12px;

        padding:
          0 18px
          18px;
      }


      .stat-card {
        display: flex;

        align-items: center;

        gap: 11px;

        padding: 13px;

        border:
          1px solid
          var(--border);

        background: var(--card2);

        border-radius: 14px;
      }


      .stat-icon {
        width: 38px;
        height: 38px;

        flex: 0 0 38px;

        display: grid;

        place-items: center;

        border-radius: 11px;
      }


      .stat-icon.blue {
        background:
          rgba(37,99,235,.1);

        color: var(--primary);
      }


      .stat-icon.cyan {
        background:
          rgba(8,145,178,.1);

        color: var(--cyan);
      }


      .stat-icon.red {
        background:
          rgba(220,38,38,.1);

        color: var(--red);
      }


      .stat-icon.green {
        background:
          rgba(22,163,74,.1);

        color: var(--green);
      }


      .stat-card strong {
        display: block;

        font-size: 12px;
      }


      .stat-card span {
        display: block;

        margin-top: 2px;

        color: var(--muted);

        font-size: 10px;
      }


      /* =========================================
         INFO
      ========================================= */

      .info-grid {
        display: grid;

        grid-template-columns:
          repeat(3, 1fr);

        gap: 14px;

        margin-top: 18px;
      }


      .info-card {
        padding: 22px;

        border:
          1px solid
          var(--border);

        background: var(--card);

        border-radius: 18px;
      }


      .info-icon {
        width: 42px;
        height: 42px;

        display: grid;

        place-items: center;

        border-radius: 12px;

        margin-bottom: 13px;
      }


      .info-icon.blue {
        background:
          rgba(37,99,235,.1);

        color: var(--primary);
      }


      .info-icon.cyan {
        background:
          rgba(8,145,178,.1);

        color: var(--cyan);
      }


      .info-icon.purple {
        background:
          rgba(124,58,237,.1);

        color: var(--purple);
      }


      .info-card h3 {
        margin:
          0 0 8px;

        font-size: 15px;
      }


      .info-card p {
        margin: 0;

        color: var(--muted);

        font-size: 12px;

        line-height: 1.9;
      }


      /* =========================================
         CONTENT
      ========================================= */

      .content-section {
        margin-top: 46px;
      }


      .section-heading {
        text-align: center;

        margin-bottom: 22px;
      }


      .section-heading span {
        display: inline-block;

        margin-bottom: 5px;

        color: var(--primary);

        font-size: 12px;

        font-weight: 700;
      }


      .section-heading h2 {
        margin: 0;

        font-size:
          clamp(21px, 4vw, 28px);
      }


      .steps-grid {
        display: grid;

        grid-template-columns:
          repeat(3, 1fr);

        gap: 14px;
      }


      .step-card {
        display: flex;

        align-items: flex-start;

        gap: 13px;

        padding: 20px;

        background: var(--card);

        border:
          1px solid
          var(--border);

        border-radius: 17px;
      }


      .step-number {
        width: 38px;
        height: 38px;

        flex: 0 0 38px;

        display: grid;

        place-items: center;

        border-radius: 11px;

        background:
          rgba(37,99,235,.1);

        color: var(--primary);

        font-weight: 800;
      }


      .step-card h3 {
        margin:
          0 0 5px;

        font-size: 14px;
      }


      .step-card p {
        margin: 0;

        color: var(--muted);

        font-size: 11px;

        line-height: 1.8;
      }


      /* =========================================
         FAQ
      ========================================= */

      .faq-list {
        max-width: 850px;

        margin: 0 auto;
      }


      .faq-list details {
        background: var(--card);

        border:
          1px solid
          var(--border);

        border-radius: 14px;

        margin-bottom: 9px;

        overflow: hidden;
      }


      .faq-list summary {
        list-style: none;

        cursor: pointer;

        display: flex;

        align-items: center;

        justify-content: space-between;

        gap: 15px;

        padding: 17px 18px;

        font-size: 13px;

        font-weight: 700;
      }


      .faq-list summary::-webkit-details-marker {
        display: none;
      }


      .faq-list summary i {
        color: var(--muted);

        transition:
          transform 0.2s ease;
      }


      .faq-list details[open] summary i {
        transform:
          rotate(180deg);
      }


      .faq-answer {
        padding:
          0 18px
          18px;

        color: var(--muted);

        font-size: 12px;

        line-height: 1.9;
      }


      /* =========================================
         RESPONSIVE
      ========================================= */

      @media (max-width: 900px) {

        .tool-top {
          align-items: flex-start;

          flex-direction: column;
        }


        .tool-actions {
          width: 100%;
        }


        .tool-actions .btn {
          flex: 1;
        }


        .workspace {
          grid-template-columns: 1fr;
        }


        .info-grid,
        .steps-grid {
          grid-template-columns:
            repeat(2, 1fr);
        }

      }


      @media (max-width: 800px) {

        .stats-grid {
          grid-template-columns:
            repeat(2, 1fr);
        }

      }


      @media (max-width: 600px) {

        .container {
          width:
            calc(100% - 20px);
        }


        .hero {
          padding-top: 25px;
        }


        .hero-icon {
          width: 58px;
          height: 58px;

          font-size: 27px;
        }


        .hero p {
          font-size: 13px;
        }


        .tool-card {
          border-radius: 17px;
        }


        .tool-top {
          padding: 15px;
        }


        .tool-title-icon {
          width: 42px;
          height: 42px;

          flex-basis: 42px;

          font-size: 19px;
        }


        .tool-title h2 {
          font-size: 15px;
        }


        .tool-title p {
          font-size: 10px;
        }


        .tool-actions {
          display: grid;

          grid-template-columns:
            repeat(3, 1fr);
        }


        .tool-actions .btn {
          padding:
            9px 7px;

          font-size: 11px;
        }


        .workspace {
          padding: 10px;

          gap: 10px;
        }


        .editor-wrapper,
        .console {
          height: 340px;
        }


        .stats-grid {
          padding:
            0 10px
            10px;

          gap: 8px;
        }


        .stat-card {
          padding: 10px;

          gap: 8px;
        }


        .stat-icon {
          width: 34px;
          height: 34px;

          flex-basis: 34px;
        }


        .stat-card strong {
          font-size: 11px;
        }


        .info-grid,
        .steps-grid {
          grid-template-columns: 1fr;
        }


        .info-card {
          padding: 18px;
        }


        .content-section {
          margin-top: 35px;
        }


        .section-heading h2 {
          font-size: 21px;
        }


        .breadcrumb {
          font-size: 11px;
        }

      }

    </style>

  </body>
</html>
