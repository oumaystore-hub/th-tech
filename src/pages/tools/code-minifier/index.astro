---
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const title =
  "Code Minifier - تصغير وضغط الأكواد أونلاين مجاناً | Th.Tech";

const description =
  "أداة Code Minifier مجانية لتصغير وضغط الأكواد وإزالة المسافات والتعليقات غير الضرورية وتحسين حجم الكود مباشرة من المتصفح.";

const faqs = [
  {
    q: "ما هو Code Minifier؟",
    a: "أداة تساعد على تصغير حجم الكود عن طريق إزالة المسافات والأسطر والتعليقات غير الضرورية مع الحفاظ على بنية الكود قدر الإمكان."
  },
  {
    q: "هل يتم إرسال الكود إلى الخادم؟",
    a: "لا. تتم معالجة الكود مباشرة داخل المتصفح، لذلك لا تحتاج إلى رفع الكود إلى خادم خارجي."
  },
  {
    q: "ما فائدة تصغير الكود؟",
    a: "تصغير الكود يقلل حجمه ويمكن أن يساعد في تقليل حجم الملفات وتسريع نقلها وتحسين أداء المواقع والتطبيقات."
  },
  {
    q: "هل يمكنني نسخ الكود بعد تصغيره؟",
    a: "نعم. يمكنك نسخ الكود المصغر مباشرة باستخدام زر النسخ."
  },
  {
    q: "هل الأداة مجانية؟",
    a: "نعم. الأداة مجانية ويمكن استخدامها مباشرة من المتصفح."
  }
];
---

<!doctype html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />

    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />

    <title>{title}</title>

    <meta
      name="description"
      content={description}
    />

    <meta
      name="robots"
      content="index, follow"
    />

    <meta
      name="theme-color"
      content="#16a34a"
    />

    <link
      rel="icon"
      href={`${base}/logo1.png`}
    />

    <link
      rel="preconnect"
      href="https://fonts.googleapis.com"
    />

    <link
      rel="preconnect"
      href="https://fonts.gstatic.com"
      crossorigin
    />

    <link
      href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap"
      rel="stylesheet"
    />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
    />

    <script is:inline>
      (() => {
        try {
          const saved = localStorage.getItem("theme");

          if (saved === "dark") {
            document.documentElement.classList.add("dark-mode");
          } else {
            document.documentElement.classList.remove("dark-mode");
          }
        } catch {}
      })();
    </script>
  </head>

  <body>

    <Header />

    <main>

      <!-- ======================================
           HERO
      ======================================= -->

      <section class="hero">

        <div class="hero-glow glow-one"></div>
        <div class="hero-glow glow-two"></div>

        <div class="container">

          <div class="breadcrumb">

            <a href={`${base}/`}>
              الرئيسية
            </a>

            <i class="fa-solid fa-chevron-left"></i>

            <a href={`${base}/tools/`}>
              الأدوات
            </a>

            <i class="fa-solid fa-chevron-left"></i>

            <span>
              Code Minifier
            </span>

          </div>


          <div class="hero-badge">

            <span class="badge-dot"></span>

            Developer Tools

            <i class="fa-solid fa-compress"></i>

          </div>


          <div class="hero-icon">

            <i class="fa-solid fa-file-code"></i>

            <span class="minify-mark">
              −
            </span>

          </div>


          <h1>

            Code

            <span>Minifier</span>

          </h1>


          <p>

            صغّر حجم الكود بطريقة سريعة واحترافية،
            وأزل المسافات والتعليقات غير الضرورية مباشرة من المتصفح.

          </p>

        </div>

      </section>


      <!-- ======================================
           TOOL
      ======================================= -->

      <section class="tool-section">

        <div class="container">

          <div class="minifier-card">


            <!-- TOP BAR -->

            <div class="topbar">

              <div class="project-info">

                <div class="project-logo">

                  <i class="fa-solid fa-compress"></i>

                </div>

                <div>

                  <strong>
                    Code Minifier
                  </strong>

                  <span>
                    Online Code Compression Tool
                  </span>

                </div>

              </div>


              <div class="topbar-status">

                <span class="online-dot"></span>

                Local Processing

              </div>

            </div>


            <!-- ACTION BAR -->

            <div class="actionbar">

              <div class="action-group">

                <button
                  id="exampleBtn"
                  class="action secondary"
                  type="button"
                >

                  <i class="fa-solid fa-wand-magic-sparkles"></i>

                  مثال

                </button>


                <button
                  id="clearBtn"
                  class="action secondary"
                  type="button"
                >

                  <i class="fa-solid fa-trash-can"></i>

                  مسح

                </button>

              </div>


              <div class="action-group">

                <button
                  id="copyBtn"
                  class="action secondary"
                  type="button"
                >

                  <i class="fa-regular fa-copy"></i>

                  نسخ

                </button>


                <button
                  id="minifyBtn"
                  class="action primary"
                  type="button"
                >

                  <i class="fa-solid fa-compress"></i>

                  تصغير الكود

                </button>

              </div>

            </div>


            <!-- EDITORS -->

            <div class="editor-grid">


              <!-- INPUT -->

              <div class="editor-card">

                <div class="editor-head">

                  <div class="editor-label">

                    <span class="file-icon">

                      <i class="fa-solid fa-file-code"></i>

                    </span>

                    <div>

                      <strong>
                        Input
                      </strong>

                      <small>
                        Original Code
                      </small>

                    </div>

                  </div>


                  <div
                    id="inputStats"
                    class="editor-stats"
                  >
                    0 حرف
                  </div>

                </div>


                <div class="code-area">

                  <div
                    id="inputLines"
                    class="line-numbers"
                  >
                    1
                  </div>


                  <textarea
                    id="inputCode"
                    spellcheck="false"
                    autocomplete="off"
                    autocorrect="off"
                    autocapitalize="off"
                    placeholder="// اكتب الكود هنا..."
                  ></textarea>

                </div>


                <div class="editor-foot">

                  <span>

                    <i class="fa-solid fa-keyboard"></i>

                    Ctrl + Enter

                  </span>

                  <span>
                    Code
                  </span>

                </div>

              </div>


              <!-- OUTPUT -->

              <div class="editor-card">

                <div class="editor-head">

                  <div class="editor-label">

                    <span class="file-icon output-icon">

                      <i class="fa-solid fa-compress"></i>

                    </span>

                    <div>

                      <strong>
                        Output
                      </strong>

                      <small>
                        Minified Code
                      </small>

                    </div>

                  </div>


                  <div
                    id="outputStats"
                    class="editor-stats"
                  >
                    0 حرف
                  </div>

                </div>


                <div class="code-area">

                  <div
                    id="outputLines"
                    class="line-numbers"
                  >
                    1
                  </div>


                  <textarea
                    id="outputCode"
                    readonly
                    spellcheck="false"
                    placeholder="// سيظهر الكود المصغر هنا..."
                  ></textarea>

                </div>


                <div class="editor-foot">

                  <span
                    id="status"
                    class="status idle"
                  >

                    <span class="status-dot"></span>

                    جاهز

                  </span>

                  <span id="outputSize">
                    0 B
                  </span>

                </div>

              </div>

            </div>


            <!-- COMPRESSION RESULT -->

            <div
              id="compressionResult"
              class="compression-result"
            >

              <div class="compression-main">

                <div class="compression-icon">

                  <i class="fa-solid fa-chart-line"></i>

                </div>

                <div>

                  <strong>
                    Compression
                  </strong>

                  <span id="compressionText">
                    قم بتصغير الكود لمعرفة نسبة التوفير
                  </span>

                </div>

              </div>


              <div class="compression-value">

                <span id="savingPercent">
                  0%
                </span>

                <small>
                  توفير
                </small>

              </div>

            </div>


            <!-- TOOL STATS -->

            <div class="tool-stats">

              <div class="mini-stat">

                <div class="mini-icon green">

                  <i class="fa-solid fa-compress"></i>

                </div>

                <div>

                  <strong>
                    Minify
                  </strong>

                  <span>
                    تصغير الكود
                  </span>

                </div>

              </div>


              <div class="mini-stat">

                <div class="mini-icon blue">

                  <i class="fa-solid fa-bolt"></i>

                </div>

                <div>

                  <strong>
                    سريع
                  </strong>

                  <span>
                    معالجة فورية
                  </span>

                </div>

              </div>


              <div class="mini-stat">

                <div class="mini-icon purple">

                  <i class="fa-solid fa-lock"></i>

                </div>

                <div>

                  <strong>
                    Private
                  </strong>

                  <span>
                    داخل المتصفح
                  </span>

                </div>

              </div>


              <div class="mini-stat">

                <div class="mini-icon orange">

                  <i class="fa-solid fa-mobile-screen"></i>

                </div>

                <div>

                  <strong>
                    Responsive
                  </strong>

                  <span>
                    جميع الأجهزة
                  </span>

                </div>

              </div>

            </div>

          </div>


          <!-- ======================================
               INFORMATION
          ======================================= -->

          <section class="information">

            <div class="section-title">

              <span>
                Developer Tools
              </span>

              <h2>
                أداة تصغير الأكواد الاحترافية
              </h2>

              <p>
                قلل حجم الكود وأزل العناصر غير الضرورية
                للحصول على ملفات أصغر وأسهل في النقل.
              </p>

            </div>


            <div class="info-grid">


              <article class="info-card">

                <div class="info-number">
                  01
                </div>

                <div class="info-card-icon">

                  <i class="fa-solid fa-compress"></i>

                </div>

                <h3>
                  تصغير الكود
                </h3>

                <p>
                  إزالة المسافات والأسطر الزائدة
                  والعناصر غير الضرورية من الكود.
                </p>

              </article>


              <article class="info-card">

                <div class="info-number">
                  02
                </div>

                <div class="info-card-icon">

                  <i class="fa-solid fa-gauge-high"></i>

                </div>

                <h3>
                  حجم أصغر
                </h3>

                <p>
                  تقليل حجم النص يساعد على نقل الملفات
                  بشكل أسرع في العديد من الحالات.
                </p>

              </article>


              <article class="info-card">

                <div class="info-number">
                  03
                </div>

                <div class="info-card-icon">

                  <i class="fa-solid fa-shield-halved"></i>

                </div>

                <h3>
                  معالجة محلية
                </h3>

                <p>
                  تتم العملية داخل المتصفح دون الحاجة
                  إلى إرسال الكود إلى خادم خارجي.
                </p>

              </article>

            </div>

          </section>


          <!-- ======================================
               HOW TO
          ======================================= -->

          <section class="how-section">

            <div class="section-title">

              <span>
                How It Works
              </span>

              <h2>
                طريقة استخدام الأداة
              </h2>

            </div>


            <div class="steps">


              <div class="step">

                <div class="step-icon">

                  <span>
                    01
                  </span>

                </div>

                <div>

                  <h3>
                    ألصق الكود
                  </h3>

                  <p>
                    ضع الكود الذي تريد تصغيره داخل مربع Input.
                  </p>

                </div>

              </div>


              <div class="step">

                <div class="step-icon">

                  <span>
                    02
                  </span>

                </div>

                <div>

                  <h3>
                    اضغط تصغير
                  </h3>

                  <p>
                    اضغط على زر "تصغير الكود".
                  </p>

                </div>

              </div>


              <div class="step">

                <div class="step-icon">

                  <span>
                    03
                  </span>

                </div>

                <div>

                  <h3>
                    انسخ النتيجة
                  </h3>

                  <p>
                    انسخ الكود المصغر واستخدمه في مشروعك.
                  </p>

                </div>

              </div>

            </div>

          </section>


          <!-- ======================================
               FAQ
          ======================================= -->

          <section class="faq-section">

            <div class="section-title">

              <span>
                FAQ
              </span>

              <h2>
                الأسئلة الشائعة
              </h2>

            </div>


            <div class="faq-list">

              {
                faqs.map((faq) => (
                  <details>

                    <summary>

                      <span>
                        {faq.q}
                      </span>

                      <i class="fa-solid fa-chevron-down"></i>

                    </summary>

                    <div class="faq-answer">
                      {faq.a}
                    </div>

                  </details>
                ))
              }

            </div>

          </section>

        </div>

      </section>

    </main>


    <Footer />


    <!-- ======================================
         JAVASCRIPT
    ======================================= -->

    <script is:inline>
      (() => {

        const input =
          document.getElementById("inputCode");

        const output =
          document.getElementById("outputCode");

        const inputLines =
          document.getElementById("inputLines");

        const outputLines =
          document.getElementById("outputLines");

        const inputStats =
          document.getElementById("inputStats");

        const outputStats =
          document.getElementById("outputStats");

        const outputSize =
          document.getElementById("outputSize");

        const status =
          document.getElementById("status");

        const minifyBtn =
          document.getElementById("minifyBtn");

        const copyBtn =
          document.getElementById("copyBtn");

        const clearBtn =
          document.getElementById("clearBtn");

        const exampleBtn =
          document.getElementById("exampleBtn");

        const savingPercent =
          document.getElementById("savingPercent");

        const compressionText =
          document.getElementById("compressionText");


        /* ======================================
           EXAMPLE
        ======================================= */

        const exampleCode = `// User data
const users = [
  {
    id: 1,
    name: "Ahmed",
    role: "admin"
  },
  {
    id: 2,
    name: "Sara",
    role: "user"
  }
];

// Find admin users
function getAdmins(list) {
  return list.filter(function(user) {
    return user.role === "admin";
  });
}

const admins = getAdmins(users);

console.log("Admins:", admins);`;


        /* ======================================
           STATS
        ======================================= */

        function updateEditorStats() {

          const inputValue =
            input.value;

          const outputValue =
            output.value;


          const inputLineCount =
            inputValue
              ? inputValue.split("\n").length
              : 1;


          const outputLineCount =
            outputValue
              ? outputValue.split("\n").length
              : 1;


          inputLines.textContent =
            Array.from(
              { length: inputLineCount },
              (_, i) => i + 1
            ).join("\n");


          outputLines.textContent =
            Array.from(
              { length: outputLineCount },
              (_, i) => i + 1
            ).join("\n");


          inputStats.textContent =
            `${inputValue.length} حرف`;


          outputStats.textContent =
            `${outputValue.length} حرف`;


          const bytes =
            new Blob([outputValue]).size;


          if (bytes < 1024) {

            outputSize.textContent =
              `${bytes} B`;

          } else {

            outputSize.textContent =
              `${(bytes / 1024).toFixed(1)} KB`;

          }

        }


        /* ======================================
           STATUS
        ======================================= */

        function setStatus(type, text) {

          status.className =
            `status ${type}`;

          status.innerHTML = `
            <span class="status-dot"></span>
            ${text}
          `;

        }


        /* ======================================
           MINIFIER
        ======================================= */

        function minifyCode(code) {

          let result =
            code
              .replace(/\r\n/g, "\n")
              .replace(/\r/g, "\n");


          const placeholders = [];


          /*
           * Protect strings, template literals
           * and comments temporarily.
           */

          result =
            result.replace(
              /(["'`])(?:\\[\s\S]|(?!\1)[\s\S])*?\1|\/\/[^\n]*|\/\*[\s\S]*?\*\//g,
              (match) => {

                const index =
                  placeholders.length;

                placeholders.push(match);

                return `___CODE_MIN_${index}___`;

              }
            );


          /*
           * Remove unnecessary whitespace.
           */

          result =
            result
              .replace(/\s+/g, " ")
              .replace(/\s*([{}[\]();,:])\s*/g, "$1")
              .replace(/\s*([=+\-*\/%<>])\s*/g, "$1")
              .replace(/\s*&&\s*/g, "&&")
              .replace(/\s*\|\|\s*/g, "||")
              .replace(/\s*===\s*/g, "===")
              .replace(/\s*!==\s*/g, "!==")
              .replace(/\s*=>\s*/g, "=>")
              .trim();


          /*
           * Restore protected content.
           */

          result =
            result.replace(
              /___CODE_MIN_(\d+)___/g,
              (_, index) =>
                placeholders[
                  Number(index)
                ]
            );


          /*
           * Remove comments that were protected
           * and are actually comments.
           */

          result =
            result.replace(
              /\/\*[\s\S]*?\*\//g,
              ""
            );


          result =
            result.replace(
              /(^|[;\s{}])\/\/[^\n]*/g,
              "$1"
            );


          /*
           * Final whitespace cleanup.
           */

          result =
            result
              .replace(/\n\s*/g, "")
              .replace(/[ \t]+/g, " ")
              .trim();


          return result;

        }


        /* ======================================
           MINIFY
        ======================================= */

        function minify() {

          const code =
            input.value;


          if (!code.trim()) {

            output.value = "";

            updateEditorStats();

            savingPercent.textContent =
              "0%";

            compressionText.textContent =
              "أدخل الكود أولاً لمعرفة نسبة التوفير";

            setStatus(
              "warning",
              "أدخل الكود أولاً"
            );

            input.focus();

            return;

          }


          const started =
            performance.now();


          try {

            const originalBytes =
              new Blob([code]).size;


            const minified =
              minifyCode(code);


            const minifiedBytes =
              new Blob([minified]).size;


            output.value =
              minified;


            updateEditorStats();


            let saving = 0;


            if (originalBytes > 0) {

              saving =
                Math.max(
                  0,
                  (
                    (
                      1 -
                      minifiedBytes /
                      originalBytes
                    ) *
                    100
                  )
                );

            }


            savingPercent.textContent =
              `${saving.toFixed(1)}%`;


            compressionText.textContent =
              `${formatBytes(originalBytes)} → ${formatBytes(minifiedBytes)}`;


            const elapsed =
              performance.now() -
              started;


            setStatus(
              "success",
              `تم التصغير خلال ${elapsed.toFixed(1)} ms`
            );


          } catch {

            output.value = "";

            updateEditorStats();

            setStatus(
              "error",
              "تعذر تصغير الكود"
            );

          }

        }


        /* ======================================
           FORMAT BYTES
        ======================================= */

        function formatBytes(bytes) {

          if (bytes < 1024) {

            return `${bytes} B`;

          }


          if (bytes < 1024 * 1024) {

            return `${(bytes / 1024).toFixed(1)} KB`;

          }


          return `${(
            bytes /
            (1024 * 1024)
          ).toFixed(1)} MB`;

        }


        /* ======================================
           COPY
        ======================================= */

        async function copyOutput() {

          const value =
            output.value;


          if (!value) {

            setStatus(
              "warning",
              "لا يوجد كود لنسخه"
            );

            return;

          }


          try {

            await navigator.clipboard.writeText(
              value
            );


            const oldText =
              copyBtn.innerHTML;


            copyBtn.innerHTML = `
              <i class="fa-solid fa-check"></i>
              تم النسخ
            `;


            setStatus(
              "success",
              "تم نسخ الكود المصغر"
            );


            setTimeout(() => {

              copyBtn.innerHTML =
                oldText;

            }, 1500);


          } catch {

            output.select();

            document.execCommand("copy");

            setStatus(
              "success",
              "تم نسخ الكود"
            );

          }

        }


        /* ======================================
           CLEAR
        ======================================= */

        function clearAll() {

          input.value = "";

          output.value = "";

          savingPercent.textContent =
            "0%";

          compressionText.textContent =
            "قم بتصغير الكود لمعرفة نسبة التوفير";

          updateEditorStats();

          setStatus(
            "idle",
            "جاهز"
          );

          input.focus();

        }


        /* ======================================
           EXAMPLE
        ======================================= */

        exampleBtn.addEventListener(
          "click",
          () => {

            input.value =
              exampleCode;

            updateEditorStats();

            minify();

            input.focus();

          }
        );


        /* ======================================
           EVENTS
        ======================================= */

        minifyBtn.addEventListener(
          "click",
          minify
        );


        copyBtn.addEventListener(
          "click",
          copyOutput
        );


        clearBtn.addEventListener(
          "click",
          clearAll
        );


        input.addEventListener(
          "input",
          updateEditorStats
        );


        input.addEventListener(
          "scroll",
          () => {

            inputLines.scrollTop =
              input.scrollTop;

          }
        );


        output.addEventListener(
          "scroll",
          () => {

            outputLines.scrollTop =
              output.scrollTop;

          }
        );


        /* ======================================
           TAB + CTRL ENTER
        ======================================= */

        input.addEventListener(
          "keydown",
          (event) => {

            if (event.key === "Tab") {

              event.preventDefault();


              const start =
                input.selectionStart;

              const end =
                input.selectionEnd;


              input.value =
                input.value.substring(
                  0,
                  start
                ) +
                "  " +
                input.value.substring(
                  end
                );


              input.selectionStart =
                input.selectionEnd =
                  start + 2;


              updateEditorStats();

            }


            if (
              event.key === "Enter" &&
              (event.ctrlKey || event.metaKey)
            ) {

              event.preventDefault();

              minify();

            }

          }
        );


        /* ======================================
           INITIAL
        ======================================= */

        updateEditorStats();

      })();
    </script>


    <!-- ======================================
         STYLE
    ======================================= -->

    <style>

      :root {

        --bg:
          #f3f7f4;

        --surface:
          #ffffff;

        --surface-2:
          #f7faf8;

        --text:
          #111827;

        --muted:
          #6b7280;

        --border:
          rgba(15, 23, 42, .09);

        --primary:
          #16a34a;

        --primary-dark:
          #15803d;

        --secondary:
          #22c55e;

        --cyan:
          #06b6d4;

        --blue:
          #2563eb;

        --purple:
          #7c3aed;

        --orange:
          #ea580c;

        --red:
          #dc2626;

        --shadow:
          0 25px 70px
          rgba(15, 23, 42, .09);

        --code-bg:
          #07110c;

        --code-bg-2:
          #0b1710;

        --code-text:
          #e5e7eb;

      }


      html.dark-mode {

        --bg:
          #050b07;

        --surface:
          #0b1510;

        --surface-2:
          #101c15;

        --text:
          #f8fafc;

        --muted:
          #94a3b8;

        --border:
          rgba(148, 163, 184, .12);

        --primary:
          #4ade80;

        --primary-dark:
          #22c55e;

        --secondary:
          #86efac;

        --shadow:
          0 30px 80px
          rgba(0,0,0,.4);

      }


      * {
        box-sizing: border-box;
      }


      body {

        margin:
          0;

        background:
          var(--bg);

        color:
          var(--text);

        font-family:
          "Cairo",
          sans-serif;

        transition:
          background .25s ease,
          color .25s ease;

      }


      button,
      textarea {
        font-family:
          inherit;
      }


      .container {

        width:
          min(
            1120px,
            calc(100% - 32px)
          );

        margin-inline:
          auto;

      }


      /* ======================================
         HERO
      ======================================= */

      .hero {

        position:
          relative;

        overflow:
          hidden;

        padding:
          34px 0 45px;

        text-align:
          center;

      }


      .hero-glow {

        position:
          absolute;

        width:
          320px;

        height:
          320px;

        border-radius:
          50%;

        filter:
          blur(90px);

        opacity:
          .15;

        pointer-events:
          none;

      }


      .glow-one {

        top:
          -170px;

        right:
          8%;

        background:
          #22c55e;

      }


      .glow-two {

        bottom:
          -210px;

        left:
          8%;

        background:
          #06b6d4;

      }


      .breadcrumb {

        display:
          flex;

        justify-content:
          center;

        align-items:
          center;

        flex-wrap:
          wrap;

        gap:
          8px;

        color:
          var(--muted);

        font-size:
          12px;

        margin-bottom:
          23px;

      }


      .breadcrumb a {

        color:
          var(--muted);

        text-decoration:
          none;

      }


      .breadcrumb a:hover {

        color:
          var(--primary);

      }


      .breadcrumb i {

        font-size:
          8px;

        opacity:
          .6;

      }


      .hero-badge {

        display:
          inline-flex;

        align-items:
          center;

        gap:
          7px;

        padding:
          7px 12px;

        border:
          1px solid
          rgba(22,163,74,.18);

        border-radius:
          999px;

        background:
          rgba(22,163,74,.07);

        color:
          var(--primary);

        font-size:
          11px;

        font-weight:
          700;

        margin-bottom:
          15px;

      }


      .badge-dot {

        width:
          6px;

        height:
          6px;

        border-radius:
          50%;

        background:
          #22c55e;

        box-shadow:
          0 0 0 4px
          rgba(34,197,94,.1);

      }


      .hero-icon {

        position:
          relative;

        width:
          72px;

        height:
          72px;

        display:
          grid;

        place-items:
          center;

        margin:
          0 auto 15px;

        border-radius:
          20px;

        background:
          linear-gradient(
            135deg,
            #16a34a,
            #22c55e
          );

        color:
          white;

        box-shadow:
          0 15px 35px
          rgba(22,163,74,.25);

        font-size:
          24px;

      }


      .minify-mark {

        position:
          absolute;

        right:
          13px;

        bottom:
          10px;

        width:
          22px;

        height:
          22px;

        display:
          grid;

        place-items:
          center;

        border:
          2px solid
          white;

        border-radius:
          7px;

        background:
          #15803d;

        font:
          800 16px
          "JetBrains Mono",
          monospace;

      }


      .hero h1 {

        margin:
          0;

        font-size:
          clamp(
            30px,
            5vw,
            46px
          );

        line-height:
          1.2;

        font-weight:
          800;

        letter-spacing:
          -.8px;

      }


      .hero h1 span {

        background:
          linear-gradient(
            90deg,
            var(--primary),
            var(--secondary)
          );

        -webkit-background-clip:
          text;

        background-clip:
          text;

        color:
          transparent;

      }


      .hero p {

        max-width:
          720px;

        margin:
          14px auto 0;

        color:
          var(--muted);

        font-size:
          14px;

        line-height:
          2;

      }


      /* ======================================
         MAIN CARD
      ======================================= */

      .minifier-card {

        background:
          var(--surface);

        border:
          1px solid
          var(--border);

        border-radius:
          24px;

        box-shadow:
          var(--shadow);

        overflow:
          hidden;

      }


      .topbar {

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        gap:
          15px;

        padding:
          17px 20px;

        border-bottom:
          1px solid
          var(--border);

      }


      .project-info {

        display:
          flex;

        align-items:
          center;

        gap:
          11px;

      }


      .project-logo {

        width:
          40px;

        height:
          40px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          11px;

        background:
          linear-gradient(
            135deg,
            #16a34a,
            #22c55e
          );

        color:
          white;

        font-size:
          14px;

      }


      .project-info strong {

        display:
          block;

        font-size:
          13px;

      }


      .project-info span {

        display:
          block;

        color:
          var(--muted);

        font-size:
          10px;

      }


      .topbar-status {

        display:
          flex;

        align-items:
          center;

        gap:
          7px;

        color:
          var(--muted);

        font-size:
          10px;

      }


      .online-dot {

        width:
          7px;

        height:
          7px;

        border-radius:
          50%;

        background:
          #22c55e;

        box-shadow:
          0 0 0 4px
          rgba(34,197,94,.08);

      }


      /* ======================================
         ACTION BAR
      ======================================= */

      .actionbar {

        display:
          flex;

        justify-content:
          space-between;

        align-items:
          center;

        gap:
          10px;

        padding:
          13px 18px;

        background:
          var(--surface-2);

        border-bottom:
          1px solid
          var(--border);

      }


      .action-group {

        display:
          flex;

        gap:
          8px;

      }


      .action {

        display:
          inline-flex;

        align-items:
          center;

        justify-content:
          center;

        gap:
          7px;

        border:
          1px solid
          var(--border);

        border-radius:
          10px;

        padding:
          9px 13px;

        cursor:
          pointer;

        font-size:
          11px;

        font-weight:
          700;

        transition:
          .2s ease;

      }


      .action:hover {

        transform:
          translateY(-1px);

      }


      .action.secondary {

        background:
          var(--surface);

        color:
          var(--text);

      }


      .action.secondary:hover {

        color:
          var(--primary);

        border-color:
          rgba(22,163,74,.25);

      }


      .action.primary {

        border-color:
          transparent;

        background:
          linear-gradient(
            135deg,
            var(--primary),
            var(--secondary)
          );

        color:
          white;

        box-shadow:
          0 8px 20px
          rgba(22,163,74,.2);

      }


      .action.primary:hover {

        box-shadow:
          0 11px 25px
          rgba(22,163,74,.3);

      }


      /* ======================================
         EDITORS
      ======================================= */

      .editor-grid {

        display:
          grid;

        grid-template-columns:
          repeat(
            2,
            minmax(
              0,
              1fr
            )
          );

        gap:
          14px;

        padding:
          16px;

      }


      .editor-card {

        min-width:
          0;

        overflow:
          hidden;

        border:
          1px solid
          rgba(148,163,184,.12);

        border-radius:
          16px;

        background:
          var(--code-bg);

        color:
          var(--code-text);

      }


      .editor-head {

        height:
          52px;

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        padding:
          0 14px;

        background:
          var(--code-bg-2);

        border-bottom:
          1px solid
          rgba(255,255,255,.06);

      }


      .editor-label {

        display:
          flex;

        align-items:
          center;

        gap:
          9px;

      }


      .file-icon {

        width:
          31px;

        height:
          31px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          8px;

        background:
          rgba(34,197,94,.12);

        color:
          #4ade80;

        font-size:
          12px;

      }


      .output-icon {

        background:
          rgba(6,182,212,.12);

        color:
          #67e8f9;

      }


      .editor-label strong {

        display:
          block;

        font-size:
          11px;

      }


      .editor-label small {

        display:
          block;

        margin-top:
          1px;

        color:
          #64748b;

        font-size:
          9px;

      }


      .editor-stats {

        color:
          #64748b;

        font:
          9px
          "JetBrains Mono",
          monospace;

      }


      .code-area {

        display:
          flex;

        height:
          420px;

        direction:
          ltr;

        overflow:
          hidden;

      }


      .line-numbers {

        width:
          45px;

        flex:
          0 0 45px;

        padding:
          15px 8px;

        background:
          rgba(0,0,0,.14);

        border-right:
          1px solid
          rgba(255,255,255,.05);

        color:
          #475569;

        text-align:
          right;

        white-space:
          pre;

        overflow:
          hidden;

        user-select:
          none;

        font:
          12px/1.8
          "JetBrains Mono",
          monospace;

      }


      textarea {

        width:
          100%;

        height:
          100%;

        resize:
          none;

        outline:
          none;

        border:
          0;

        padding:
          15px;

        background:
          var(--code-bg);

        color:
          var(--code-text);

        font:
          12px/1.8
          "JetBrains Mono",
          monospace;

        direction:
          ltr;

        text-align:
          left;

        tab-size:
          2;

      }


      textarea::placeholder {

        color:
          #475569;

      }


      .editor-foot {

        min-height:
          38px;

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        padding:
          0 13px;

        border-top:
          1px solid
          rgba(255,255,255,.06);

        color:
          #64748b;

        font-size:
          9px;

      }


      .editor-foot span {

        display:
          flex;

        align-items:
          center;

        gap:
          5px;

      }


      /* ======================================
         STATUS
      ======================================= */

      .status {

        display:
          flex;

        align-items:
          center;

        gap:
          6px;

      }


      .status-dot {

        width:
          6px;

        height:
          6px;

        border-radius:
          50%;

        background:
          #64748b;

      }


      .status.success {

        color:
          #4ade80;

      }


      .status.success .status-dot {

        background:
          #22c55e;

      }


      .status.warning {

        color:
          #fbbf24;

      }


      .status.warning .status-dot {

        background:
          #f59e0b;

      }


      .status.error {

        color:
          #f87171;

      }


      .status.error .status-dot {

        background:
          #ef4444;

      }


      /* ======================================
         COMPRESSION
      ======================================= */

      .compression-result {

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        gap:
          15px;

        margin:
          0 16px 14px;

        padding:
          14px 16px;

        border:
          1px solid
          rgba(22,163,74,.14);

        border-radius:
          14px;

        background:
          rgba(22,163,74,.045);

      }


      .compression-main {

        display:
          flex;

        align-items:
          center;

        gap:
          10px;

      }


      .compression-icon {

        width:
          38px;

        height:
          38px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          10px;

        background:
          rgba(22,163,74,.11);

        color:
          var(--primary);

      }


      .compression-main strong {

        display:
          block;

        font-size:
          11px;

      }


      .compression-main span {

        display:
          block;

        margin-top:
          2px;

        color:
          var(--muted);

        font-size:
          9px;

      }


      .compression-value {

        text-align:
          center;

      }


      .compression-value span {

        display:
          block;

        color:
          var(--primary);

        font:
          800 20px
          "JetBrains Mono",
          monospace;

      }


      .compression-value small {

        display:
          block;

        margin-top:
          1px;

        color:
          var(--muted);

        font-size:
          8px;

      }


      /* ======================================
         STATS
      ======================================= */

      .tool-stats {

        display:
          grid;

        grid-template-columns:
          repeat(
            4,
            1fr
          );

        gap:
          10px;

        padding:
          0 16px 16px;

      }


      .mini-stat {

        display:
          flex;

        align-items:
          center;

        gap:
          10px;

        padding:
          12px;

        border:
          1px solid
          var(--border);

        border-radius:
          13px;

        background:
          var(--surface-2);

      }


      .mini-icon {

        width:
          35px;

        height:
          35px;

        flex:
          0 0 35px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          9px;

      }


      .mini-icon.green {

        background:
          rgba(22,163,74,.1);

        color:
          var(--primary);

      }


      .mini-icon.blue {

        background:
          rgba(37,99,235,.1);

        color:
          var(--blue);

      }


      .mini-icon.purple {

        background:
          rgba(124,58,237,.1);

        color:
          var(--purple);

      }


      .mini-icon.orange {

        background:
          rgba(234,88,12,.1);

        color:
          var(--orange);

      }


      .mini-stat strong {

        display:
          block;

        font-size:
          10px;

      }


      .mini-stat span {

        display:
          block;

        margin-top:
          2px;

        color:
          var(--muted);

        font-size:
          8px;

      }


      /* ======================================
         INFORMATION
      ======================================= */

      .information {

        margin-top:
          55px;

      }


      .section-title {

        text-align:
          center;

        max-width:
          700px;

        margin:
          0 auto 25px;

      }


      .section-title > span {

        color:
          var(--primary);

        font-size:
          11px;

        font-weight:
          800;

        letter-spacing:
          .5px;

      }


      .section-title h2 {

        margin:
          5px 0 7px;

        font-size:
          clamp(
            21px,
            4vw,
            28px
          );

      }


      .section-title p {

        margin:
          0;

        color:
          var(--muted);

        font-size:
          12px;

        line-height:
          1.9;

      }


      .info-grid {

        display:
          grid;

        grid-template-columns:
          repeat(
            3,
            1fr
          );

        gap:
          14px;

      }


      .info-card {

        position:
          relative;

        padding:
          24px;

        overflow:
          hidden;

        border:
          1px solid
          var(--border);

        border-radius:
          18px;

        background:
          var(--surface);

      }


      .info-number {

        position:
          absolute;

        top:
          15px;

        left:
          18px;

        color:
          var(--muted);

        opacity:
          .25;

        font:
          700 28px
          "JetBrains Mono",
          monospace;

      }


      .info-card-icon {

        width:
          42px;

        height:
          42px;

        display:
          grid;

        place-items:
          center;

        margin-bottom:
          13px;

        border-radius:
          11px;

        background:
          rgba(22,163,74,.1);

        color:
          var(--primary);

      }


      .info-card h3 {

        margin:
          0 0 7px;

        font-size:
          14px;

      }


      .info-card p {

        margin:
          0;

        color:
          var(--muted);

        font-size:
          11px;

        line-height:
          1.9;

      }


      /* ======================================
         HOW
      ======================================= */

      .how-section {

        margin-top:
          55px;

      }


      .steps {

        display:
          grid;

        grid-template-columns:
          repeat(
            3,
            1fr
          );

        gap:
          14px;

      }


      .step {

        display:
          flex;

        align-items:
          flex-start;

        gap:
          12px;

        padding:
          20px;

        background:
          var(--surface);

        border:
          1px solid
          var(--border);

        border-radius:
          17px;

      }


      .step-icon {

        width:
          43px;

        height:
          43px;

        flex:
          0 0 43px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          12px;

        background:
          linear-gradient(
            135deg,
            rgba(22,163,74,.12),
            rgba(34,197,94,.12)
          );

        color:
          var(--primary);

        font:
          700 10px
          "JetBrains Mono",
          monospace;

      }


      .step h3 {

        margin:
          0 0 5px;

        font-size:
          13px;

      }


      .step p {

        margin:
          0;

        color:
          var(--muted);

        font-size:
          10px;

        line-height:
          1.8;

      }


      /* ======================================
         FAQ
      ======================================= */

      .faq-section {

        margin:
          55px auto 70px;

        max-width:
          850px;

      }


      .faq-list details {

        margin-bottom:
          9px;

        overflow:
          hidden;

        border:
          1px solid
          var(--border);

        border-radius:
          14px;

        background:
          var(--surface);

      }


      .faq-list summary {

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        gap:
          15px;

        padding:
          16px 18px;

        cursor:
          pointer;

        list-style:
          none;

        font-size:
          12px;

        font-weight:
          700;

      }


      .faq-list summary::-webkit-details-marker {
        display:
          none;
      }


      .faq-list summary i {

        color:
          var(--muted);

        font-size:
          10px;

        transition:
          transform .2s ease;

      }


      .faq-list details[open]
      summary i {

        transform:
          rotate(180deg);

      }


      .faq-answer {

        padding:
          0 18px 17px;

        color:
          var(--muted);

        font-size:
          11px;

        line-height:
          1.9;

      }


      /* ======================================
         RESPONSIVE
      ======================================= */

      @media (max-width: 900px) {

        .editor-grid {

          grid-template-columns:
            1fr;

        }


        .info-grid,
        .steps {

          grid-template-columns:
            repeat(
              2,
              1fr
            );

        }

      }


      @media (max-width: 700px) {

        .tool-stats {

          grid-template-columns:
            repeat(
              2,
              1fr
            );

        }


        .actionbar {

          flex-direction:
            column;

          align-items:
            stretch;

        }


        .action-group {

          width:
            100%;

        }


        .action {

          flex:
            1;

        }


        .compression-result {

          align-items:
            center;

        }

      }


      @media (max-width: 600px) {

        .container {

          width:
            calc(
              100% - 20px
            );

        }


        .hero {

          padding:
            25px 0 32px;

        }


        .hero-icon {

          width:
            60px;

          height:
            60px;

          font-size:
            20px;

        }


        .hero p {

          font-size:
            12px;

        }


        .topbar {

          padding:
            14px;

        }


        .topbar-status {

          display:
            none;

        }


        .actionbar {

          padding:
            10px;

        }


        .action {

          padding:
            9px 7px;

          font-size:
            10px;

        }


        .editor-grid {

          padding:
            10px;

        }


        .code-area {

          height:
            330px;

        }


        .compression-result {

          margin:
            0 10px 10px;

          padding:
            12px;

        }


        .compression-main span {

          max-width:
            170px;

        }


        .info-grid,
        .steps {

          grid-template-columns:
            1fr;

        }


        .faq-section {

          margin-bottom:
            45px;

        }

      }

    </style>

  </body>
</html>
