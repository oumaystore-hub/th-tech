---
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const title =
  "TypeScript Playground - تشغيل TypeScript أونلاين مجاناً | Th.Tech";

const description =
  "TypeScript Playground مجاني لتجربة وتشغيل أكواد TypeScript مباشرة من المتصفح.";

const faqs = [
  {
    q: "ما هو TypeScript Playground؟",
    a: "أداة تتيح لك تجربة أكواد TypeScript وتشغيلها مباشرة من المتصفح."
  },
  {
    q: "هل يتم إرسال الكود إلى الخادم؟",
    a: "لا. تتم معالجة وتشغيل الكود محلياً داخل المتصفح."
  },
  {
    q: "هل يمكن استخدام console.log؟",
    a: "نعم. ستظهر نتائج console.log داخل نافذة Console الموجودة أسفل المحرر."
  },
  {
    q: "هل الأداة مجانية؟",
    a: "نعم. الأداة مجانية ويمكن استخدامها مباشرة من المتصفح."
  }
];
---

<!doctype html>
<html lang="ar" dir="rtl">
  <head>

    <meta charset="UTF-8" />

    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />

    <title>{title}</title>

    <meta
      name="description"
      content={description}
    />

    <meta
      name="robots"
      content="index, follow"
    />

    <meta
      name="theme-color"
      content="#0891b2"
    />

    <link
      rel="icon"
      href={`${base}/logo1.png`}
    />

    <link
      rel="preconnect"
      href="https://fonts.googleapis.com"
    />

    <link
      rel="preconnect"
      href="https://fonts.gstatic.com"
      crossorigin
    />

    <link
      href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap"
      rel="stylesheet"
    />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
    />

    <script is:inline>
      (() => {
        try {
          const saved = localStorage.getItem("theme");

          if (saved === "dark") {
            document.documentElement.classList.add("dark-mode");
          } else {
            document.documentElement.classList.remove("dark-mode");
          }
        } catch (error) {
        }
      })();
    </script>

  </head>

  <body>

    <Header />

    <main>

      <!-- HERO -->

      <section class="hero">

        <div class="hero-glow glow-one"></div>
        <div class="hero-glow glow-two"></div>

        <div class="container">

          <div class="breadcrumb">

            <a href={`${base}/`}>
              الرئيسية
            </a>

            <i class="fa-solid fa-chevron-left"></i>

            <a href={`${base}/tools/`}>
              الأدوات
            </a>

            <i class="fa-solid fa-chevron-left"></i>

            <span>
              TypeScript Playground
            </span>

          </div>


          <div class="hero-badge">

            <span class="badge-dot"></span>

            Developer Tools

            <i class="fa-solid fa-terminal"></i>

          </div>


          <div class="hero-icon">

            <span class="ts-mark">TS</span>

            <span class="terminal-mark">
              <i class="fa-solid fa-terminal"></i>
            </span>

          </div>


          <h1>

            TypeScript

            <span>Playground</span>

          </h1>


          <p>

            جرّب وشغّل أكواد TypeScript مباشرة من المتصفح
            مع Console لعرض النتائج والأخطاء بشكل واضح.

          </p>

        </div>

      </section>


      <!-- PLAYGROUND -->

      <section class="tool-section">

        <div class="container">

          <div class="playground-card">


            <!-- TOP BAR -->

            <div class="topbar">

              <div class="project-info">

                <div class="project-logo">

                  <span>TS</span>

                  <i class="fa-solid fa-terminal"></i>

                </div>

                <div>

                  <strong>
                    TypeScript Playground
                  </strong>

                  <span>
                    Online TypeScript Runtime
                  </span>

                </div>

              </div>


              <div class="topbar-status">

                <span class="online-dot"></span>

                Local Processing

              </div>

            </div>


            <!-- ACTION BAR -->

            <div class="actionbar">

              <div class="action-group">

                <button
                  id="exampleBtn"
                  class="action secondary"
                  type="button"
                >
                  <i class="fa-solid fa-wand-magic-sparkles"></i>
                  مثال
                </button>


                <button
                  id="clearBtn"
                  class="action secondary"
                  type="button"
                >
                  <i class="fa-solid fa-trash-can"></i>
                  مسح
                </button>

              </div>


              <div class="action-group">

                <button
                  id="copyBtn"
                  class="action secondary"
                  type="button"
                >
                  <i class="fa-regular fa-copy"></i>
                  نسخ
                </button>


                <button
                  id="runBtn"
                  class="action primary"
                  type="button"
                >
                  <i class="fa-solid fa-play"></i>
                  تشغيل الكود
                </button>

              </div>

            </div>


            <!-- EDITOR -->

            <div class="editor-card">

              <div class="editor-head">

                <div class="editor-label">

                  <span class="file-icon">

                    <span class="mini-ts">
                      TS
                    </span>

                  </span>

                  <div>

                    <strong>
                      main.ts
                    </strong>

                    <small>
                      TypeScript
                    </small>

                  </div>

                </div>


                <div
                  id="inputStats"
                  class="editor-stats"
                >
                  0 حرف
                </div>

              </div>


              <div class="code-area">

                <div
                  id="inputLines"
                  class="line-numbers"
                >
                  1
                </div>


                <textarea
                  id="inputCode"
                  spellcheck="false"
                  autocomplete="off"
                  autocorrect="off"
                  autocapitalize="off"
                  placeholder="// اكتب كود TypeScript هنا..."
                ></textarea>

              </div>


              <div class="editor-foot">

                <span>

                  <i class="fa-solid fa-keyboard"></i>

                  Ctrl + Enter

                </span>


                <span>

                  TypeScript

                </span>

              </div>

            </div>


            <!-- CONSOLE -->

            <div class="console-card">

              <div class="console-head">

                <div class="console-title">

                  <span class="console-icon">

                    <i class="fa-solid fa-terminal"></i>

                  </span>

                  <div>

                    <strong>
                      Console
                    </strong>

                    <small>
                      Output
                    </small>

                  </div>

                </div>


                <button
                  id="clearConsoleBtn"
                  class="console-clear"
                  type="button"
                >

                  <i class="fa-solid fa-broom"></i>

                  مسح

                </button>

              </div>


              <div
                id="consoleOutput"
                class="console-output"
              >

                <div class="console-placeholder">

                  <i class="fa-solid fa-terminal"></i>

                  <span>
                    ستظهر نتيجة تشغيل الكود هنا...
                  </span>

                </div>

              </div>


              <div class="console-foot">

                <span
                  id="status"
                  class="status idle"
                >

                  <span class="status-dot"></span>

                  جاهز

                </span>


                <span id="executionTime">
                  0 ms
                </span>

              </div>

            </div>


            <!-- STATS -->

            <div class="tool-stats">


              <div class="mini-stat">

                <div class="mini-icon cyan">

                  <i class="fa-solid fa-play"></i>

                </div>

                <div>

                  <strong>
                    تشغيل فوري
                  </strong>

                  <span>
                    داخل المتصفح
                  </span>

                </div>

              </div>


              <div class="mini-stat">

                <div class="mini-icon blue">

                  <span class="mini-ts-large">
                    TS
                  </span>

                </div>

                <div>

                  <strong>
                    TypeScript
                  </strong>

                  <span>
                    دعم TypeScript
                  </span>

                </div>

              </div>


              <div class="mini-stat">

                <div class="mini-icon green">

                  <i class="fa-solid fa-lock"></i>

                </div>

                <div>

                  <strong>
                    Private
                  </strong>

                  <span>
                    لا رفع للكود
                  </span>

                </div>

              </div>


              <div class="mini-stat">

                <div class="mini-icon orange">

                  <i class="fa-solid fa-mobile-screen"></i>

                </div>

                <div>

                  <strong>
                    Responsive
                  </strong>

                  <span>
                    جميع الأجهزة
                  </span>

                </div>

              </div>


            </div>

          </div>


          <!-- INFORMATION -->

          <section class="information">

            <div class="section-title">

              <span>
                TypeScript Tools
              </span>

              <h2>
                TypeScript Playground احترافي
              </h2>

              <p>
                بيئة بسيطة وسريعة لتجربة أكواد TypeScript
                ومشاهدة النتائج مباشرة.
              </p>

            </div>


            <div class="info-grid">


              <article class="info-card">

                <div class="info-number">
                  01
                </div>

                <div class="info-card-icon">

                  <i class="fa-solid fa-play"></i>

                </div>

                <h3>
                  تشغيل مباشر
                </h3>

                <p>
                  شغّل الكود مباشرة من المتصفح
                  دون الحاجة إلى تثبيت بيئة تطوير.
                </p>

              </article>


              <article class="info-card">

                <div class="info-number">
                  02
                </div>

                <div class="info-card-icon">

                  <i class="fa-solid fa-terminal"></i>

                </div>

                <h3>
                  Console مدمج
                </h3>

                <p>
                  شاهد نتائج console.log والأخطاء
                  داخل نافذة Console مخصصة.
                </p>

              </article>


              <article class="info-card">

                <div class="info-number">
                  03
                </div>

                <div class="info-card-icon">

                  <i class="fa-solid fa-shield-halved"></i>

                </div>

                <h3>
                  معالجة محلية
                </h3>

                <p>
                  يتم تشغيل الكود محلياً داخل المتصفح
                  ولا يتم رفعه إلى خادم خارجي.
                </p>

              </article>

            </div>

          </section>


          <!-- HOW TO -->

          <section class="how-section">

            <div class="section-title">

              <span>
                How It Works
              </span>

              <h2>
                طريقة استخدام الأداة
              </h2>

            </div>


            <div class="steps">


              <div class="step">

                <div class="step-icon">

                  <span>
                    01
                  </span>

                </div>

                <div>

                  <h3>
                    اكتب الكود
                  </h3>

                  <p>
                    اكتب أو ألصق كود TypeScript
                    داخل المحرر.
                  </p>

                </div>

              </div>


              <div class="step">

                <div class="step-icon">

                  <span>
                    02
                  </span>

                </div>

                <div>

                  <h3>
                    اضغط تشغيل
                  </h3>

                  <p>
                    اضغط على تشغيل الكود أو
                    استخدم Ctrl + Enter.
                  </p>

                </div>

              </div>


              <div class="step">

                <div class="step-icon">

                  <span>
                    03
                  </span>

                </div>

                <div>

                  <h3>
                    شاهد النتيجة
                  </h3>

                  <p>
                    ستظهر النتائج والأخطاء
                    داخل Console.
                  </p>

                </div>

              </div>

            </div>

          </section>


          <!-- FAQ -->

          <section class="faq-section">

            <div class="section-title">

              <span>
                FAQ
              </span>

              <h2>
                الأسئلة الشائعة
              </h2>

            </div>


            <div class="faq-list">

              {
                faqs.map((faq) => (
                  <details>

                    <summary>

                      <span>
                        {faq.q}
                      </span>

                      <i class="fa-solid fa-chevron-down"></i>

                    </summary>

                    <div class="faq-answer">
                      {faq.a}
                    </div>

                  </details>
                ))
              }

            </div>

          </section>

        </div>

      </section>

    </main>


    <Footer />


    <!-- JAVASCRIPT -->

    <script is:inline>
      (() => {

        const input =
          document.getElementById("inputCode");

        const inputLines =
          document.getElementById("inputLines");

        const inputStats =
          document.getElementById("inputStats");

        const consoleOutput =
          document.getElementById("consoleOutput");

        const executionTime =
          document.getElementById("executionTime");

        const status =
          document.getElementById("status");

        const runBtn =
          document.getElementById("runBtn");

        const copyBtn =
          document.getElementById("copyBtn");

        const clearBtn =
          document.getElementById("clearBtn");

        const clearConsoleBtn =
          document.getElementById("clearConsoleBtn");

        const exampleBtn =
          document.getElementById("exampleBtn");


        const exampleCode =
`interface User {
  id: number;
  name: string;
  age: number;
}

const user: User = {
  id: 1,
  name: "Ahmed",
  age: 25
};

console.log("Hello from TypeScript!");
console.log("User:", user);
console.log("Name:", user.name);
console.log("Age:", user.age);`;


        function updateStats() {

          const value =
            input.value;

          const count =
            value
              ? value.split("\n").length
              : 1;

          inputLines.textContent =
            Array.from(
              { length: count },
              (_, index) => index + 1
            ).join("\n");

          inputStats.textContent =
            value.length + " حرف";

        }


        function setStatus(
          type,
          text
        ) {

          status.className =
            "status " + type;

          status.innerHTML =
            '<span class="status-dot"></span>' +
            text;

        }


        function escapeHtml(value) {

          return String(value)
            .replace(/&/g, "&amp;")
            .replace(/</g, "&lt;")
            .replace(/>/g, "&gt;")
            .replace(/"/g, "&quot;")
            .replace(/'/g, "&#039;");

        }


        function addConsoleLine(
          type,
          values
        ) {

          const line =
            document.createElement("div");

          line.className =
            "console-line " + type;

          const icon =
            document.createElement("span");

          icon.className =
            "console-line-icon";

          if (type === "error") {

            icon.innerHTML =
              '<i class="fa-solid fa-circle-xmark"></i>';

          } else if (type === "warn") {

            icon.innerHTML =
              '<i class="fa-solid fa-triangle-exclamation"></i>';

          } else {

            icon.innerHTML =
              '<i class="fa-solid fa-angle-right"></i>';

          }


          const text =
            document.createElement("span");

          text.className =
            "console-line-text";

          text.textContent =
            values
              .map((value) => {

                if (
                  typeof value === "object" &&
                  value !== null
                ) {

                  try {

                    return JSON.stringify(
                      value,
                      null,
                      2
                    );

                  } catch {

                    return String(value);

                  }

                }

                return String(value);

              })
              .join(" ");


          line.appendChild(icon);

          line.appendChild(text);

          consoleOutput.appendChild(line);

          consoleOutput.scrollTop =
            consoleOutput.scrollHeight;

        }


        function showPlaceholder() {

          consoleOutput.innerHTML =
            '<div class="console-placeholder">' +
              '<i class="fa-solid fa-terminal"></i>' +
              '<span>ستظهر نتيجة تشغيل الكود هنا...</span>' +
            '</div>';

        }


        function stripTypeScript(code) {

          let result =
            code;

          /*
           * Remove interface declarations.
           */

          result =
            result.replace(
              /interface\s+[A-Za-z_$][\w$]*(?:\s+extends[^{]+)?\s*\{[\s\S]*?\}/g,
              ""
            );


          /*
           * Remove type aliases.
           */

          result =
            result.replace(
              /type\s+[A-Za-z_$][\w$]*(?:\s*<[^>]*>)?\s*=\s*[^;]+;/g,
              ""
            );


          /*
           * Remove access modifiers.
           */

          result =
            result.replace(
              /\b(public|private|protected|readonly|abstract|declare)\s+/g,
              ""
            );


          /*
           * Remove generic parameters from common functions.
           */

          result =
            result.replace(
              /\b(function\s+[A-Za-z_$][\w$]*)\s*<[^>{}]+>/g,
              "$1"
            );


          /*
           * Remove variable type annotations.
           */

          result =
            result.replace(
              /(\b(?:const|let|var)\s+[A-Za-z_$][\w$]*)\s*:\s*[A-Za-z_$][\w$]*(?:\s*<[^;=,\n]+>)?(?:\[\])?/g,
              "$1"
            );


          /*
           * Remove function parameter types.
           */

          result =
            result.replace(
              /([A-Za-z_$][\w$]*)\s*:\s*[A-Za-z_$][\w$]*(?:\s*<[^,\)]+>)?(?:\[\])?/g,
              "$1"
            );


          /*
           * Remove return types.
           */

          result =
            result.replace(
              /\)\s*:\s*[A-Za-z_$][\w$]*(?:\s*<[^>{}]+>)?(?:\[\])?\s*\{/g,
              ") {"
            );


          /*
           * Remove "as Type" assertions.
           */

          result =
            result.replace(
              /\s+as\s+[A-Za-z_$][\w$]*(?:\s*<[^>]+>)?(?:\[\])?/g,
              ""
            );


          return result;

        }


        function executeCode(code) {

          const cleaned =
            stripTypeScript(code);


          const logs = [];

          const fakeConsole = {

            log: function() {

              logs.push({
                type: "log",
                values: Array.from(arguments)
              });

            },

            warn: function() {

              logs.push({
                type: "warn",
                values: Array.from(arguments)
              });

            },

            error: function() {

              logs.push({
                type: "error",
                values: Array.from(arguments)
              });

            },

            info: function() {

              logs.push({
                type: "log",
                values: Array.from(arguments)
              });

            }

          };


          const runner =
            new Function(
              "console",
              '"use strict";\n' +
              cleaned
            );


          runner(fakeConsole);


          return logs;

        }


        function runCode() {

          const code =
            input.value.trim();


          if (!code) {

            showPlaceholder();

            setStatus(
              "warning",
              "أدخل كود TypeScript أولاً"
            );

            input.focus();

            return;

          }


          const started =
            performance.now();


          consoleOutput.innerHTML = "";


          try {

            const logs =
              executeCode(code);


            if (!logs.length) {

              addConsoleLine(
                "log",
                ["تم تشغيل الكود بنجاح."]
              );

            } else {

              logs.forEach(
                function(item) {

                  addConsoleLine(
                    item.type,
                    item.values
                  );

                }
              );

            }


            const elapsed =
              performance.now() - started;


            executionTime.textContent =
              elapsed.toFixed(1) + " ms";


            setStatus(
              "success",
              "تم تشغيل الكود بنجاح"
            );


          } catch (error) {

            const elapsed =
              performance.now() - started;


            executionTime.textContent =
              elapsed.toFixed(1) + " ms";


            addConsoleLine(
              "error",
              [
                error &&
                error.message
                  ? error.message
                  : String(error)
              ]
            );


            setStatus(
              "error",
              "حدث خطأ أثناء التشغيل"
            );

          }

        }


        async function copyCode() {

          const value =
            input.value;


          if (!value) {

            setStatus(
              "warning",
              "لا يوجد كود لنسخه"
            );

            return;

          }


          try {

            await navigator.clipboard.writeText(
              value
            );


            const old =
              copyBtn.innerHTML;


            copyBtn.innerHTML =
              '<i class="fa-solid fa-check"></i> تم النسخ';


            setStatus(
              "success",
              "تم نسخ الكود"
            );


            setTimeout(
              function() {

                copyBtn.innerHTML =
                  old;

              },
              1500
            );


          } catch {

            input.select();

            document.execCommand(
              "copy"
            );


            setStatus(
              "success",
              "تم نسخ الكود"
            );

          }

        }


        function clearCode() {

          input.value =
            "";

          updateStats();

          showPlaceholder();

          executionTime.textContent =
            "0 ms";

          setStatus(
            "idle",
            "جاهز"
          );

          input.focus();

        }


        function clearConsole() {

          showPlaceholder();

          executionTime.textContent =
            "0 ms";

          setStatus(
            "idle",
            "جاهز"
          );

        }


        exampleBtn.addEventListener(
          "click",
          function() {

            input.value =
              exampleCode;

            updateStats();

            showPlaceholder();

            setStatus(
              "idle",
              "جاهز للتشغيل"
            );

            input.focus();

          }
        );


        runBtn.addEventListener(
          "click",
          runCode
        );


        copyBtn.addEventListener(
          "click",
          copyCode
        );


        clearBtn.addEventListener(
          "click",
          clearCode
        );


        clearConsoleBtn.addEventListener(
          "click",
          clearConsole
        );


        input.addEventListener(
          "input",
          updateStats
        );


        input.addEventListener(
          "scroll",
          function() {

            inputLines.scrollTop =
              input.scrollTop;

          }
        );


        input.addEventListener(
          "keydown",
          function(event) {

            if (event.key === "Tab") {

              event.preventDefault();


              const start =
                input.selectionStart;

              const end =
                input.selectionEnd;


              input.value =
                input.value.substring(
                  0,
                  start
                ) +
                "  " +
                input.value.substring(
                  end
                );


              input.selectionStart =
                start + 2;

              input.selectionEnd =
                start + 2;


              updateStats();

            }


            if (
              event.key === "Enter" &&
              (event.ctrlKey || event.metaKey)
            ) {

              event.preventDefault();

              runCode();

            }

          }
        );


        updateStats();

      })();
    </script>


    <style>

      :root {

        --bg: #f3f8fa;
        --surface: #ffffff;
        --surface-2: #f6fafb;

        --text: #102027;
        --muted: #64748b;

        --border: rgba(15, 23, 42, .09);

        --primary: #0891b2;
        --primary-dark: #0e7490;

        --secondary: #06b6d4;

        --cyan: #22d3ee;

        --green: #16a34a;
        --orange: #ea580c;
        --red: #dc2626;

        --shadow:
          0 25px 70px
          rgba(8, 145, 178, .10);

        --code-bg: #07141a;
        --code-bg-2: #0a1d25;

        --code-text: #e2f5f8;

      }


      html.dark-mode {

        --bg: #050b0f;
        --surface: #0b151a;
        --surface-2: #0e1b21;

        --text: #f1f5f9;
        --muted: #94a3b8;

        --border:
          rgba(148, 163, 184, .12);

        --primary: #22d3ee;
        --primary-dark: #06b6d4;

        --secondary: #67e8f9;

        --shadow:
          0 30px 80px
          rgba(0, 0, 0, .38);

      }


      * {
        box-sizing: border-box;
      }


      body {

        margin: 0;

        background:
          var(--bg);

        color:
          var(--text);

        font-family:
          "Cairo",
          sans-serif;

        transition:
          background .25s ease,
          color .25s ease;

      }


      button,
      textarea {
        font-family: inherit;
      }


      .container {

        width:
          min(
            1120px,
            calc(100% - 32px)
          );

        margin-inline:
          auto;

      }


      /* HERO */

      .hero {

        position:
          relative;

        overflow:
          hidden;

        padding:
          34px 0 45px;

        text-align:
          center;

      }


      .hero-glow {

        position:
          absolute;

        width:
          330px;

        height:
          330px;

        border-radius:
          50%;

        filter:
          blur(95px);

        opacity:
          .16;

        pointer-events:
          none;

      }


      .glow-one {

        top:
          -170px;

        right:
          8%;

        background:
          #06b6d4;

      }


      .glow-two {

        bottom:
          -220px;

        left:
          8%;

        background:
          #0891b2;

      }


      .breadcrumb {

        display:
          flex;

        justify-content:
          center;

        align-items:
          center;

        flex-wrap:
          wrap;

        gap:
          8px;

        color:
          var(--muted);

        font-size:
          12px;

        margin-bottom:
          23px;

      }


      .breadcrumb a {

        color:
          var(--muted);

        text-decoration:
          none;

      }


      .breadcrumb a:hover {

        color:
          var(--primary);

      }


      .breadcrumb i {

        font-size:
          8px;

        opacity:
          .6;

      }


      .hero-badge {

        display:
          inline-flex;

        align-items:
          center;

        gap:
          7px;

        padding:
          7px 12px;

        border:
          1px solid
          rgba(8,145,178,.20);

        border-radius:
          999px;

        background:
          rgba(8,145,178,.07);

        color:
          var(--primary);

        font-size:
          11px;

        font-weight:
          700;

        margin-bottom:
          15px;

      }


      .badge-dot {

        width:
          6px;

        height:
          6px;

        border-radius:
          50%;

        background:
          #22c55e;

        box-shadow:
          0 0 0 4px
          rgba(34,197,94,.1);

      }


      .hero-icon {

        width:
          76px;

        height:
          76px;

        display:
          flex;

        align-items:
          center;

        justify-content:
          center;

        gap:
          4px;

        margin:
          0 auto 15px;

        border-radius:
          21px;

        background:
          linear-gradient(
            135deg,
            #0891b2,
            #06b6d4
          );

        color:
          white;

        box-shadow:
          0 15px 35px
          rgba(8,145,178,.27);

        font-family:
          "JetBrains Mono",
          monospace;

        position:
          relative;

      }


      .ts-mark {

        font-size:
          21px;

        font-weight:
          800;

      }


      .terminal-mark {

        position:
          absolute;

        right:
          8px;

        bottom:
          7px;

        width:
          25px;

        height:
          20px;

        display:
          grid;

        place-items:
          center;

        border:
          2px solid
          rgba(255,255,255,.8);

        border-radius:
          5px;

        font-size:
          8px;

        background:
          rgba(0,0,0,.15);

      }


      .hero h1 {

        margin:
          0;

        font-size:
          clamp(
            30px,
            5vw,
            46px
          );

        line-height:
          1.2;

        font-weight:
          800;

      }


      .hero h1 span {

        background:
          linear-gradient(
            90deg,
            var(--primary),
            var(--secondary)
          );

        -webkit-background-clip:
          text;

        background-clip:
          text;

        color:
          transparent;

      }


      .hero p {

        max-width:
          720px;

        margin:
          14px auto 0;

        color:
          var(--muted);

        font-size:
          14px;

        line-height:
          2;

      }


      /* PLAYGROUND */

      .playground-card {

        overflow:
          hidden;

        background:
          var(--surface);

        border:
          1px solid
          var(--border);

        border-radius:
          24px;

        box-shadow:
          var(--shadow);

      }


      .topbar {

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        gap:
          15px;

        padding:
          17px 20px;

        border-bottom:
          1px solid
          var(--border);

      }


      .project-info {

        display:
          flex;

        align-items:
          center;

        gap:
          11px;

      }


      .project-logo {

        position:
          relative;

        width:
          40px;

        height:
          40px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          11px;

        background:
          linear-gradient(
            135deg,
            #0891b2,
            #06b6d4
          );

        color:
          white;

        font-family:
          "JetBrains Mono",
          monospace;

        font-size:
          12px;

        font-weight:
          800;

      }


      .project-logo i {

        position:
          absolute;

        right:
          -4px;

        bottom:
          -4px;

        width:
          18px;

        height:
          18px;

        display:
          grid;

        place-items:
          center;

        border:
          2px solid
          var(--surface);

        border-radius:
          5px;

        background:
          #0f172a;

        color:
          #67e8f9;

        font-size:
          7px;

      }


      .project-info strong {

        display:
          block;

        font-size:
          13px;

      }


      .project-info span {

        display:
          block;

        color:
          var(--muted);

        font-size:
          10px;

      }


      .topbar-status {

        display:
          flex;

        align-items:
          center;

        gap:
          7px;

        color:
          var(--muted);

        font-size:
          10px;

      }


      .online-dot {

        width:
          7px;

        height:
          7px;

        border-radius:
          50%;

        background:
          #22c55e;

        box-shadow:
          0 0 0 4px
          rgba(34,197,94,.08);

      }


      /* ACTIONS */

      .actionbar {

        display:
          flex;

        justify-content:
          space-between;

        align-items:
          center;

        gap:
          10px;

        padding:
          13px 18px;

        background:
          var(--surface-2);

        border-bottom:
          1px solid
          var(--border);

      }


      .action-group {

        display:
          flex;

        gap:
          8px;

      }


      .action {

        display:
          inline-flex;

        align-items:
          center;

        justify-content:
          center;

        gap:
          7px;

        border:
          1px solid
          var(--border);

        border-radius:
          10px;

        padding:
          9px 13px;

        cursor:
          pointer;

        font-size:
          11px;

        font-weight:
          700;

        transition:
          .2s ease;

      }


      .action:hover {

        transform:
          translateY(-1px);

      }


      .action.secondary {

        background:
          var(--surface);

        color:
          var(--text);

      }


      .action.secondary:hover {

        color:
          var(--primary);

        border-color:
          rgba(8,145,178,.30);

      }


      .action.primary {

        border-color:
          transparent;

        background:
          linear-gradient(
            135deg,
            #0891b2,
            #06b6d4
          );

        color:
          white;

        box-shadow:
          0 8px 22px
          rgba(8,145,178,.22);

      }


      .action.primary:hover {

        box-shadow:
          0 11px 28px
          rgba(8,145,178,.32);

      }


      /* EDITOR */

      .editor-card {

        margin:
          16px;

        overflow:
          hidden;

        border:
          1px solid
          rgba(148,163,184,.12);

        border-radius:
          16px;

        background:
          var(--code-bg);

        color:
          var(--code-text);

      }


      .editor-head {

        height:
          52px;

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        padding:
          0 14px;

        background:
          var(--code-bg-2);

        border-bottom:
          1px solid
          rgba(255,255,255,.06);

      }


      .editor-label {

        display:
          flex;

        align-items:
          center;

        gap:
          9px;

      }


      .file-icon {

        width:
          31px;

        height:
          31px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          8px;

        background:
          rgba(6,182,212,.13);

        color:
          #67e8f9;

      }


      .mini-ts {

        font:
          800 10px
          "JetBrains Mono",
          monospace;

      }


      .editor-label strong {

        display:
          block;

        font-size:
          11px;

      }


      .editor-label small {

        display:
          block;

        margin-top:
          1px;

        color:
          #64748b;

        font-size:
          9px;

      }


      .editor-stats {

        color:
          #64748b;

        font:
          9px
          "JetBrains Mono",
          monospace;

      }


      .code-area {

        display:
          flex;

        height:
          430px;

        direction:
          ltr;

        overflow:
          hidden;

      }


      .line-numbers {

        width:
          45px;

        flex:
          0 0 45px;

        padding:
          15px 8px;

        background:
          rgba(0,0,0,.14);

        border-right:
          1px solid
          rgba(255,255,255,.05);

        color:
          #475569;

        text-align:
          right;

        white-space:
          pre;

        overflow:
          hidden;

        user-select:
          none;

        font:
          12px/1.8
          "JetBrains Mono",
          monospace;

      }


      textarea {

        width:
          100%;

        height:
          100%;

        resize:
          none;

        outline:
          none;

        border:
          0;

        padding:
          15px;

        background:
          var(--code-bg);

        color:
          var(--code-text);

        font:
          12px/1.8
          "JetBrains Mono",
          monospace;

        direction:
          ltr;

        text-align:
          left;

        tab-size:
          2;

      }


      textarea::placeholder {

        color:
          #475569;

      }


      .editor-foot {

        min-height:
          38px;

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        padding:
          0 13px;

        border-top:
          1px solid
          rgba(255,255,255,.06);

        color:
          #64748b;

        font-size:
          9px;

      }


      .editor-foot span {

        display:
          flex;

        align-items:
          center;

        gap:
          5px;

      }


      /* CONSOLE */

      .console-card {

        margin:
          0 16px 16px;

        overflow:
          hidden;

        border:
          1px solid
          rgba(148,163,184,.12);

        border-radius:
          16px;

        background:
          #07141a;

        color:
          #e2f5f8;

      }


      .console-head {

        height:
          52px;

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        padding:
          0 14px;

        background:
          #0a1d25;

        border-bottom:
          1px solid
          rgba(255,255,255,.06);

      }


      .console-title {

        display:
          flex;

        align-items:
          center;

        gap:
          9px;

      }


      .console-icon {

        width:
          31px;

        height:
          31px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          8px;

        background:
          rgba(34,211,238,.10);

        color:
          #67e8f9;

        font-size:
          11px;

      }


      .console-title strong {

        display:
          block;

        font-size:
          11px;

      }


      .console-title small {

        display:
          block;

        margin-top:
          1px;

        color:
          #64748b;

        font-size:
          9px;

      }


      .console-clear {

        display:
          inline-flex;

        align-items:
          center;

        gap:
          6px;

        padding:
          7px 10px;

        border:
          1px solid
          rgba(255,255,255,.08);

        border-radius:
          8px;

        background:
          transparent;

        color:
          #94a3b8;

        cursor:
          pointer;

        font-size:
          9px;

      }


      .console-clear:hover {

        color:
          #67e8f9;

        border-color:
          rgba(34,211,238,.25);

      }


      .console-output {

        min-height:
          150px;

        max-height:
          260px;

        overflow:
          auto;

        padding:
          15px;

        direction:
          ltr;

        text-align:
          left;

        font:
          12px/1.8
          "JetBrains Mono",
          monospace;

      }


      .console-placeholder {

        min-height:
          120px;

        display:
          flex;

        flex-direction:
          column;

        align-items:
          center;

        justify-content:
          center;

        gap:
          9px;

        color:
          #475569;

        font-size:
          10px;

      }


      .console-placeholder i {

        font-size:
          22px;

        opacity:
          .7;

      }


      .console-line {

        display:
          flex;

        align-items:
          flex-start;

        gap:
          8px;

        padding:
          5px 0;

        white-space:
          pre-wrap;

        word-break:
          break-word;

      }


      .console-line-icon {

        flex:
          0 0 auto;

        width:
          16px;

        padding-top:
          2px;

        color:
          #67e8f9;

      }


      .console-line.warn .console-line-icon {

        color:
          #fbbf24;

      }


      .console-line.error .console-line-icon {

        color:
          #f87171;

      }


      .console-line-text {

        color:
          #dbeafe;

      }


      .console-line.error .console-line-text {

        color:
          #fca5a5;

      }


      .console-line.warn .console-line-text {

        color:
          #fde68a;

      }


      .console-foot {

        min-height:
          38px;

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        padding:
          0 13px;

        border-top:
          1px solid
          rgba(255,255,255,.06);

        color:
          #64748b;

        font:
          9px
          "JetBrains Mono",
          monospace;

      }


      /* STATUS */

      .status {

        display:
          flex;

        align-items:
          center;

        gap:
          6px;

      }


      .status-dot {

        width:
          6px;

        height:
          6px;

        border-radius:
          50%;

        background:
          #64748b;

      }


      .status.success {

        color:
          #4ade80;

      }


      .status.success .status-dot {

        background:
          #22c55e;

      }


      .status.warning {

        color:
          #fbbf24;

      }


      .status.warning .status-dot {

        background:
          #f59e0b;

      }


      .status.error {

        color:
          #f87171;

      }


      .status.error .status-dot {

        background:
          #ef4444;

      }


      /* STATS */

      .tool-stats {

        display:
          grid;

        grid-template-columns:
          repeat(4, 1fr);

        gap:
          10px;

        padding:
          0 16px 16px;

      }


      .mini-stat {

        display:
          flex;

        align-items:
          center;

        gap:
          10px;

        padding:
          12px;

        border:
          1px solid
          var(--border);

        border-radius:
          13px;

        background:
          var(--surface-2);

      }


      .mini-icon {

        width:
          35px;

        height:
          35px;

        flex:
          0 0 35px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          9px;

      }


      .mini-icon.cyan {

        background:
          rgba(6,182,212,.11);

        color:
          var(--primary);

      }


      .mini-icon.blue {

        background:
          rgba(14,116,144,.11);

        color:
          #0e7490;

      }


      html.dark-mode
      .mini-icon.blue {

        color:
          #67e8f9;

      }


      .mini-icon.green {

        background:
          rgba(22,163,74,.10);

        color:
          var(--green);

      }


      .mini-icon.orange {

        background:
          rgba(234,88,12,.10);

        color:
          var(--orange);

      }


      .mini-ts-large {

        font:
          800 10px
          "JetBrains Mono",
          monospace;

      }


      .mini-stat strong {

        display:
          block;

        font-size:
          10px;

      }


      .mini-stat span {

        display:
          block;

        margin-top:
          2px;

        color:
          var(--muted);

        font-size:
          8px;

      }


      /* INFORMATION */

      .information {

        margin-top:
          55px;

      }


      .section-title {

        max-width:
          700px;

        margin:
          0 auto 25px;

        text-align:
          center;

      }


      .section-title > span {

        color:
          var(--primary);

        font-size:
          11px;

        font-weight:
          800;

      }


      .section-title h2 {

        margin:
          5px 0 7px;

        font-size:
          clamp(
            21px,
            4vw,
            28px
          );

      }


      .section-title p {

        margin:
          0;

        color:
          var(--muted);

        font-size:
          12px;

        line-height:
          1.9;

      }


      .info-grid {

        display:
          grid;

        grid-template-columns:
          repeat(3, 1fr);

        gap:
          14px;

      }


      .info-card {

        position:
          relative;

        padding:
          24px;

        overflow:
          hidden;

        border:
          1px solid
          var(--border);

        border-radius:
          18px;

        background:
          var(--surface);

      }


      .info-number {

        position:
          absolute;

        top:
          15px;

        left:
          18px;

        color:
          var(--muted);

        opacity:
          .25;

        font:
          700 28px
          "JetBrains Mono",
          monospace;

      }


      .info-card-icon {

        width:
          42px;

        height:
          42px;

        display:
          grid;

        place-items:
          center;

        margin-bottom:
          13px;

        border-radius:
          11px;

        background:
          rgba(6,182,212,.10);

        color:
          var(--primary);

      }


      .info-card h3 {

        margin:
          0 0 7px;

        font-size:
          14px;

      }


      .info-card p {

        margin:
          0;

        color:
          var(--muted);

        font-size:
          11px;

        line-height:
          1.9;

      }


      /* HOW */

      .how-section {

        margin-top:
          55px;

      }


      .steps {

        display:
          grid;

        grid-template-columns:
          repeat(3, 1fr);

        gap:
          14px;

      }


      .step {

        display:
          flex;

        align-items:
          flex-start;

        gap:
          12px;

        padding:
          20px;

        background:
          var(--surface);

        border:
          1px solid
          var(--border);

        border-radius:
          17px;

      }


      .step-icon {

        width:
          43px;

        height:
          43px;

        flex:
          0 0 43px;

        display:
          grid;

        place-items:
          center;

        border-radius:
          12px;

        background:
          linear-gradient(
            135deg,
            rgba(8,145,178,.12),
            rgba(6,182,212,.12)
          );

        color:
          var(--primary);

        font:
          700 10px
          "JetBrains Mono",
          monospace;

      }


      .step h3 {

        margin:
          0 0 5px;

        font-size:
          13px;

      }


      .step p {

        margin:
          0;

        color:
          var(--muted);

        font-size:
          10px;

        line-height:
          1.8;

      }


      /* FAQ */

      .faq-section {

        max-width:
          850px;

        margin:
          55px auto 70px;

      }


      .faq-list details {

        margin-bottom:
          9px;

        overflow:
          hidden;

        border:
          1px solid
          var(--border);

        border-radius:
          14px;

        background:
          var(--surface);

      }


      .faq-list summary {

        display:
          flex;

        align-items:
          center;

        justify-content:
          space-between;

        gap:
          15px;

        padding:
          16px 18px;

        cursor:
          pointer;

        list-style:
          none;

        font-size:
          12px;

        font-weight:
          700;

      }


      .faq-list summary::-webkit-details-marker {

        display:
          none;

      }


      .faq-list summary i {

        color:
          var(--muted);

        font-size:
          10px;

        transition:
          transform .2s ease;

      }


      .faq-list details[open]
      summary i {

        transform:
          rotate(180deg);

      }


      .faq-answer {

        padding:
          0 18px 17px;

        color:
          var(--muted);

        font-size:
          11px;

        line-height:
          1.9;

      }


      /* RESPONSIVE */

      @media (max-width: 900px) {

        .info-grid,
        .steps {

          grid-template-columns:
            repeat(2, 1fr);

        }

      }


      @media (max-width: 700px) {

        .tool-stats {

          grid-template-columns:
            repeat(2, 1fr);

        }


        .actionbar {

          flex-direction:
            column;

          align-items:
            stretch;

        }


        .action-group {

          width:
            100%;

        }


        .action {

          flex:
            1;

        }

      }


      @media (max-width: 600px) {

        .container {

          width:
            calc(100% - 20px);

        }


        .hero {

          padding:
            25px 0 32px;

        }


        .hero-icon {

          width:
            62px;

          height:
            62px;

        }


        .hero p {

          font-size:
            12px;

        }


        .topbar {

          padding:
            14px;

        }


        .topbar-status {

          display:
            none;

        }


        .actionbar {

          padding:
            10px;

        }


        .action {

          padding:
            9px 7px;

          font-size:
            10px;

        }


        .editor-card {

          margin:
            10px;

        }


        .console-card {

          margin:
            0 10px 10px;

        }


        .code-area {

          height:
            330px;

        }


        .console-output {

          min-height:
            130px;

        }

      }


      @media (max-width: 500px) {

        .info-grid,
        .steps {

          grid-template-columns:
            1fr;

        }


        .project-info span {

          display:
            none;

        }


        .project-logo {

          width:
            36px;

          height:
            36px;

        }

      }

    </style>

  </body>
</html>
