---
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const title = "URL Validator - التحقق من URL أونلاين مجاناً | Th.Tech";

const description =
  "أداة URL Validator مجانية للتحقق من صحة الروابط وعناوين URL وتحليل البروتوكول والنطاق والمسار والمعلمات مباشرة من المتصفح.";

const faqs = [
  {
    q: "ما هي أداة URL Validator؟",
    a: "هي أداة تساعدك على التحقق من صحة صيغة عنوان URL والتأكد من إمكانية تحليله كعنوان ويب صالح."
  },
  {
    q: "ما الذي تتحقق منه الأداة؟",
    a: "تتحقق الأداة من صيغة URL، والبروتوكول، واسم النطاق، والمسار، والمنفذ، ومعلمات البحث وغيرها من مكونات الرابط."
  },
  {
    q: "هل يتم إرسال الرابط إلى الخادم؟",
    a: "لا. تتم عملية التحقق والتحليل بالكامل داخل المتصفح."
  },
  {
    q: "هل تدعم الأداة HTTPS؟",
    a: "نعم، تدعم الأداة عناوين HTTP وHTTPS وتعرض نوع البروتوكول المستخدم."
  }
];
---

<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>{title}</title>
  <meta name="description" content={description} />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

  <link
    href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap"
    rel="stylesheet"
  />

  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
  />

  <script is:inline>
    (() => {
      try {
        const saved = localStorage.getItem("theme");

        if (saved === "dark") {
          document.documentElement.classList.add("dark-mode");
        }
      } catch {}
    })();
  </script>
</head>

<body>
  <Header />

  <main>

    <!-- HERO -->
    <section class="hero">
      <div class="container">

        <div class="breadcrumb">
          <a href={`${base}/`}>الرئيسية</a>
          <i class="fa-solid fa-chevron-left"></i>
          <a href={`${base}/tools/`}>الأدوات</a>
          <i class="fa-solid fa-chevron-left"></i>
          <span>URL Validator</span>
        </div>

        <div class="hero-content">

          <div class="hero-icon">
            <i class="fa-solid fa-circle-check"></i>
            <span>URL</span>
          </div>

          <div class="hero-text">

            <div class="badge">
              <i class="fa-solid fa-shield-halved"></i>
              Developer Tools
            </div>

            <h1>URL Validator</h1>

            <p>
              تحقق من صحة عناوين URL وحلل مكوناتها بسرعة
              مباشرة من متصفحك.
            </p>

            <div class="tags">
              <span>URL Validation</span>
              <span>HTTP</span>
              <span>HTTPS</span>
              <span>Local</span>
            </div>

          </div>

        </div>
      </div>
    </section>


    <!-- TOOL -->
    <section class="tool-section">
      <div class="container">

        <div class="validator-card">

          <!-- TOP BAR -->
          <div class="card-topbar">

            <div class="window-dots">
              <span></span>
              <span></span>
              <span></span>
            </div>

            <div class="tool-name">
              <i class="fa-solid fa-circle-check"></i>
              URL Validator
            </div>

            <div class="secure">
              <i class="fa-solid fa-shield-halved"></i>
              Local
            </div>

          </div>


          <!-- ACTION BAR -->
          <div class="actionbar">

            <button id="exampleBtn" class="btn btn-secondary">
              <i class="fa-solid fa-flask"></i>
              مثال
            </button>

            <button id="clearBtn" class="btn btn-secondary">
              <i class="fa-solid fa-eraser"></i>
              مسح
            </button>

            <button id="copyBtn" class="btn btn-secondary">
              <i class="fa-regular fa-copy"></i>
              نسخ
            </button>

            <button id="validateBtn" class="btn btn-primary">
              <i class="fa-solid fa-circle-check"></i>
              التحقق من URL
            </button>

          </div>


          <!-- INPUT -->
          <div class="input-section">

            <div class="editor-header">

              <div>
                <i class="fa-solid fa-link"></i>
                عنوان URL
              </div>

              <span id="inputCount">0 حرف</span>

            </div>

            <div class="url-editor">

              <div class="url-icon">
                <i class="fa-solid fa-globe"></i>
              </div>

              <textarea
                id="urlInput"
                rows="3"
                spellcheck="false"
                placeholder="https://example.com/path?query=value"
              ></textarea>

            </div>

          </div>


          <!-- RESULT -->
          <div
            class="result-box"
            id="resultBox"
          >

            <div
              class="result-icon"
              id="resultIcon"
            >
              <i class="fa-solid fa-circle-question"></i>
            </div>

            <div class="result-content">

              <strong id="resultTitle">
                جاهز للتحقق
              </strong>

              <p id="resultMessage">
                أدخل عنوان URL ثم اضغط على زر التحقق.
              </p>

            </div>

          </div>


          <!-- URL DETAILS -->
          <div
            class="details-panel"
            id="detailsPanel"
          >

            <div class="details-title">
              <i class="fa-solid fa-magnifying-glass"></i>
              تحليل عنوان URL
            </div>

            <div class="details-grid">

              <div class="detail-item">
                <span>
                  <i class="fa-solid fa-globe"></i>
                  البروتوكول
                </span>
                <strong id="protocolValue">—</strong>
              </div>

              <div class="detail-item">
                <span>
                  <i class="fa-solid fa-server"></i>
                  النطاق
                </span>
                <strong id="hostnameValue">—</strong>
              </div>

              <div class="detail-item">
                <span>
                  <i class="fa-solid fa-plug"></i>
                  المنفذ
                </span>
                <strong id="portValue">—</strong>
              </div>

              <div class="detail-item">
                <span>
                  <i class="fa-solid fa-folder-open"></i>
                  المسار
                </span>
                <strong id="pathnameValue">—</strong>
              </div>

              <div class="detail-item">
                <span>
                  <i class="fa-solid fa-magnifying-glass"></i>
                  Query
                </span>
                <strong id="queryValue">—</strong>
              </div>

              <div class="detail-item">
                <span>
                  <i class="fa-solid fa-hashtag"></i>
                  Fragment
                </span>
                <strong id="hashValue">—</strong>
              </div>

            </div>

          </div>


          <!-- STATS -->
          <div class="stats-grid">

            <div class="stat">

              <div class="stat-icon">
                <i class="fa-solid fa-link"></i>
              </div>

              <div>
                <span>طول URL</span>
                <strong id="statLength">0</strong>
              </div>

            </div>


            <div class="stat">

              <div class="stat-icon">
                <i class="fa-solid fa-globe"></i>
              </div>

              <div>
                <span>البروتوكول</span>
                <strong id="statProtocol">—</strong>
              </div>

            </div>


            <div class="stat">

              <div class="stat-icon">
                <i class="fa-solid fa-server"></i>
              </div>

              <div>
                <span>النطاق</span>
                <strong id="statDomain">—</strong>
              </div>

            </div>


            <div class="stat">

              <div class="stat-icon">
                <i class="fa-solid fa-bolt"></i>
              </div>

              <div>
                <span>الحالة</span>
                <strong id="statStatus">جاهز</strong>
              </div>

            </div>

          </div>

        </div>

      </div>
    </section>


    <!-- INFO -->
    <section class="info-section">
      <div class="container">

        <div class="section-heading">

          <span>URL VALIDATION</span>

          <h2>
            ما هو URL Validator؟
          </h2>

          <p>
            أداة للتحقق من صحة صيغة عناوين URL وتحليل
            المكونات الأساسية للرابط بطريقة سهلة وسريعة.
          </p>

        </div>


        <div class="info-grid">

          <article class="info-card">

            <div class="info-icon">
              <i class="fa-solid fa-circle-check"></i>
            </div>

            <h3>
              التحقق من الصيغة
            </h3>

            <p>
              تتحقق الأداة من إمكانية تحليل النص كعنوان URL
              صالح وتكشف الصيغ غير الصحيحة.
            </p>

          </article>


          <article class="info-card">

            <div class="info-icon">
              <i class="fa-solid fa-sitemap"></i>
            </div>

            <h3>
              تحليل المكونات
            </h3>

            <p>
              اعرض البروتوكول والنطاق والمنفذ والمسار
              ومعلمات البحث وFragment.
            </p>

          </article>


          <article class="info-card">

            <div class="info-icon">
              <i class="fa-solid fa-shield-halved"></i>
            </div>

            <h3>
              خصوصية كاملة
            </h3>

            <p>
              تتم عملية التحقق محلياً داخل المتصفح دون
              إرسال الرابط إلى أي خادم.
            </p>

          </article>

        </div>

      </div>
    </section>


    <!-- HOW TO -->
    <section class="how-section">
      <div class="container">

        <div class="section-heading">

          <span>HOW IT WORKS</span>

          <h2>
            كيفية استخدام URL Validator
          </h2>

        </div>


        <div class="steps">

          <div class="step">

            <div class="step-number">
              1
            </div>

            <div>
              <h3>
                أدخل عنوان URL
              </h3>

              <p>
                الصق الرابط الذي تريد التحقق من صحته.
              </p>
            </div>

          </div>


          <div class="step">

            <div class="step-number">
              2
            </div>

            <div>
              <h3>
                اضغط تحقق
              </h3>

              <p>
                ستقوم الأداة بتحليل الرابط والتحقق من صيغته.
              </p>
            </div>

          </div>


          <div class="step">

            <div class="step-number">
              3
            </div>

            <div>
              <h3>
                راجع التفاصيل
              </h3>

              <p>
                شاهد البروتوكول والنطاق والمسار وبقية مكونات الرابط.
              </p>
            </div>

          </div>

        </div>

      </div>
    </section>


    <!-- FAQ -->
    <section class="faq-section">
      <div class="container">

        <div class="section-heading">

          <span>FAQ</span>

          <h2>
            الأسئلة الشائعة
          </h2>

        </div>


        <div class="faq-list">

          {faqs.map((faq) => (
            <details>

              <summary>

                <span>
                  {faq.q}
                </span>

                <i class="fa-solid fa-chevron-down"></i>

              </summary>

              <p>
                {faq.a}
              </p>

            </details>
          ))}

        </div>

      </div>
    </section>

  </main>


  <Footer />


  <!-- JAVASCRIPT -->
  <script is:inline>
    (() => {

      const input =
        document.getElementById("urlInput");

      const validateBtn =
        document.getElementById("validateBtn");

      const exampleBtn =
        document.getElementById("exampleBtn");

      const clearBtn =
        document.getElementById("clearBtn");

      const copyBtn =
        document.getElementById("copyBtn");


      const inputCount =
        document.getElementById("inputCount");


      const resultBox =
        document.getElementById("resultBox");

      const resultIcon =
        document.getElementById("resultIcon");

      const resultTitle =
        document.getElementById("resultTitle");

      const resultMessage =
        document.getElementById("resultMessage");


      const detailsPanel =
        document.getElementById("detailsPanel");


      const protocolValue =
        document.getElementById("protocolValue");

      const hostnameValue =
        document.getElementById("hostnameValue");

      const portValue =
        document.getElementById("portValue");

      const pathnameValue =
        document.getElementById("pathnameValue");

      const queryValue =
        document.getElementById("queryValue");

      const hashValue =
        document.getElementById("hashValue");


      const statLength =
        document.getElementById("statLength");

      const statProtocol =
        document.getElementById("statProtocol");

      const statDomain =
        document.getElementById("statDomain");

      const statStatus =
        document.getElementById("statStatus");


      function updateCount() {

        const length =
          input.value.length;

        inputCount.textContent =
          `${length.toLocaleString()} حرف`;

        statLength.textContent =
          length.toLocaleString();
      }


      function resetDetails() {

        protocolValue.textContent = "—";
        hostnameValue.textContent = "—";
        portValue.textContent = "—";
        pathnameValue.textContent = "—";
        queryValue.textContent = "—";
        hashValue.textContent = "—";

        statProtocol.textContent = "—";
        statDomain.textContent = "—";
      }


      function setSuccess(message) {

        resultBox.classList.remove("error");

        resultBox.classList.add("success");


        resultIcon.innerHTML =
          '<i class="fa-solid fa-circle-check"></i>';


        resultTitle.textContent =
          "URL صالح";


        resultMessage.textContent =
          message;


        statStatus.textContent =
          "صالح";
      }


      function setError(message) {

        resultBox.classList.remove("success");

        resultBox.classList.add("error");


        resultIcon.innerHTML =
          '<i class="fa-solid fa-circle-xmark"></i>';


        resultTitle.textContent =
          "URL غير صالح";


        resultMessage.textContent =
          message;


        statStatus.textContent =
          "غير صالح";


        detailsPanel.classList.remove("visible");

        resetDetails();
      }


      function setReady() {

        resultBox.classList.remove(
          "success",
          "error"
        );


        resultIcon.innerHTML =
          '<i class="fa-solid fa-circle-question"></i>';


        resultTitle.textContent =
          "جاهز للتحقق";


        resultMessage.textContent =
          "أدخل عنوان URL ثم اضغط على زر التحقق.";


        statStatus.textContent =
          "جاهز";


        detailsPanel.classList.remove(
          "visible"
        );
      }


      function validateURL() {

        const value =
          input.value.trim();


        updateCount();


        if (!value) {

          setError(
            "أدخل عنوان URL أولاً."
          );

          return;
        }


        const start =
          performance.now();


        try {

          const url =
            new URL(value);


          if (
            url.protocol !== "http:" &&
            url.protocol !== "https:"
          ) {

            setError(
              "البروتوكول غير مدعوم. استخدم HTTP أو HTTPS."
            );

            return;
          }


          if (!url.hostname) {

            setError(
              "لم يتم العثور على اسم نطاق صالح."
            );

            return;
          }


          protocolValue.textContent =
            url.protocol.replace(":", "").toUpperCase();


          hostnameValue.textContent =
            url.hostname;


          portValue.textContent =
            url.port || "الافتراضي";


          pathnameValue.textContent =
            url.pathname || "/";


          queryValue.textContent =
            url.search || "لا توجد";


          hashValue.textContent =
            url.hash || "لا يوجد";


          statProtocol.textContent =
            url.protocol
              .replace(":", "")
              .toUpperCase();


          statDomain.textContent =
            url.hostname;


          detailsPanel.classList.add(
            "visible"
          );


          const end =
            performance.now();


          setSuccess(
            `تم التحقق من الرابط بنجاح خلال ${(end - start).toFixed(2)} ms.`
          );

        } catch (error) {

          setError(
            "صيغة عنوان URL غير صحيحة. تأكد من كتابة الرابط بشكل صحيح."
          );
        }
      }


      function loadExample() {

        input.value =
          "https://th.tech/tools/url-validator/?query=test&lang=ar#result";


        updateCount();

        validateURL();
      }


      function clearAll() {

        input.value = "";

        updateCount();

        resetDetails();

        setReady();

        input.focus();
      }


      async function copyURL() {

        const value =
          input.value.trim();


        if (!value) {

          setError(
            "لا يوجد URL لنسخه."
          );

          return;
        }


        try {

          await navigator.clipboard.writeText(
            value
          );


          const original =
            copyBtn.innerHTML;


          copyBtn.innerHTML =
            '<i class="fa-solid fa-check"></i> تم النسخ';


          setTimeout(() => {

            copyBtn.innerHTML =
              original;

          }, 1500);

        } catch {

          input.select();

          document.execCommand("copy");

          copyBtn.innerHTML =
            '<i class="fa-solid fa-check"></i> تم النسخ';


          setTimeout(() => {

            copyBtn.innerHTML =
              '<i class="fa-regular fa-copy"></i> نسخ';

          }, 1500);
        }
      }


      input.addEventListener(
        "input",
        updateCount
      );


      input.addEventListener(
        "keydown",
        (event) => {

          if (
            event.ctrlKey &&
            event.key === "Enter"
          ) {

            event.preventDefault();

            validateURL();
          }

        }
      );


      validateBtn.addEventListener(
        "click",
        validateURL
      );


      exampleBtn.addEventListener(
        "click",
        loadExample
      );


      clearBtn.addEventListener(
        "click",
        clearAll
      );


      copyBtn.addEventListener(
        "click",
        copyURL
      );


      updateCount();

      resetDetails();

    })();
  </script>


  <style>

    :root {

      --bg: #f7fafc;
      --surface: #ffffff;
      --surface-2: #f8fafc;

      --text: #172033;
      --muted: #718096;

      --border:
        rgba(15, 23, 42, 0.09);

      --primary: #0891b2;
      --primary-dark: #0e7490;
      --secondary: #22d3ee;

      --success: #16a34a;
      --danger: #dc2626;

      --shadow:
        0 25px 70px
        rgba(15, 23, 42, 0.10);
    }


    html.dark-mode {

      --bg: #070b10;
      --surface: #0d141a;
      --surface-2: #121b23;

      --text: #f8fafc;
      --muted: #94a3b8;

      --border:
        rgba(148, 163, 184, 0.13);

      --primary: #22d3ee;
      --primary-dark: #06b6d4;
      --secondary: #67e8f9;

      --shadow:
        0 30px 80px
        rgba(0, 0, 0, 0.45);
    }


    * {
      box-sizing: border-box;
    }


    body {

      margin: 0;

      background:
        var(--bg);

      color:
        var(--text);

      font-family:
        "Cairo",
        sans-serif;
    }


    .container {

      width:
        min(
          1180px,
          calc(100% - 32px)
        );

      margin:
        auto;
    }


    /* HERO */

    .hero {

      padding:
        38px 0 55px;

      background:
        radial-gradient(
          circle at 10% 20%,
          rgba(8, 145, 178, 0.13),
          transparent 35%
        ),
        var(--bg);
    }


    .breadcrumb {

      display:
        flex;

      align-items:
        center;

      gap:
        10px;

      flex-wrap:
        wrap;

      color:
        var(--muted);

      font-size:
        13px;

      margin-bottom:
        38px;
    }


    .breadcrumb a {

      color:
        var(--muted);

      text-decoration:
        none;
    }


    .breadcrumb i {

      font-size:
        9px;
    }


    .hero-content {

      display:
        flex;

      align-items:
        center;

      gap:
        28px;
    }


    .hero-icon {

      width:
        112px;

      height:
        112px;

      flex:
        0 0 112px;

      border-radius:
        30px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      flex-direction:
        column;

      gap:
        4px;

      background:
        rgba(8, 145, 178, 0.12);

      color:
        var(--primary);

      box-shadow:
        inset 0 0 0 1px
        rgba(8, 145, 178, 0.10);
    }


    .hero-icon i {

      font-size:
        42px;
    }


    .hero-icon span {

      font:
        700 13px
        "JetBrains Mono",
        monospace;
    }


    .badge {

      display:
        inline-flex;

      align-items:
        center;

      gap:
        8px;

      padding:
        7px 13px;

      border-radius:
        999px;

      background:
        rgba(8, 145, 178, 0.10);

      color:
        var(--primary);

      font-size:
        12px;

      font-weight:
        700;
    }


    h1 {

      margin:
        12px 0 8px;

      font-size:
        clamp(
          32px,
          5vw,
          52px
        );

      line-height:
        1.1;
    }


    .hero-text p {

      margin:
        0;

      max-width:
        700px;

      color:
        var(--muted);

      font-size:
        16px;

      line-height:
        1.9;
    }


    .tags {

      display:
        flex;

      flex-wrap:
        wrap;

      gap:
        8px;

      margin-top:
        17px;
    }


    .tags span {

      padding:
        5px 10px;

      border:
        1px solid
        var(--border);

      border-radius:
        8px;

      color:
        var(--muted);

      background:
        var(--surface);

      font:
        500 11px
        "JetBrains Mono",
        monospace;
    }


    /* TOOL */

    .tool-section {

      padding-bottom:
        70px;
    }


    .validator-card {

      overflow:
        hidden;

      background:
        var(--surface);

      border:
        1px solid
        var(--border);

      border-radius:
        22px;

      box-shadow:
        var(--shadow);
    }


    .card-topbar {

      min-height:
        55px;

      padding:
        0 20px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        space-between;

      border-bottom:
        1px solid
        var(--border);

      background:
        var(--surface-2);
    }


    .window-dots {

      display:
        flex;

      gap:
        6px;
    }


    .window-dots span {

      width:
        9px;

      height:
        9px;

      border-radius:
        50%;

      background:
        var(--border);
    }


    .tool-name {

      display:
        flex;

      align-items:
        center;

      gap:
        8px;

      color:
        var(--text);

      font:
        600 13px
        "JetBrains Mono",
        monospace;
    }


    .tool-name i {

      color:
        var(--primary);
    }


    .secure {

      display:
        flex;

      align-items:
        center;

      gap:
        6px;

      color:
        var(--success);

      font-size:
        11px;

      font-weight:
        700;
    }


    .actionbar {

      display:
        flex;

      flex-wrap:
        wrap;

      gap:
        9px;

      padding:
        15px 18px;

      border-bottom:
        1px solid
        var(--border);
    }


    .btn {

      border:
        1px solid
        var(--border);

      border-radius:
        10px;

      padding:
        10px 15px;

      font-family:
        "Cairo",
        sans-serif;

      font-size:
        12px;

      font-weight:
        700;

      cursor:
        pointer;

      transition:
        0.2s;
    }


    .btn:hover {

      transform:
        translateY(-1px);
    }


    .btn i {

      margin-left:
        6px;
    }


    .btn-secondary {

      color:
        var(--text);

      background:
        var(--surface-2);
    }


    .btn-primary {

      color:
        white;

      background:
        var(--primary);

      border-color:
        var(--primary);
    }


    .btn-primary:hover {

      background:
        var(--primary-dark);
    }


    /* INPUT */

    .input-section {

      margin:
        20px;

      overflow:
        hidden;

      border:
        1px solid
        var(--border);

      border-radius:
        15px;

      background:
        var(--surface-2);
    }


    .editor-header {

      display:
        flex;

      align-items:
        center;

      justify-content:
        space-between;

      padding:
        11px 14px;

      border-bottom:
        1px solid
        var(--border);

      color:
        var(--muted);

      font-size:
        12px;

      font-weight:
        700;
    }


    .editor-header div {

      display:
        flex;

      align-items:
        center;

      gap:
        8px;

      color:
        var(--text);
    }


    .editor-header i {

      color:
        var(--primary);
    }


    .editor-header span {

      font:
        10px
        "JetBrains Mono",
        monospace;
    }


    .url-editor {

      display:
        flex;

      align-items:
        flex-start;

      gap:
        12px;

      padding:
        18px;
    }


    .url-icon {

      width:
        42px;

      height:
        42px;

      flex:
        0 0 42px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      border-radius:
        11px;

      color:
        var(--primary);

      background:
        rgba(8, 145, 178, 0.10);
    }


    textarea {

      width:
        100%;

      min-height:
        110px;

      resize:
        vertical;

      padding:
        10px 4px;

      border:
        0;

      outline:
        0;

      background:
        transparent;

      color:
        var(--text);

      font:
        14px/1.8
        "JetBrains Mono",
        monospace;

      direction:
        ltr;

      text-align:
        left;
    }


    textarea::placeholder {

      color:
        var(--muted);

      opacity:
        0.7;
    }


    /* RESULT */

    .result-box {

      margin:
        0 20px 15px;

      display:
        flex;

      align-items:
        center;

      gap:
        14px;

      padding:
        15px 17px;

      border:
        1px solid
        var(--border);

      border-radius:
        14px;

      background:
        var(--surface-2);
    }


    .result-icon {

      width:
        42px;

      height:
        42px;

      flex:
        0 0 42px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      border-radius:
        11px;

      background:
        rgba(8, 145, 178, 0.10);

      color:
        var(--primary);

      font-size:
        17px;
    }


    .result-box strong {

      display:
        block;

      font-size:
        13px;
    }


    .result-box p {

      margin:
        3px 0 0;

      color:
        var(--muted);

      font-size:
        11px;

      line-height:
        1.7;
    }


    .result-box.success {

      border-color:
        rgba(22, 163, 74, 0.25);
    }


    .result-box.success .result-icon {

      color:
        var(--success);

      background:
        rgba(22, 163, 74, 0.10);
    }


    .result-box.error {

      border-color:
        rgba(220, 38, 38, 0.25);
    }


    .result-box.error .result-icon {

      color:
        var(--danger);

      background:
        rgba(220, 38, 38, 0.10);
    }


    /* DETAILS */

    .details-panel {

      display:
        none;

      margin:
        0 20px 15px;

      padding:
        18px;

      border:
        1px solid
        var(--border);

      border-radius:
        15px;

      background:
        var(--surface-2);
    }


    .details-panel.visible {

      display:
        block;
    }


    .details-title {

      display:
        flex;

      align-items:
        center;

      gap:
        8px;

      margin-bottom:
        15px;

      font-size:
        13px;

      font-weight:
        800;
    }


    .details-title i {

      color:
        var(--primary);
    }


    .details-grid {

      display:
        grid;

      grid-template-columns:
        repeat(3, 1fr);

      gap:
        10px;
    }


    .detail-item {

      min-width:
        0;

      padding:
        13px;

      border:
        1px solid
        var(--border);

      border-radius:
        11px;

      background:
        var(--surface);
    }


    .detail-item span {

      display:
        flex;

      align-items:
        center;

      gap:
        6px;

      color:
        var(--muted);

      font-size:
        10px;

      margin-bottom:
        6px;
    }


    .detail-item span i {

      color:
        var(--primary);
    }


    .detail-item strong {

      display:
        block;

      overflow:
        hidden;

      color:
        var(--text);

      text-overflow:
        ellipsis;

      white-space:
        nowrap;

      direction:
        ltr;

      text-align:
        left;

      font:
        500 11px
        "JetBrains Mono",
        monospace;
    }


    /* STATS */

    .stats-grid {

      display:
        grid;

      grid-template-columns:
        repeat(4, 1fr);

      gap:
        12px;

      padding:
        0 20px 20px;
    }


    .stat {

      display:
        flex;

      align-items:
        center;

      gap:
        11px;

      min-width:
        0;

      padding:
        14px;

      border:
        1px solid
        var(--border);

      border-radius:
        13px;

      background:
        var(--surface-2);
    }


    .stat-icon {

      width:
        36px;

      height:
        36px;

      flex:
        0 0 36px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      border-radius:
        9px;

      color:
        var(--primary);

      background:
        rgba(8, 145, 178, 0.10);
    }


    .stat span {

      display:
        block;

      color:
        var(--muted);

      font-size:
        9px;
    }


    .stat strong {

      display:
        block;

      max-width:
        150px;

      overflow:
        hidden;

      margin-top:
        2px;

      color:
        var(--text);

      text-overflow:
        ellipsis;

      white-space:
        nowrap;

      font:
        600 11px
        "JetBrains Mono",
        monospace;
    }


    /* SECTIONS */

    .info-section,
    .how-section,
    .faq-section {

      padding:
        70px 0;
    }


    .info-section {

      background:
        var(--surface-2);

      border-top:
        1px solid
        var(--border);

      border-bottom:
        1px solid
        var(--border);
    }


    .section-heading {

      max-width:
        700px;

      margin:
        0 auto 35px;

      text-align:
        center;
    }


    .section-heading > span {

      color:
        var(--primary);

      font:
        700 11px
        "JetBrains Mono",
        monospace;

      letter-spacing:
        1px;
    }


    .section-heading h2 {

      margin:
        8px 0;

      font-size:
        clamp(
          25px,
          4vw,
          34px
        );
    }


    .section-heading p {

      margin:
        0;

      color:
        var(--muted);

      line-height:
        1.9;

      font-size:
        14px;
    }


    .info-grid {

      display:
        grid;

      grid-template-columns:
        repeat(3, 1fr);

      gap:
        18px;
    }


    .info-card {

      padding:
        25px;

      border:
        1px solid
        var(--border);

      border-radius:
        17px;

      background:
        var(--surface);
    }


    .info-icon {

      width:
        48px;

      height:
        48px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      margin-bottom:
        17px;

      border-radius:
        13px;

      color:
        var(--primary);

      background:
        rgba(8, 145, 178, 0.10);
    }


    .info-card h3 {

      margin:
        0 0 7px;

      font-size:
        16px;
    }


    .info-card p {

      margin:
        0;

      color:
        var(--muted);

      font-size:
        13px;

      line-height:
        1.9;
    }


    /* STEPS */

    .steps {

      display:
        grid;

      grid-template-columns:
        repeat(3, 1fr);

      gap:
        18px;
    }


    .step {

      display:
        flex;

      gap:
        15px;

      padding:
        22px;

      border:
        1px solid
        var(--border);

      border-radius:
        16px;

      background:
        var(--surface);
    }


    .step-number {

      width:
        38px;

      height:
        38px;

      flex:
        0 0 38px;

      display:
        flex;

      align-items:
        center;

      justify-content:
        center;

      border-radius:
        10px;

      color:
        white;

      background:
        var(--primary);

      font-weight:
        800;
    }


    .step h3 {

      margin:
        0 0 5px;

      font-size:
        14px;
    }


    .step p {

      margin:
        0;

      color:
        var(--muted);

      font-size:
        12px;

      line-height:
        1.8;
    }


    /* FAQ */

    .faq-list {

      max-width:
        850px;

      margin:
        auto;
    }


    details {

      margin-bottom:
        10px;

      border:
        1px solid
        var(--border);

      border-radius:
        13px;

      background:
        var(--surface);

      overflow:
        hidden;
    }


    summary {

      display:
        flex;

      align-items:
        center;

      justify-content:
        space-between;

      padding:
        17px 19px;

      cursor:
        pointer;

      font-size:
        13px;

      font-weight:
        700;

      list-style:
        none;
    }


    summary::-webkit-details-marker {

      display:
        none;
    }


    summary i {

      color:
        var(--primary);

      transition:
        transform 0.2s;
    }


    details[open] summary i {

      transform:
        rotate(180deg);
    }


    details p {

      margin:
        0;

      padding:
        0 19px 18px;

      color:
        var(--muted);

      font-size:
        12px;

      line-height:
        1.9;
    }


    /* RESPONSIVE */

    @media (max-width: 850px) {

      .details-grid {

        grid-template-columns:
          repeat(2, 1fr);
      }


      .stats-grid {

        grid-template-columns:
          repeat(2, 1fr);
      }


      .info-grid,
      .steps {

        grid-template-columns:
          1fr;
      }
    }


    @media (max-width: 600px) {

      .container {

        width:
          min(
            100% - 22px,
            1180px
          );
      }


      .hero {

        padding-top:
          25px;
      }


      .hero-content {

        align-items:
          flex-start;

        flex-direction:
          column;
      }


      .hero-icon {

        width:
          88px;

        height:
          88px;

        flex-basis:
          88px;

        border-radius:
          23px;
      }


      .hero-icon i {

        font-size:
          31px;
      }


      .card-topbar {

        padding:
          0 13px;
      }


      .actionbar {

        padding:
          12px;
      }


      .btn {

        flex:
          1 1
          calc(50% - 8px);

        padding:
          9px 8px;
      }


      .input-section {

        margin:
          12px;
      }


      .url-editor {

        padding:
          13px;
      }


      .url-icon {

        width:
          36px;

        height:
          36px;

        flex-basis:
          36px;
      }


      textarea {

        min-height:
          120px;

        font-size:
          12px;
      }


      .result-box {

        margin:
          0 12px 12px;
      }


      .details-panel {

        margin:
          0 12px 12px;

        padding:
          13px;
      }


      .details-grid {

        grid-template-columns:
          1fr 1fr;

        gap:
          8px;
      }


      .detail-item {

        padding:
          10px;
      }


      .stats-grid {

        padding:
          0 12px 12px;

        gap:
          8px;
      }


      .stat {

        padding:
          11px 9px;
      }


      .stat-icon {

        width:
          31px;

        height:
          31px;

        flex-basis:
          31px;
      }


      .stat span {

        font-size:
          8px;
      }


      .stat strong {

        max-width:
          95px;

        font-size:
          9px;
      }


      .info-section,
      .how-section,
      .faq-section {

        padding:
          50px 0;
      }

    }

  </style>

</body>
</html>
